# blog

개인 기술 블로그 + 노션/깃헙에 흩어져 있던 학습 노트 아카이브를 하나로 합친 것.

**DB가 정본이다.** 글은 웹 UI에서 직접 쓰고 고친다. 마크다운 파일을 정본으로 두고
빌드해서 배포하는 정적 사이트 생성기가 아니다. 파일 기반 워크플로를 전제한 제안
(콘텐츠를 `content/*.md`로 빼기, 프론트매터 파싱, 빌드 시 렌더링 등)은 이 프로젝트의
전제와 어긋난다.

## 이 파일이 곧 인수인계다

**대화가 아니라 이 파일이 기록이다.** 도구를 바꿔도(Claude Code → Codex 등) 여기부터
읽으면 이어서 일할 수 있다. `AGENTS.md`는 이 파일을 가리키는 심볼릭 링크다 —
Codex는 `AGENTS.md`를, Claude Code는 `CLAUDE.md`를 읽는데 둘이 갈라지면 안 된다.

**새로 이어받았다면 이 순서로 확인한다.**

```sh
git log --oneline -8          # 최근에 무엇을 했는지
go test ./...                 # 깨진 것이 없는지
go run ./cmd/blog -db blog.db -addr 127.0.0.1:8080   # 눈으로 확인
```

- **정본은 서버의 `/var/lib/blog/blog.db`다** (2026-08-31에 뒤집었다). 로컬
  `blog.db`는 이관 파이프라인을 돌리는 **작업본**이다. 새 기계에서 시작하거나
  서버 것을 보고 싶으면 **받아온다**:

  ```sh
  ./deploy/fetch-db.sh          # → backups/blog-<UTC>.db (로컬 blog.db를 덮지 않는다)
  ```

- **`blog.db`는 gitignore다.** 지금까지의 삭제·이동이 전부 이 파일에만 있다.
  받아올 수 없는 상황이면 아래 파이프라인을 **이 순서로** 돌려야 같은 상태가 된다:

  ```sh
  go run ./cmd/import -db blog.db      # 덤프 → DB (curation의 Drop/BodyEdits 반영)
  go run ./cmd/relink -db blog.db -apply
  go run ./cmd/categorize -db blog.db -apply
  go run ./cmd/regroup -db blog.db -apply
  ```

  `postparent`는 카테고리마다 따로 돌린다(아래 "글 계층" 참고).
  **그 결과를 서버로 올리는 것은 `./deploy/upload-db.sh`뿐이다** — 웹에서 쓴
  글을 덮으면 멈춘다. 아래 "DB 백업과 정본 방향" 참고.
- **사람이 정한 결정은 전부 `internal/curation`에 있다.** DB를 손으로 고치지 마라 —
  다음 `import -db`가 덮어쓴다. 표에 적으면 몇 번을 다시 이관해도 같은 결과가 나온다.
- **네 도구를 어떤 순서로 몇 번 돌려도 결과가 같아야 한다.** 바꾼 뒤에는 두 바퀴
  돌려 확인할 것. 실제로 categorize와 regroup이 서로 지운 것을 되살리며 싸운 적이 있다.

## 스택

- **Go 단일 바이너리** — `html/template`로 서버사이드 렌더링, `embed.FS`로 템플릿과
  정적 자산을 바이너리에 포함. 배포물은 바이너리 + SQLite 파일.
- **SQLite** — 애플리케이션 DB.
- **프론트엔드 프레임워크 없음, 빌드 스텝 없음.** npm/webpack/vite/tailwind CLI 등
  자산 빌드 파이프라인을 도입하지 않는다. CSS와 JS는 손으로 쓴 것을 그대로 서빙한다.
  (`scripts/`의 노션 이관 스크립트만 Node를 쓴다. 이건 일회용 도구고 런타임과 무관하다.)

## 화려한 UI 효과 구현 원칙

**화려한 효과를 넣을 때도 순수 HTML을 먼저 쓴다.** `section`·`nav`·`ul`과
`details`/`summary` 같은 의미 있는 요소와 브라우저 기본 동작만으로 구조와
인터랙션을 만든다. HTML만으로 표현이 부족할 때 CSS로 배치·색·깊이·애니메이션을
보탠다. **순수 JavaScript는 HTML과 CSS로 구현할 수 없는 상태 변화나 입력 반응에만
마지막 수단으로 쓴다.** CSS로 되는 효과를 JS로 다시 만들지 않는다.

- 스크립트가 꺼져도 링크와 콘텐츠 탐색이 가능해야 한다(점진적 향상).
- CSS 애니메이션에는 `prefers-reduced-motion` 정지 규칙을 함께 둔다.
- 효과 때문에 본문 가독성·키보드 포커스·모바일 폭이 깨지면 효과를 줄인다.
- 이 원칙은 `CLAUDE.md`가 정본이고 `AGENTS.md`가 이 파일의 심볼릭 링크이므로
  Claude Code와 Codex 양쪽에 똑같이 적용된다.

## 구조

```
cmd/blog/       서버. 시작할 때 마이그레이션을 적용하고 HTTP를 연다.
internal/web/   읽기 전용 공개 페이지 (라우팅, 템플릿)
internal/admin/ 글쓰기 화면 (CSR) + GitHub 로그인. **-admin을 줘야 뜬다**
internal/markdown/ 본문 마크다운 → HTML (goldmark + 수식 확장)
cmd/import/     노션 이관 CLI. 지금은 마크다운 파일로만 출력하고 DB에는 넣지 않는다.
internal/db/    연결(Open)과 마이그레이션 러너(Migrate)
internal/notion/ 덤프 파싱과 마크다운 변환기
internal/importer/ 변환 결과를 DB에 넣기 (posts/images upsert)
cmd/postparent/  글끼리의 부모-자식(parent_id) 복원. 카테고리 하나씩
cmd/importgh/    GitHub 저장소의 마크다운 노트를 DB로 (아래 "GitHub 이관")
internal/curation/ 사람이 정한 예외. 분류는 categorize·regroup이, 본문에서
                   덜어낼 줄(BodyEdits)은 import가 본다
migrations/     번호순 SQL. 001_init.sql 형식.
out/            (gitignore) 변환 결과 검토용
embed.go        루트 패키지. migrations/를 embed해서 MigrationsFS()로 노출.
deploy/         GCP 배포물. systemd 유닛과 인스턴스 준비 스크립트, 절차 문서
.github/workflows/deploy.yml  main push → 빌드 → 인스턴스에 바이너리 전송
scripts/        노션 이관용 일회용 Node 스크립트 (런타임과 무관)
  notion-workspace-audit.mjs   워크스페이스 구조 조사 → notion-audit-raw.json
  notion-block-dump.mjs        페이지 블록 원본 덤프 → dump/
  notion-page-status.mjs       덤프 분석해서 초기 status 결정 → notion-page-status.csv
  dump/                        (gitignore) 페이지 JSON 1408개 + images/ 446개
.env                           (gitignore) NOTION_TOKEN
```

`embed.go`가 루트에 있는 이유: `//go:embed`는 상위 디렉토리를 못 가리킨다
(`../migrations` 불가). `migrations/`를 루트에 두려면 embed 선언도 루트여야 한다.

```sh
go test ./...                                  # 전체 테스트
go run ./cmd/blog -db blog.db -addr :8080      # 서버
CGO_ENABLED=0 go build -o blog ./cmd/blog      # 단일 바이너리

go run ./cmd/import -pages <id>,<id> -v        # 특정 페이지만 변환 (리포트 전체 출력)
go run ./cmd/import -limit 20                  # 앞 20개만 변환
go run ./cmd/import                            # 전체 1408개, out/에만 (DB는 안 건드림)
go run ./cmd/import -db blog.db                # 변환 + DB 이관 (몇 번 돌려도 안전)
```

## DB 이관 현황

덤프 1408개 중 사람이 빼기로 정한 `DropPosts` 152개를 제외한 **1256개**가 들어가
있다(GitHub에서 온 16편을 더해 posts는 1272편이다). `-db`를 줄 때만 DB를 건드린다.

- **글은 `notion_page_id`, 이미지는 `sha256`이 멱등 키다.** 다시 돌리면 갱신만 되고
  행이 늘지 않는다. `created_at`은 처음 넣을 때 값을 유지하고 `updated_at`만 바뀐다.
- 전부 **한 트랜잭션**에서 처리한다. 중간에 실패하면 아무것도 안 들어간다.
  반쯤 들어간 DB를 손으로 정리하는 것보다 통째로 다시 도는 게 낫다.
- 지금은 `slug`가 페이지 ID 그대로다. `published_at`은 NULL.
- 이미지 446개 중 자기소개 증명사진 1개를 제외한 445개를 BLOB으로 들고 있다.
- `parent_id`는 5개 카테고리 172건에 채워져 있다(수리통계1 46, 수리통계2 43,
  탐색적 자료분석 22, 확률과정론 19, 빅데이터 분석기사 42). 아래 "글 계층" 참고.
- 아직 안 한 것은 맨 아래 "진행 상황" 절에 모아뒀다.

## 형제 순서 (sort_order)

`cmd/sortorder`가 채운다. **출처에 따라 신뢰도가 다르다.**

| 출처 | 건수 | 신뢰도 |
|---|---:|---|
| `child_page` 배열 위치 | 596 | 정확. 노션 API가 블록을 화면 순서대로 준다 |
| `created_time` 순위 | 708 | 차선. 쌍 기준 85.8%만 정해짐 |
| 근거 없음 → 0 | 104 | 전부 같은 분에 생성된 85건 + 최상위 19건 |

- **노션 API는 데이터베이스 행의 화면 순서를 노출하지 않는다.** 뷰의 수동 정렬은
  API에 없고, `sorts`로는 속성·타임스탬프 정렬만 지정할 수 있다. 실제로 3개 DB를
  쿼리해 확인했다 — 반환 순서가 화면 순서가 아니었다. **재수집해도 안 나온다.**
- **`created_time`은 분 단위다** (초가 항상 `00`). 한 번에 만든 페이지들이 같은 값을
  가진다. 그런 건 `DenseRank`가 **같은 순위**를 줘서 "순서 미정"으로 남긴다.
  억지로 다른 값을 주면 없는 순서를 지어내는 것이다.
- 검증해보니 **시각이 다르면 98.3%가 논리적 순서와 일치**한다(제목에 번호가 있는
  DB 18개, 347쌍 중 341쌍). 틀리는 게 아니라 자주 침묵하는 신호다.

**사람이 정한 순서는 예외다** (2026-09-01, `migrations/005`). 아래 규칙은
"이 sort_order가 `created_time`에서 나왔다"는 불신에서 온 것이지 "sort_order라서"가
아니다. `curation.PostMetadataEdits`로 사람이 적어둔 순서는 `posts.sort_order_manual`에
표시가 남고, 화면은 **그 글들을 맨 앞에 제 순서대로** 놓는다.

- **왜 컬럼인가.** 웹이 `internal/curation`을 직접 읽게 하면 같은 DB를 다른
  바이너리로 서빙할 때 순서가 달라진다 — DB가 정본이라는 전제가 거기서 깨진다.
  순서를 누가 정했나는 데이터의 성질이라 데이터가 들고 있어야 한다.
- **섞인 목록에서는 앞뒤로 가른다.** "전부 sort_order"로 돌아서면 나머지가 못
  믿을 `created_time` 순서로 되돌아가고, 무시하면 사람이 정한 것이 사라진다.
- 실제로 `다변량분석 : 코드` 12편이 이 때문에 어긋났다. 표에 0~11을 적고
  `sortorder -apply`까지 했는데 화면은 가나다순이었다 — 제목에 앞 번호가 없어서
  아래 자연 정렬이 다시 세웠기 때문이다.
- **`cmd/sortorder`의 수동 묶음 이름은 표에서 가져온다**(`PostMetadataEdit.Group`).
  예전에는 `"manual:linear-algebra"`가 박혀 있었는데, 수동 묶음이 둘이 되면서
  두 묶음의 `sort_order` 0이 한 묶음 안에서 겹치는 것으로 보였다.

**그 밖의 화면 순서는 `sort_order`가 아니라 제목 앞 번호를 먼저 따른다** (`web.sortPosts`).
`created_time` 출처 708건이 분 단위라 자주 어긋나기 때문이다 — 실제로
`10. 무중단 배포`가 `2. 테스트 코드`보다 앞에 오는 목록이 있었다. 제목 앞 번호는
사람이 직접 붙인 것이라 훨씬 믿을 만하다. **DB의 `sort_order`는 안 고친다**;
읽을 때만 다시 세운다.

- 구분자(`.` 또는 `)`)와 공백을 반드시 요구한다. 안 그러면 `2022년 탐자 1차 시험`,
  `1주차 정리`, `42 : Cpp 모듈`의 앞자리를 번호로 착각한다.
- 번호 없는 글은 **번호 있는 것들 사이에 끼우지 않고 뒤로 보낸다.** 어느 번호 옆에
  놓을지 알 방법이 없다.
- **그 뒤쪽은 제목 자연 정렬로 세운다** (`naturalCompare`, 2026-08-23). 제목을 숫자
  덩어리와 글자 덩어리로 갈라 앞에서부터 견주고, 숫자끼리는 값으로, 숫자와 글자가
  만나면 숫자가 앞이다. 예전에는 들어온 순서(`sort_order`)를 그대로 뒀는데 그 값이
  노션 작성 시각이라, **번호가 제목 끝이나 중간에 붙은 시리즈가 통째로 엇갈렸다** —
  `practice problem 1 / 2022년 탐자 1차 / practice problem 2 / 연습문제 …`처럼.
  제목에 이미 번호가 적혀 있는데 그걸 안 읽고 작성 시각을 따르는 것이라, 없는 순서를
  지어내지 않는다는 원칙과는 반대쪽 문제였다.
  - 글자로 견주면 `(10)`이 `(2)`보다 앞에 온다. 그래서 숫자는 값으로 읽는다 —
    `인자분석 : 코드 (1)~(4)`가 실제로 그랬다.
  - **앞 번호 규칙은 그대로다.** 자연 정렬은 앞 번호가 없는 뒤쪽에만 걸린다.
    `42 : Cpp 모듈`이 `1. 확률`과 `2. 확률분포` 사이로 끼어들지 않는다.
- 카테고리 87개와 글 1357편을 전부 훑어 **51페이지(카테고리 17)의 목록 순서가
  바뀌는 것**을 확인했다. 전부 시리즈 번호가 흩어져 있던 자리다.
- 목록에 **원본 작성일을 같이 찍는다**(`original_created_at`). 순서가 이상해 보일 때
  근거를 눈으로 확인하라고 둔 것이다.
- **사람이 직접 정한 제목·작성일·순서는 `curation.PostMetadataEdits`가 이긴다.**
  import가 이 표의 제목·`original_created_at`·`sort_order`를 함께 적용하고,
  sortorder도 `-only all`일 때 같은 표로 덮는다. 일반 글의 재이관은 이미 복원한
  `sort_order`를 보존한다. 현재 선형대수 11편이 이 규칙을 쓴다.

```sh
go run ./cmd/sortorder -db blog.db -only childpage          # child_page 출처만
go run ./cmd/sortorder -db blog.db -only created            # created_time 출처만
go run ./cmd/sortorder -db blog.db                          # 전체 미리보기
go run ./cmd/sortorder -db blog.db -only created -apply     # 반영
```

## 글 계층 (parent_id)

`cmd/postparent`가 채운다. 카테고리는 3단계가 끝이라 그보다 깊었던 노션 계층은
categorize를 거치며 평평해진다. `수학 & 통계 > 수리통계1 > 3. 일변량 분포 : 예시 >
베르누이 분포`의 글은 수리통계1 카테고리에 붙고, 사이의 `3. 일변량 분포 : 예시`는
카테고리가 아니라 **같은 카테고리의 다른 글**이 된다. `parent_id`가 그 잃어버린
층을 되살린다.

```sh
go run ./cmd/postparent -db blog.db -category 수리통계1          # 미리보기 (기본)
go run ./cmd/postparent -db blog.db -category 수리통계1 -apply   # 반영
```

- **한 번에 한 카테고리씩만 한다.** `-category`가 필수고 전체를 한 번에 도는 모드는
  없다. 먼저 수리통계1로만 시험했다(2026-08-16, 46건 전부 붙음). 나머지 카테고리로
  넓힐지는 아직 안 정했다.
- **출처는 sort_order와 같은 child_page 블록이다.** 덤프 스크립트가
  `child_page`/`child_database` 안으로는 재귀하지 않으므로(`notion-block-dump.mjs`),
  `{page}.json`에 나오는 child_page 블록은 **전부 그 페이지의 직속 자식**이다.
  단이나 토글 안에 깊이 박혀 있어도 부모는 덤프의 주인 페이지 하나로 정해진다.
  데이터베이스 행은 그 데이터베이스를 담은 페이지를 부모로 삼는다
  (`child_database` 블록의 id가 곧 데이터베이스 id다).
- **부모가 다른 카테고리에 있어도 그대로 가리킨다.** 수리통계1의 최상단 10건은
  부모가 `수리통계1` 글(id 597)인데 그 글은 상위 카테고리에 붙어 있다. 그래도
  **고치는 행은 언제나 `-category`로 준 카테고리의 글뿐이다.**
- 검증은 `original_path`로 한다. 자식 경로에서 마지막 한 칸(자기 제목)을 떼면
  부모 경로와 같아야 한다. 46/46이 맞았다. 어긋나면 미리보기가 짚어준다.
- **빅데이터 분석기사는 42건 전부 연결했다**(2026-08-22). 정확한 child_page는
  3건이고 나머지 39건은 경로 중간의 인라인 데이터베이스 한 칸을 건너뛰어 그
  데이터베이스를 담은 실제 글에 붙는다. 그래서 미리보기의 단순 경로 검사는
  3/42만 일치한다고 찍지만, 출처는 `데이터베이스 소유자`로 따로 명시되고 적용 뒤
  계획과 다른 값 0건·재실행 변경 0건을 확인했다. 명령의 `-category`는 이름이 아니라
  현재 slug이므로 `-category 빅데이터-분석기사`로 실행한다.
- 자식만 있고 본문이 없는 중간 노드도 **글로 남겨서 사슬에 유지한다.** 버리면
  원래 몇 단계였는지가 사라진다.
- `posts.parent_id`에는 `categories`와 달리 **깊이/순환을 막는 트리거가 없다.**
  postparent가 반영 전에 사슬이 도는지 보고, 웹 쪽 `nestPosts`와 `PostAncestors`도
  각자 방어한다.

## 카테고리

**3단계다.** 현재 DB는 사람이 정한 상위 분류 8개 > 그 아래 18개 > 62개다.

**중간층이 늘 노션 최상위인 것은 아니다** (2026-08-21). `데이터 & 수리`는 노션
최상위 하나(`수학 & 통계`)에 이론·응용·머신러닝이 다 들어 있어서 사람이 세 갈래로
갈랐다. 그 갈래는 노션에 없는 층이라 `source_name`이 NULL이고, `cmd/regroup`의
`subs`가 만든다. 노션 최상위였던 `수학 & 통계`와 `머신러닝 & 딥러닝`은 할 일이
없어져 `DropCategories`로 없앴다.

```
데이터 & 수리                  ← 사람 (groups)
├─ 수리/통계: 이론             ← 사람 (subs)
│   └─ 선형대수 …              ← 노션 2단계
├─ 수리/통계: 응용
└─ 머신러닝
```

**`개발`은 `members`와 `subs`를 함께 쓴다** (2026-08-23). Language·리눅스 & 쉘·
tooling은 노션 최상위가 그대로 한 갈래인데, `웹 프로그래밍` 77편만은 배운 순서
(Django → Spring)와 곁다리로 붙은 것들(HTML/CSS, React, 로그인, 웹소켓)이 한 갈래에
뒤엉켜 있었다. **3단계가 끝이라 `웹 프로그래밍 > 백엔드 > Django`로는 못 내려간다** —
그래서 웹 프로그래밍 자리를 두 갈래가 대신하고, 그 분류는 `DropCategories`로 없앴다.

```
개발                           ← 사람 (groups)
├─ Language …                  ← 노션 최상위 (members)
├─ 리눅스 & 쉘
├─ tooling
├─ 서버 & API                  ← 사람 (subs)
│   └─ Django · Spring · Node.js
└─ 클라이언트 & UI
    └─ Javascript · React · 모바일 프로그래밍
```

- **이름을 "프론트엔드"가 아니라 "클라이언트 & UI"로 뒀다.** 그 밑에 모바일
  프로그래밍(Swift/UIKit)이 들어간다. 웹만 가리키는 말이면 어긋난다.
- **`subs`의 `sort_order`는 `members` 다음부터 센다.** 둘 다 같은 분류의 자식이라
  0부터 다시 세면 사이드바에서 같은 자리를 다툰다. `데이터 & 수리`는 members가
  비어 있어서 이 문제가 안 보였다.
- **입구 글 다섯(Django·Spring·Node.js·Javascript·React)은 같은 이름의 하위 분류로
  내려보내 그 분류의 표지로 삼았다.** école 42에서 쓴 방법과 같다 — 안 그러면
  `서버 & API` 화면에 `Django` 글과 `Django` 분류가 나란히 서서 같은 곳으로 가는
  길이 두 벌이 된다. 나머지 일곱(로그인 5 + 웹소켓 1 + HTML & CSS 1)은 제 분류가
  없어서 두 갈래에 직접 붙였다.
- **`웹 프로그래밍` 표지 글은 뺐다**(`DropPosts`). 본문이 링크 목록과 내용 없는
  `## 실전 프로젝트` 제목뿐이라, 그 목차가 하던 일을 두 갈래가 그대로 대신한다.

**3단계 갈래를 사람이 직접 두는 자리가 생겼다** (2026-09-02, `cmd/regroup`의
`topics`). `subs`는 최상위(group) 밑에만 층을 놓는데, 백준 문제는 그보다 한 칸
아래가 필요했다 — `알고리즘 > 알고리즘: 실전 > 동적 계획법`.

- **categorize로는 못 만든다.** 그 도구는 `original_path`가 알려주는 것만 아는데,
  백준 문제의 경로는 `알고리즘: 실전 > 백준 단계별로 풀기: 1번 ~ 9번 > 1978번:
  소수 찾기`라 **푼 순서**만 적혀 있다. 무엇을 배웠나는 어디에도 없다. 사람이
  정하는 층이라 `groups`·`subs`와 같은 자리에 있는 것이 맞다.
- **부모의 깊이를 regroup이 먼저 본다.** 3단계가 끝이라 부모가 2단계여야 하는데,
  넘으면 002의 트리거가 막아준다 — 다만 그때 나오는 말은 "트리거가 거부했다"라
  무엇이 잘못됐는지 알려주지 않는다.
- **`sort_order`는 표에 적은 순서다.** 갈래 이름은 사람이 지은 것이라 가나다순도
  알파벳순도 뜻이 없다. 지금은 노션에서 온 형제가 남지 않아 0부터 세지만,
  남겨야 할 형제가 생기면 `subs`가 `members` 다음부터 세는 것처럼 고칠 자리다.

- **표지가 가리키는 분류는 `renames`가 이번 실행에서 만들 slug여도 된다**
  (2026-09-02). `Covers`가 이름이 바뀔 분류를 가리키면 그 slug는 아직 DB에
  없는데, 이름 변경은 계획 검사보다 **뒤에** 일어난다. 막으면 이름을 바꾸면서
  표지를 지정하는 실행이 통째로 멈춘다 — `Moves`의 `ToSlug`를 받아주는 것과
  같은 이유다. **categorize 쪽은 아예 미룬다**: 그 분류를 만들거나 이름을 바꾸는
  것은 regroup의 일이라, 새 `Covers`를 추가한 직후에는 categorize가 먼저 돌 수
  있다. 실패로 다루면 두 도구가 서로를 기다리는 교착이 된다.
- **표지 글은 `notHidden`을 거친다.** `store.coverSlug`가 숨긴 글을 걸러내므로
  **draft인 표지는 없는 것과 같다.** 그러면 `linkCoveredChildCategories`가 안
  걸려서 표지 본문의 링크가 분류 URL로 안 바뀌고, 같은 이름이 본문과
  `하위 분류`에 두 벌로 나온다. 네트워크의 입구 글 셋이 실제로 그랬다 —
  `Covers`를 적을 때 그 글이 보이는 글인지 함께 봐야 한다(`StatusEdits`).
- **`curation.Moves`의 `ToSlug`는 최상위가 아니어도 된다.** 사람이 둔 중간 층도
  부모가 될 수 있다. regroup이 `groups`와 `subs` 양쪽에서 찾는다.
- **새 층을 처음 얹을 때 두 도구가 서로를 기다리지 않게 한다** (2026-08-23).
  categorize가 글을 옮겨야 regroup이 빈 분류를 지울 수 있고, regroup이 층을 만들어야
  categorize가 거기 붙일 수 있다. 새 `subs`를 추가한 직후에는 이게 교착이 된다.
  - **categorize는 아직 없는 `PostMoves` 대상 slug를 기다린다.** 실패가 아니라
    "미룬 글 n건"으로 알리고 그대로 둔다.
  - **regroup은 `Moves`의 `ToSlug`가 DB에 없어도 된다.** `groups`/`subs`에 있으면
    이번 실행이 먼저 만든다(1a·1b가 3보다 앞이다).
  - **regroup은 남은 것이 전부 떠나기로 예정돼 있으면 삭제를 미룬다**
    (`allScheduledToLeave`). 글은 `PostMoves`가, 하위 분류는 `Moves`가 데려간다.
    **하나라도 표에 없으면 예전대로 에러다** — 갈 곳이 정해지지 않은 것을 조용히
    지워서 잃는 것이 그 검사가 막으려는 일이다.
  - 그래서 **categorize → regroup을 두 바퀴** 돌려야 수렴한다. 세 번째 바퀴는 0건이다.
- **`categories.sort_order`는 categorize가 덮지 않는다** (2026-09-02에 고쳤다).
  형제 순서는 사람이 정하는 것이라(regroup의 `members`·`subs`·`topics`) 경로가
  알려줄 수 있는 것이 아니다. 예전에는 두 도구가 같은 칸을 서로 다른 방식으로
  썼다 — categorize는 계획 전체를 훑는 **전역 색인**을, regroup은 **부모별
  색인**을 넣어서 **나중에 돈 쪽이 이겼다.** `categorize → regroup`과
  `regroup → categorize`가 실제로 다른 값을 남겼고, 이 문서가 금지하는 바로
  그 싸움이다. 이제 categorize는 **처음 만들 때만** 순서를 정한다 —
  `name`/`slug`/`parent_id`를 안 덮는 것과 같은 이유다.
- **`DropCategories`는 categorize도 봐야 한다.** 안 그러면 regroup이 지운 분류를
  categorize가 다음 실행에서 되살려서, 두 도구를 번갈아 돌릴 때마다 트리가 달라진다.
  실제로 그렇게 됐다가 고쳤다.
- **regroup은 이름을 바꾸기 전에 먼저 지운다.** 없앨 분류가 쓰던 slug를 다른
  분류가 물려받는 경우가 있어서(`핸즈온 머신러닝 2` → `머신러닝: 기초이론`),
  나중에 지우면 UNIQUE에 걸린다.
- **사람이 만든 최상위 분류를 없앨 때는 `groupDrops`를 쓴다.**
  `curation.DropCategories`는 멱등 키가 `source_name`이라 노션에서 온 분류만
  지운다 — `groups`가 만든 분류는 그 칸이 NULL이다. 배정표에서 줄만 지우면 그
  분류가 "최상위인데 배정표에 없다"로 계획 검사에 걸린 채 남는다.
  안전장치는 `DropCategories`와 같다: 글도 자식도 없을 때만 지운다.
- **`allScheduledToLeave`는 지워질 자식도 떠나는 것으로 센다.** 예전에는
  옮겨지는 것(`Moves`)만 셌는데, 그러면 자식이 전부 `DropCategories`에 있는
  부모가 "예정에 없는 것이 남았다"로 멈춘다. 커리어를 뺄 때 `취업 준비`가
  실제로 그랬다.
- **최상위 분류의 slug를 바꿀 때는 `groupRenames`를 쓴다.** `groups`는 slug가
  멱등 키라 배정표에서 slug만 바꾸면 새 행이 하나 더 생기고 옛 행이 자식을 안은
  채 남는다.

```
dev > Language > 프로그래밍 언어
└ 사람이 정함    └ 노션 최상위    └ 노션 2단계
  (regroup)        (categorize가 original_path에서 뽑음)
```

두 도구가 층을 나눠 맡는다. 순서는 상관없고 몇 번을 섞어 돌려도 결과가 같다.

| 도구 | 하는 일 | 멱등 키 |
|---|---|---|
| `cmd/categorize` | `original_path`에서 분류를 뽑아 글에 붙임 | `source_name` |
| `cmd/regroup` | 사람이 정한 층·이름·순서를 반영 | `slug` |

```sh
go run ./cmd/categorize -db blog.db -apply
go run ./cmd/regroup -db blog.db -apply
```

- **`source_name`이 카테고리의 안정적인 키다** (003에서 추가). `name`/`slug`는 사람이
  바꿀 수 있고 실제로 바꿨다(소프트스킬 → tooling). slug를 키로 쓰면 categorize가
  이름 바뀐 카테고리를 못 찾고 옛 이름으로 다시 만들면서 글까지 가져간다.
  실제로 그렇게 됐다가 003으로 고쳤다.
- `source_name`이 NULL인 카테고리는 사람이 만든 상위 분류다(dev, cs-theory 등).
  categorize는 그런 건 건드리지 않는다.
- **categorize는 이미 있는 카테고리의 `name`/`slug`/`parent_id`를 덮지 않는다.**
  사람이 바꾼 걸 존중한다. 처음 INSERT할 때만 경로가 알려준 부모를 쓰고, 그 뒤의
  이동은 사람 몫이다(regroup). 덤프가 고정이라 경로가 나중에 달라지지 않으므로
  잃는 것도 없다.
- **깊이 제한은 트리거가 지킨다** (002). 삽입과 이동 양방향을 보고, 이동할 때는
  딸려가는 서브트리 높이까지 계산한다. 001의 트리거는 "새 부모가 이미 자식인가"만
  봐서, 자식이 있는 노드를 옮기면 제한을 조용히 넘겼다.
- 경로에서 뽑을 때 **마지막 요소는 글 제목 자신이라 버린다** (1408건 전부 그렇다).
- **노션 최상위 페이지 19개는 자기 카테고리의 "표지 글"이다.** 경로가 자기 제목뿐이라
  조상이 없지만, 그 제목이 하위 글들의 경로에서 카테고리 이름으로 등장한다. 그 이름과
  맞으면 같은 이름의 카테고리에 붙이고 **`categories.cover_post_id`로도 이어둔다** (004).
  방향을 categories → posts로 잡은 이유: 카테고리당 표지는 하나다. posts 쪽에 깃발을
  두면 한 카테고리에 표지가 여럿 생기는 걸 스키마가 못 막는다.
- **`categories.cover_post_id`는 UNIQUE다.** 한 글은 한 카테고리의 표지만 될 수 있다.
  그래서 사람이 표지를 지정하는 건 "새로 만드는" 게 아니라 **"옮기는"** 것이다.
  regroup이 옛 자리를 먼저 비우고, categorize는 그 글이 이미 다른 카테고리의 표지면
  건드리지 않는다(덮으려 들면 UNIQUE에 걸려 categorize 전체가 실패한다).
- `category_id`가 NULL인 글은 현재 0건이다. 표지가 있는 카테고리는 45개다.
  노션 최상위에서 자동 판별한 15개와 사람이 지정한 31개가 있지만, 자기소개 글
  하나는 원래 카테고리의 표지에서 `/intro`로 옮긴 것이어서 합계에서는 한 번 빠진다.
- **표지 수를 셀 때 이름이 아니라 DB의 id로 센다.** 사람이 분류 이름을 바꾸면
  계획의 이름과 실제 slug가 갈라진다. 또 **한 글은 한 카테고리의 표지만 될 수
  있어서**(UNIQUE), 사람이 어떤 글을 다른 분류의 표지로 가져가면 경로가 그 글을
  표지로 두려던 카테고리는 표지를 잃는다(`최인렬 (Inryeol Choi)` → `/intro`).
- slug는 소문자 + 공백/`:`/`/`/`.`→하이픈, 나머지 기호는 버린다. **한글은 그대로 둔다.**
- 같은 2단계 이름이 서로 다른 부모 밑에 나오거나, 서로 다른 이름이 같은 slug가 되면
  **에러로 멈춘다.** 조용히 한쪽으로 몰면 트리가 조작된다.

### 사람이 정한 예외 (`internal/curation`)

categorize는 `original_path`가 알려주는 것만 안다. 노션에서 어디에 뒀느냐와
블로그에서 어디에 두고 싶으냐가 다를 때 그 차이를 `internal/curation`에 적는다.
**regroup이 이대로 DB를 고치고, categorize는 이걸 "경로와 다르다"고 트집잡지 않는다.**
두 도구가 같은 표를 봐야 해서 별도 패키지로 뺐다.

| 표 | 뜻 | 멱등 키 | 보는 쪽 | 현재 |
|---|---|---|---|---|
| `Moves` | 카테고리를 사람이 정한 다른 분류 밑으로 | `source_name` | categorize·regroup | 17건 (웹 5갈래 + 모바일 → 개발 두 갈래) |
| `Covers` | 사람이 정한 분류에 표지 글 붙이기 | `notion_page_id` | 〃 | 36건 (기존 14 + école 42 하위 분류 17 + 웹 입구 글 5) |
| `PostMoves` | 글 하나를 경로와 다른 카테고리에 붙임 | `notion_page_id` | 〃 | 171건 (기존 67 + 백준 104) |
| `PostMetadataEdits` | 글 제목·원작성일·순서를 사람이 지정 | `notion_page_id` | import·sortorder | 28건 (선형대수 11 + 다변량분석 12 + 최적화이론 실전 순서 5) |
| `PostTitleEdits` | 작성일·순서는 유지하고 글 제목만 지정 | `notion_page_id` | import | 수리통계2 참고자료 4건의 `(1)` 제거 |
| `StatusEdits` | **글의 status를 사람이 지정** | `notion_page_id` | **import** | 0건 (빅데이터 분석기사의 마디 둘은 커리어와 함께 뺐다) |
| `DropCategories` | 카테고리 없애기 | `source_name` | 〃 | 21건 (`컴퓨터 시스템` 계열, `웹 프로그래밍`, 커리어 4갈래, 옛 백준 묶음 4갈래 포함) |
| `DropPosts` | **글을 이관에서 뺌** | `notion_page_id` | **import** | 152건 (커리어 50 · 백준 35 · 알고리즘 이론 15 포함) |
| `DropImages` | **이미지를 DB에서 뺌** | `sha256` | **import** | 1건 (증명사진) |
| `BodyEdits` | **본문에서 줄 삭제·교체** | `notion_page_id` | **import** | 삭제 8줄 + 교체 5줄 |
| `BodyAppends` | **본문 끝에 지속적으로 덧붙임** | `notion_page_id` | **import** | 시험 해답 3글 7묶음 (확률과정론·수리통계1·수리통계2) |

- 키는 전부 사람이 바꾸지 않는 값이다. 이름이나 slug를 바꿔도 안 깨진다.
- 표에서 빼면 다음 categorize 실행이 "경로와 다르다"고 **실패한다.** 되돌리려면
  regroup으로 제자리에 돌려놓은 뒤에 뺀다.
- **`PostMoves`는 경로 처리보다 먼저 걸린다.** 옮긴 글의 경로는 카테고리를 만드는 데
  아예 쓰이지 않아서, 한 카테고리의 글이 전부 옮겨지면 그 카테고리가 계획에서 사라진다.
  `DropCategories`가 남은 빈 행을 지운다. 노션의 `프로젝트` 하위 분류를 이렇게 없앴다.
- **단, 옮긴 글의 1단계 조상 분류는 계획에 남긴다.** 안 그러면 그 분류의 존재 근거가
  옮긴 글들뿐일 때 분류가 통째로 사라지면서 **자기 표지 글까지 카테고리를 잃는다.**
  실제로 `최인렬 (Inryeol Choi)` 밑의 글이 이 6건뿐이라 그렇게 됐다가 고쳤다.
- **`DropCategories`는 글도 자식도 없을 때만 지운다.** 남아 있으면 에러로 멈춘다.
  그래서 순서가 있다: `categorize`로 글을 먼저 옮기고 그다음 `regroup`으로 지운다.
- 네 도구(categorize · regroup · sortorder · postparent)를 어떤 순서로 몇 번
  돌려도 결과가 같아야 한다. 바꾼 뒤에는 그걸 확인할 것.

**`DropPosts`와 `DropImages`는 이 프로젝트의 원칙에 대한 예외다.** 원래 글은
지우지 않고 status로 가린다. 그래서 표에 이유를 함께 적는다. **DB에서 행만 지우면
안 된다** — 덤프에 있는 한 다음 `import -db`가 다시 넣는다. 여기 적어두면 변환·이미지
이관에서 건너뛰고, `runImport`가 기존 DB 행도 같은 트랜잭션에서 지우므로 몇 번을
다시 이관해도 안 돌아온다. 현재 글 102건, 이미지 1건이다.

- **잎부터 지운다.** `posts.parent_id`가 `ON DELETE RESTRICT`라 부모를 먼저 만나면
  FK가 막는데, 자식까지 전부 표에 있으면 그건 고아가 생기는 것이 아니라 **순서
  문제**다. 커리어 42편을 통째로 뺄 때 실제로 걸렸다. `runImport`가 한 바퀴에
  지울 수 있는 것만 지우고 더 못 지울 때까지 돈다. **그러고도 남으면 에러다** —
  표에 없는 자식이 매달려 있다는 뜻이고, 그건 그 FK가 막으려는 바로 그 일이다.

**`BodyEdits`만 보는 쪽이 다르다** — 분류가 아니라 **본문**을 고치기 때문에
`cmd/import`가 변환 직후에 적용한다.

- **DB의 body를 손으로 고치지 않는다.** 고쳐두면 `import -db`를 다시 돌릴 때
  변환 결과가 덮어써서 되살아난다(relink 재작성이 날아가는 것과 같은 성질).
  여기 적어두면 몇 번을 다시 이관해도 같은 결과가 나온다.
- **`out/`에 쓰기 전에 적용한다.** 검토용 파일과 DB에 들어가는 것이 같아야 한다.
- **줄 단위로만 지우거나 바꾼다.** 문장 중간을 고치면 앞뒤가 어떻게 이어지는지
  표만 보고는 알 수 없다. 지운 자리에 빈 줄이 겹치면 하나로 줄인다.
- **못 찾으면 그 페이지의 변환이 실패한다.** 덤프가 고정이라 변환 결과도 고정이다.
  안 맞으면 표가 낡았거나 변환기가 바뀐 것이지 "그냥 없는" 경우가 아니다.
  조용히 넘어가면 지웠다고 믿는 것이 본문에 그대로 남는다.
- 전체를 돌린 실행에서는 **표에 있는데 한 번도 안 걸린 항목**도 리포트가 짚는다.
- 현재 8줄: 선형대수의 죽은 링크 2개, 소개 글의 증명사진과 `[프로젝트]` 링크,
  핸즈온 머신러닝 2의 `연습문제 2` 제목과 유일한 글 링크, 최적화이론 표지의
  `[수업 : 통수 & 선계]` 묶음 링크, R 표지의 `[R언어 : 시각화]` 묶음 링크다.
- **행을 전부 빼면 그 묶음 링크도 함께 빼야 한다.** 인라인 데이터베이스는 posts에
  행이 없어서, 펼칠 글이 하나도 안 남으면 렌더링이 짝을 못 찾고 링크를 그대로 둔다 —
  눌러도 404다. 선형대수와 R이 그래서 링크째 없앤 자리다.
- 별도로 수리통계2 표지의 참고자료 링크 4줄은 링크 대상은 유지하고 표시 제목 끝의
  불필요한 `(1)`만 교체한다. 대상 글 제목도 `PostTitleEdits`로 함께 맞춘다.
- 핸즈온 머신러닝 2 표지의 `모르면 여기, 저기 클릭!`은 `여기, 저기`가 존재하지 않는
  페이지라 404였다. 깨진 링크와 문구를 없애고 정상 링크 하나인 `모르면 클릭!`으로
  교체한다.
- **원본에 없던 긴 보충 본문은 `BodyAppends`로 덧붙인다.** marker가 이미 있고 내용이
  같으면 그대로 두어 멱등이고, marker만 같은 다른 내용이 있으면 덮어쓰지 않고
  실패한다. 해답은 각각 `<details>` 안에 있어 제목을 클릭해야 보이며 문제 이미지와
  문제 글은 항상 보인다. 현재 세 글에 붙는다:
  - 확률과정론 `중간 & 기말` (중간·기말)
  - 수리통계1 `연습문제와 시험` (연습문제 1·2, 중간·기말) — 문제가 전부 시험지
    **사진**이라 읽어서 옮긴 뒤 풀었다. 연습문제 1의 1번은 인쇄된 분포함수가
    그대로는 **감소해서** 분포함수가 될 수 없어(계수 0.4/0.8이 뒤바뀐 것으로 보인다)
    그 해석을 풀이 안에 적어뒀다.
  - 수리통계2 `수리통계2 - 시험` (중간·기말) — 신뢰구간 문항의 힌트값
    $t_{0.025}(5)=2.571$이 자유도 5의 값이라 $n-1=4$와 어긋난다. 힌트를 그대로
    쓰되 그 사실을 본문에 적었다.
- **`BodyEdits`도 글마다 파일을 나누고 `body_edits.go`가 합친다** (2026-09-02).
  빈 절을 통째로 채우는 손질이 생기면서 한 글의 표가 수백 줄이 됐다. 나눠 두면
  다른 줄 하나를 고칠 때 그 덩어리를 지나가지 않아도 된다.
  - **`Replace`에 여러 줄을 넣을 수 있다.** `replaceLine`이 줄 하나를 갈아끼운
    뒤 다시 이어붙이므로 제목 한 줄을 제목 + 본문으로 바꾸는 것이 그대로 된다.
    `BodyAppends`는 글 끝에만 붙일 수 있어서 **본문 가운데를 채울 때** 이 길을 쓴다.
  - **같은 줄이 두 번 나오면 앞엣것을 먼저 다른 글자로 바꾼다.** `replaceLine`은
    첫 번째 것만 건드리므로, 뒤엣것을 고치려면 앞엣것이 더 이상 안 걸리게 해야
    한다(다항분포의 `Y_{1} \\`가 좌변 벡터와 모수 벡터에 각각 있었다).
    그래서 **표의 차례가 곧 의미다** — 순서를 바꾸면 깨진다.
  - `concatBodyEdits`는 `concatBodyAppends`와 달리 **중복을 막지 않는다.**
    같은 줄을 일부러 두 번 고치는 것이 정상적인 쓰임이기 때문이다.
- **해답 표는 글마다 파일을 나누고 `body_appends.go`가 합친다.** 한 글의 해답이
  수백 줄이라 한 파일에 다 넣으면 다른 표를 고칠 때마다 그 덩어리를 지나가야 한다.
  `concatBodyAppends`는 **같은 (글, marker) 짝이 두 번 나오면 멈춘다** — 그대로 두면
  `appendBody`가 "marker는 있는데 내용이 다르다"로 잡는데, 그 말로는 표가 겹쳤다는
  것을 알 수 없다.

## GitHub 이관 (2026-09-01)

**노션 말고 GitHub 저장소에서도 글을 가져온다.** 지금은 `Java_Modern`의
`src/main/theory` 16편(모던 자바 인 액션 정리)뿐이다.

```sh
GITHUB_TOKEN=$(gh auth token) go run ./cmd/importgh -db blog.db          # 미리보기
GITHUB_TOKEN=$(gh auth token) go run ./cmd/importgh -db blog.db -apply   # 반영
```

### 노션 이관과 무엇이 다른가

**노션은 덤프가 고정이라 "몇 번을 돌려도 같은 결과"가 목표였다.** GitHub은
저장소가 계속 바뀌므로 목표가 하나 다르다 — **"다시 돌리면 지금의 저장소와
같아진다"**이다. 그래서 멱등 키가 다르다.

| | 노션 | GitHub |
|---|---|---|
| 멱등 키 | `notion_page_id` | `source_ref` (`github:{repo}@{path}`) |
| 원본 | `scripts/dump/` (고정) | 저장소 (계속 바뀜) |
| 작성일 | 노션 `created_time` | **첫 커밋 시각** |

- **`source_ref`는 `migrations/006`이 만든 칸이다.** slug를 키로 쓰면 안 된다 —
  사람이 admin에서 바꿀 수 있는 값이고, 바꾸는 순간 다음 이관이 같은 글을
  못 찾아 하나 더 만든다. `notion_page_id`를 따로 둔 이유가 정확히 이것이다.
- **작성일은 git이 이미 알고 있다.** 목록에 찍히는 날짜는 "실제로 쓴 날"이어야
  하고(노션도 `original_created_at`을 쓰는 이유다), GitHub에서는 그 값이
  **파일을 처음 커밋한 때**다. GitHub이 커밋을 새것부터 주므로 목록의 **마지막**
  원소가 그것이다. `per_page=100`이라 100번 넘게 고친 파일은 늦어질 수 있는데,
  그때는 리포트에 커밋 수가 100으로 찍혀 눈에 띈다 — 조용히 틀리지 않는다.
- **slug는 갱신 때 안 덮는다.** 사람이 바꿨을 수 있고, 이관이 되돌리면 그
  자리가 사람 것이 아니게 된다. 제목·본문·작성일은 저장소가 정본이라 덮는다.

### 어디에 붙나

`internal/curation/github.go`의 표가 정한다. **표에 없는 파일은 안 가져온다** —
저장소에 파일이 늘어도 조용히 딸려 들어오지 않는다.

- **분류를 만들지 않는다.** 트리를 손대는 곳은 categorize·regroup 둘뿐이어야
  해서, 표에 적은 분류가 DB에 없으면 실패한다.
- **`original_path`에 `Language > 프로그래밍 언어 > Java > 모던 자바`를 적는다.**
  노션 경로 자리를 쓰는 이유는 웹이 그 값의 **세 번째 칸으로 언어 갈래를
  나누기** 때문이다(`web.PathBranches`). 그래야 기존 Java 글과 한 갈래에 선다.
- **제목을 표에 사람이 적는다.** 파일 안의 첫 제목은 `3. 람다식 써보기!`처럼
  파일 사정이 그대로 나오고, 파일 이름은 `chapter3`이다. 둘 다 읽는 사람을
  위한 이름이 아니다. 순서(`sort_order`)도 여기서 정하므로
  `sort_order_manual`이 켜진다(migrations/005).

### 본문에 손대는 세 가지 (`prepareBody`)

**한 쌍이라 한 함수로 묶었다.** 하나만 빠져도 화면이 어긋나고, 예전에는
손질을 따로 테스트해서 호출부에서 하나를 빼도 아무 테스트도 안 깨졌다.

1. **`<javascript>`를 글자로 바꾼다.** 글쓴이는 꺾쇠를 절 제목 표시로 썼는데
   (`<특징>`, `<예시>`), 라틴 낱말은 CommonMark의 태그 이름 규칙에 맞아
   **raw HTML로 통과해 화면에서 사라진다.** 브라우저가 모르는 요소라 아무것도
   안 그린다. 한글 표시는 규칙에 안 맞아 우연히 안전하므로 **건드리지 않는다.**
2. **파일 첫머리의 `# 제목`을 뗀다.** 페이지의 `<h1>`은 템플릿이 그리는 글
   제목이라 본문에도 있으면 같은 말이 두 번 나온다.
3. **남은 제목을 한 단계 올린다.** 떼기만 하면 절이 `##`로 남아 화면에서 h3이
   되고, h1 바로 아래 층이 빈다. 목차 들여쓰기(`toc-h2`/`toc-h3`)도 밀린다.

셋 다 **코드 블록 안은 건드리지 않는다.** 셸 주석(`# 주석`)이나 마크다운을
보여주는 예제가 제목으로 둔갑한다.

### 이미지 (2026-09-02)

**저장소의 그림을 받아 DB에 넣는다** (`cmd/importgh/images.go`). 예전에는
"지금 대상에 이미지가 없다. 생기면 그때 노션 쪽 처리를 그대로 가져온다"고
적어뒀는데, `Database-System-Concepts`가 그 "생기면"이었다.

- **멱등 키는 `images.sha256`이다.** 노션 이미지와 같은 표, 같은 서빙 경로
  (`GET /img/{sha256}`)를 쓴다. 같은 그림이 두 저장소에 있어도 행이 하나고,
  파일 이름을 바꿔도 새 그림이 되지 않는다.
- **왜 링크로 안 두나.** `raw.githubusercontent.com`으로 바꿔 두면 글을 열
  때마다 독자 IP가 GitHub에 가고, 저장소를 지우거나 비공개로 돌리면 그림이
  통째로 사라진다 — 외부 파비콘을 안 불러오기로 한 것과 같은 판단이다.
- **경로는 글 파일 기준 상대경로다**(`./images/x.png`). 그 파일의 디렉터리로
  풀어야 저장소 경로가 나온다.
- **`http(s)://`로 시작하는 것은 안 건드린다.** 저장소 파일이 아니라 남의
  그림이다.
- **SVG는 받지 않는다.** `/img/`가 `images.mime`을 그대로 Content-Type에 실어
  보내는데 `image/svg+xml`은 브라우저가 문서로 열고 그 안의 `<script>`가
  **우리 도메인에서** 실행된다. admin 업로드가 SVG를 막는 것과 같은 이유다.
- **못 받은 그림은 원문 그대로 둔다.** 주소만 `/img/`로 바꿔 두면 깨진 그림이
  멀쩡해 보인다 — 죽은 링크를 노션 형태로 남기는 relink의 판단과 같다.
  리포트가 어느 경로를 못 받았는지 이름으로 짚는다.
- Contents API는 1MB가 넘는 파일에 내용을 안 실어 주므로 그때는
  `download_url`을 한 번 더 따라간다.

### 짝이 안 되는 굵게 (2026-09-02)

**손으로 쓴 마크다운에도 노션과 같은 자리가 있다.** CommonMark는 닫는 기호
바로 앞이 문장부호면 기호 뒤가 공백이거나 문장부호여야 짝으로 인정하는데,
한국어에서 이게 자주 걸린다 — `**경로 표현(Path Expression)**으로`는 닫는
기호 앞이 `)`이고 뒤가 `으`라서 **별표가 그대로 화면에 나온다.**

- 판정 규칙의 정본을 `internal/markdown`으로 옮겼다(`CanPairEmphasis`).
  이건 노션의 사정도 GitHub의 사정도 아니라 **CommonMark의 규칙**이고,
  두 이관 도구가 각자 들고 있으면 언젠가 갈라진다.
- **짝이 안 되는 자리만** `<strong>`으로 낸다. 본문은 사람이 웹에서 고칠
  것이라 되도록 마크다운으로 두는 편이 낫다.
- **코드 펜스와 인라인 코드 안은 건드리지 않는다.** 거기서는 `n**2`가 글자로
  나오는 것이 맞다.

### 겪은 것

- **`cmd/categorize`가 깨졌다.** `SELECT notion_page_id …`를 문자열로 스캔하는데
  GitHub 글은 그 칸이 NULL이다. `WHERE notion_page_id IS NOT NULL`을 붙여 고쳤다 —
  그 도구가 하는 일은 노션 경로에서 분류를 뽑는 것이라 다른 출처에는 할 말이
  없다. **출처가 하나 늘 때 이런 자리가 또 있는지 보라.**
- 이미지·내부 링크가 없어서 `images` 이관도 relink도 필요 없었다. 생기면
  노션 쪽 처리를 그대로 가져온다.

## 내부 링크

**재작성 결과는 `](/p/{slug})`다.** 서버 라우트가 `GET /p/{slug}` 하나뿐이라
접두사를 반드시 붙여야 한다. 안 붙이면 `GET /{l1}` 카테고리 라우트에 잡혀서
전부 404다. 실제로 옛 relink가 `](/{slug})`로 써서 본문 링크 670개가 전부
404였다(2026-08-18에 고쳤다).

`cmd/relink`가 세 형태를 모두 `](/p/{slug})`로 바꾼다.

| 형태 | 어디서 오나 | 개수 |
|---|---|---|
| `](/p/{페이지ID})` | child_page, child_database, link_to_page 블록 | 729 |
| `](/{32자리 16진수})` | 본문 인라인 링크(rich_text의 href) | 87 |
| `](/{slug})` | 옛 relink가 접두사 없이 써둔 것 | 670 |

두 번째 형태는 `/p/` 접두사가 없고 **하이픈도 없다**. `#조각`이 붙어 있으면
페이지 안의 특정 블록을 가리키며, 재작성할 때 조각은 그대로 둔다.
`importer.SlugIndex`가 두 형태의 키를 모두 만들어준다.

```sh
go run ./cmd/relink -db blog.db          # 미리보기 (기본)
go run ./cmd/relink -db blog.db -apply   # 실제로 고침
```

- 대응하는 글이 없는 링크는 **바꾸지 않고 노션 형태로 남긴다.** 경로만 slug 모양으로
  바꾸면 깨진 링크가 멀쩡해 보인다. 현재 146개(`/p/` 119 + 인라인 27)가 여기 해당한다.
  **이건 눌러도 404가 맞다.** 대부분 인라인 데이터베이스(`object: database`)라
  글이 될 수 없는 것이다. 예: `가상화기술`의 `Docker : 기초 이론`.
- **`/p/`가 노션 형태와 최종 형태를 겸한다.** 그래서 "접두사가 `/p/`면 노션"이 아니라
  **"경로가 실제 `posts.slug`가 아니면 노션"**으로 판별한다. relink는 글 전체의 slug
  집합을 들고 다니며 이걸 가린다. 이게 없으면 이미 바꾼 링크를 또 바꿨다고 세서
  멱등성 검사가 거짓으로 실패한다. slug가 페이지 ID와 같은 지금은 특히 그렇다.
- **`import -db`를 다시 돌리면 본문을 덮어써서 이 재작성이 사라진다.** import가
  경고를 찍는다. 그 뒤에는 relink를 다시 돌려야 한다.
- **slug를 바꾸면 링크도 같이 바꿔야 한다.** 지금은 slug가 페이지 ID라서 relink가
  노션 ID → slug 매핑으로 해결하지만, slug를 제목 기반으로 바꾸는 시점에는
  옛 slug → 새 slug 재작성을 같이 해야 한다.
- `-apply`가 끝나면 검증이 세 가지를 확인한다: 설명되지 않는 노션 형태 링크 0개,
  **접두사 없이 글을 가리키는 링크 0개**, 다시 돌려도 안 바뀜.

## 노션 이관

- 변환기는 미지원 블록을 조용히 버리지 않는다. 렌더러가 없는 타입은 **warn**,
  의도적으로 다르게 옮기는 것(단 레이아웃 평탄화, 목차 제거 등)은 **note**로 남긴다.
  새 블록 타입을 지원할 때 이 원칙을 깨지 마라.
- 검증은 결과 문자열을 다시 세어서 한다(렌더링 중에 센 값을 믿지 않는다).
  조립 단계에서 내용이 사라져도 잡히게 하려는 것이다.
- **노션은 목록 번호를 저장하지 않는다.** 렌더링할 때 매긴다. 그래서 중간에 다른
  블록이 끼면 이어지는 목록인지 새 목록인지 데이터만으로는 알 수 없고, **무엇이
  끼었는지**로 판단한다 (`softInterruptTypes`):
  - 코드/그림/수식/표 등과 빈 문단이 끼면 → **번호를 이어간다.** 항목에 딸린
    예시나 그림일 뿐이다. (노션 원본에서 이렇게 보이는 것을 확인했다)
  - 제목이나 내용이 있는 문단이 끼면 → **1부터 다시.** 문단은 대개 "알고리즘은
    다음과 같이 작동한다" 같은 새 목록의 도입문이고, 제목은 아예 새 절이다.
  - 덤프 전체 506건의 끊김 중 94건이 이어가는 쪽, 412건이 재시작하는 쪽이다.
- **목차(`table_of_contents`)는 버린다.** 렌더링할 때 제목에서 다시 만든다.
- **토글(`<details>`)에 관해 두 가지를 변환기가 처리한다** (2026-08-21에 고쳤다):
  - **펼쳐도 아무것도 안 나오는 토글은 내보내지 않는다** (35개 → 0). 화면에 보이는
    모습은 하나지만 원인은 셋이라 리포트에는 종류를 나눠 남긴다:
    목차만 들어서 25개(`dropped-toc-toggle`), 노션에서 원래부터 비어 있어서 8개
    (`dropped-empty-toggle`), 자식은 있었는데 그 내용이 비어서 2개.
  - **마지막 2개는 껍데기만 벗기고 summary를 문단으로 남긴다**
    (`toggle-to-paragraph`). 자식조차 없던 토글과 자식을 넣어둔 토글은 구조가
    다르고, 그 차이가 곧 "라벨이냐 문장이냐"와 겹친다. 자식이 없던 8개의 summary는
    `p.123`·`코드`·`3차 적률` 같은 라벨이지만, 자식을 넣어둔 2개는 문장이었다
    (`없으면 스프링부트 플러그인에 들어가 깔아야 한다.`,
    `이를 시행한 뒤 train_dataset[0]을 시행하면 다음과 같음`). 사람이 거기 뭔가를
    쓰려고 자리를 잡아둔 것이라 글자를 버리지 않는다. 문단이 되므로 summary와 달리
    **마크다운으로** 낸다 — 거긴 goldmark의 인라인 파싱이 닿는 자리다.
  - 토글을 뺄 때는 summary 글자를 리포트 메시지에 같이 찍는다. 무엇이 사라졌는지
    눈으로 볼 수 있어야 한다.
  - **`<summary>` 안은 마크다운이 아니라 HTML로 낸다** (`renderRichTextHTML`).
    CommonMark는 raw HTML 블록 안을 인라인 파싱하지 않아서 `**목차**`의 별표가
    글자로 보였다(75개). 굵게는 `<strong>`, 코드는 `<code>`로 내고 글자는
    HTML 이스케이프한다 — 결과가 raw HTML 자리에 그대로 들어가기 때문이다.
  - **내용이 빈 코드 블록이 든 토글은 남는다.** 그건 노션 원본의 코드 블록이 비어
    있는 것이라 `<pre>`가 비어 보이는 게 맞다. 현재 1건(`10. 무중단 배포`).
  - 이 변경에는 **1408건 재변환 + `relink -apply` 재실행**이 따라왔다.
- **문단의 자식은 들여쓰지 않는다.** 마크다운에는 "문단의 자식"이라는 구조가 없어서
  들여써도 뜻이 없고, 대신 위험하다: **CommonMark에서 4칸 들여쓰기는 코드 블록이다.**
  문단 안에 문단이 겹치면 2칸씩 쌓여 4칸을 넘고, 그 아래 목록이 통째로 회색 상자가
  되면서 안의 `$수식$`이 코드 안이라 글자로 굳는다. `1. 확률`이 그랬다.
  - 제목(`heading_*`)은 원래부터 안 들여썼다. 목록 항목만 표시 너비만큼 들여쓴다 —
    거기서는 들여쓰기가 실제로 중첩을 뜻한다.
  - **goldmark에서 들여쓰기 코드 블록 파서를 빼는 길은 안 된다.** goldmark는 4칸
    이상 들여쓴 줄을 그 파서에만 넘기므로, 빼면 그 줄이 어느 파서에도 안 걸려
    **내용이 통째로 사라진다.** 실제로 해보고 되돌렸다.
  - 고치기 전 덤프 전체에 이렇게 생긴 블록이 5개였고 **다섯 다 코드가 아니었다**
    (LaTeX 2, 산문 1, 목록 2). 고친 뒤 0개다. 본문이 바뀐 글은 27편.
- **렌더링된 글 전체에서 "눈에 보이는 고장"을 종류별로 센다** (2026-08-21).
  표본 하나를 손보는 대신, 징후별로 전수를 세고 그 원인을 고치는 방식이다.
  1390편을 훑어 나온 것과 처리:

  | 징후 | 전 | 후 | 판정 |
  |---|---:|---:|---|
  | 빈 표 칸 | 303 | 76 | 227개는 **머리 없는 표에 넣은 빈 머리**였다. 나머지 76은 원본에 실제로 빈 칸 |
  | 빈 목록 항목 | 53 | 0 | 노션에서 만들다 만 빈 줄. 자식이 있는 것은 없어서 그냥 뺐다 |
  | 노션으로 나가는 링크 | 3 | 0 | relink가 `https://app.notion.com/p/...` 형태를 아예 안 잡고 있었다 |
  | 굵게 기호 노출 | 46 | 46 | **원문에 사람이 직접 친 것.** `int(n**(0.5))`가 섞여 있어 손대지 않는다 |
  | alt 없는 이미지 | 469 | 469 | 변환기는 이미 캡션을 alt로 쓴다. **원본에 캡션이 500개 중 7개뿐**이라 쓸 글자가 없다 |
  | 취소선 기호 노출 | 4 | 4 | 대부분 LaTeX의 `~~`(공백)이고, 나머지는 원문 그대로다 |
  | 만료된 노션 첨부 링크 | 8 | 8 | "남은 일" 참고. 덤프에 파일이 없다 |

  - **`~~`를 세려면 수식과 코드를 먼저 빼야 한다.** LaTeX에서 `~`는 공백이라
    수식 안에 `~~`가 흔하다. 안 빼면 취소선 오탐이 백 건 넘게 나온다.
- **변환에서 글자가 새는지 전수로 본다** (2026-08-21에 했다). 블록 타입별 렌더러를
  믿지 않고 **결과 문자열을 다시 세는** 방식이다 — 변환기 자신의 검증과 같은 원칙이라
  조립 단계에서 사라져도 잡힌다. 덤프 1408개의 모든 `rich_text`/`caption`/표 칸/제목을
  모아 변환 결과에 남았는지 확인했다.
  - **글자 조각 40,459개 중 못 찾은 것 32개, 전부 의도한 것이다.** 일부러 뺀 토글
    summary 31개와 `curation.BodyEdits`로 지운 `프로젝트` 링크 1개. **조용히 새는
    곳은 없다.**
  - 비교할 때 **결과 쪽의 줄머리 `>`(인용)와 들여쓰기는 떼고 본다.** 원문 한 덩어리가
    여러 줄로 나가면 그 표시가 사이에 끼어서, 안 떼면 멀쩡한 글자를 "사라졌다"고 센다.
    처음에 99개가 나왔는데 그중 67개가 이 오탐이었다.
  - **목록 표시(`-`, `1.`)까지 떼면 안 된다.** 원문에 있던 "1. "까지 지워져서 이번엔
    반대 방향으로 못 맞춘다. 실제로 그렇게 17개를 잘못 셌다.
- `embed`/`bookmark`/`link_preview`는 링크로 바꾼다(각 1/24/12건). 링크가 죽어
  보이는 건 대개 원본이 이미 죽은 것이다. apption.co 임베드 1건이 그렇다.
- 이미지 블록 500개 중 **24개는 원본 URL이 비어 있다**(노션 쪽에서 이미 깨진 것).
  주석만 남긴다. 별도로 10개는 다운로드가 실패해 외부 URL을 그대로 쓴다.
- **수식은 마크다운의 빈 줄과 충돌한다.** 노션 수식에는 개행과 빈 줄이 들어 있는데,
  마크다운에서 빈 줄은 문단을 끊는다. 그대로 두면 여는 `$`와 닫는 `$`가 다른 문단으로
  갈라져서 수식이 렌더링되지 않고 `$`가 글자로 보인다. 그래서:
  - 인라인 수식은 공백을 정리해 **한 줄로 편다** (`normalizeInlineMath`)
  - 블록 수식은 여러 줄 배치를 살리되 **빈 줄만 지운다** (`normalizeBlockMath`)
  - LaTeX는 수식 모드에서 공백 길이를 구분하지 않고 진짜 줄바꿈은 `\\`로 쓰므로
    의미는 그대로다. 이스케이프는 절대 넣지 않는다 — `\`, `{`, `}`, `_`, `|`에
    백슬래시를 덧붙이면 LaTeX가 깨진다.
- **서식 기호는 CommonMark가 짝을 지을 수 있게 낸다.** 노션의 굵게/기울임을
  `**...**`로 옮길 때 두 가지가 걸린다. 덤프 전체에서 이 때문에 별표가 글자로
  보이던 자리가 **186개(글 38편)**였고 지금은 0개다.
  - **기호가 공백을 감싸면 안 된다.** 노션은 굵게 범위에 뒤따르는 공백까지 넣는
    일이 잦은데(`"시그마 대수, "`), 그대로 쓰면 닫는 기호 앞이 공백이라 짝이 안
    된다. 공백을 기호 밖으로 뺀다(237조각). **줄바꿈 없는 공백(U+00A0)까지 봐야
    한다** — 노션 본문에 흔하고 CommonMark도 공백으로 친다.
  - **알맹이가 문장부호로 끝나고 뒤에 조사가 붙으면 짝이 안 된다.**
    `**릴레이션(relation)**이라고`가 그렇다. 닫는 기호 앞이 `)`이고 뒤가 한글이면
    CommonMark는 닫는 기호로 보지 않는다. 한국어에서 특히 자주 걸린다.
    **그런 자리만 `<strong>`/`<em>`/`<del>` HTML로 낸다** — 본문은 사람이 웹에서
    고칠 것이라 되도록 마크다운으로 두는 편이 낫다.
  - **원문에 사람이 직접 친 `**`는 건드리지 않는다.** 33조각(글 20편)이 그렇다.
    붙여넣기로 들어온 마크다운이라 굵게로 바꾸고 싶어 보이지만, 그중에는
    `int(n**(0.5))`(파이썬 거듭제곱)처럼 바꾸면 망가지는 것이 섞여 있다.
    현재 화면에 남은 별표 46개(글 14편)가 전부 이것이다.
- **수식 안의 한글은 `\text{}`로 감싼다** (`internal/notion/mathtext.go`). KaTeX는
  수식 모드의 글자를 수학 글꼴로 그리는데, 라틴 문자는 이탤릭 변수로 보이면 그만이지만
  **한글은 자모가 겹쳐 뭉개진다.** `"조건부확률" = \dfrac{"결합확률"}{"주변확률"}`이
  읽을 수 없게 나왔다. 대상은 24개(글 11편)뿐이고 전부 설명하려고 한글을 그냥 쓴
  자리다 — 첨자(`X_{선수}`), 행렬 칸, 분수 같은 것들이다.
  - **한글 덩어리 사이의 공백은 함께 감싼다.** 수식 모드에서 공백은 무시되므로
    `행의 갯수`를 따로 감싸면 `행의갯수`가 된다.
  - **이미 `\text{...}` 안에 있는 한글은 건드리지 않는다.** 두 번 감싸면 중괄호만
    늘고, 원문이 옳게 쓴 자리를 다시 쓰는 셈이다.
- 본문에 **진짜 `$` 문자**를 쓴 페이지가 90개 있다(R의 `data1$col`, Makefile 변수,
  정규식 등). 그중 20개는 수식도 같이 있어서, 렌더러가 본문 `$`와 수식 `$`의 짝을
  잘못 지을 수 있다. 렌더러를 고를 때 확인할 것.
- `file`/`video` 블록의 노션 호스팅 URL은 서명이 붙은 임시 주소라 만료된다.
  덤프 스크립트는 이미지만 받아뒀다. 첨부파일이 필요하면 따로 받아와야 한다.

## 배포 (GCP)

**자세한 절차와 명령은 `deploy/README.md`에 있다.** 여기서는 다시 읽는 사람이
반드시 알아야 할 것만 적는다.

| | |
|---|---|
| 프로젝트 / 인스턴스 | `statcode` / `playground` (us-west1-a, e2-micro, Ubuntu 22.04, amd64) |
| 주소 | **`https://inquieto.dev`** (A 레코드 → 35.230.119.252) |
| 앞단 | Caddy가 80/443을 듣고 `127.0.0.1:8080`으로 넘긴다. 인증서는 자동 |
| 바이너리 | `/opt/blog/blog` — `main` push → GitHub Actions |
| DB | `/var/lib/blog/blog.db` — **여기가 정본이다** |
| DB 백업 | `/var/backups/blog/` — `blog-backup.timer`가 하루 한 번, 7벌 |
| 서비스 | systemd `blog` (`deploy/blog.service`) + `caddy` |
| admin 설정 | `/etc/blog/admin.env` (0600 root:root) — **저장소에 없다** |

- **`blog.db`는 WAL 모드다.** 인스턴스로 옮기기 전에 반드시
  `pragma wal_checkpoint(truncate)`를 하고 `blog.db-wal`이 0바이트인지 본다.
  본체만 복사하면 **최근 변경이 통째로 빠진 옛 DB가 올라간다** — 첫 배포에서
  실제로 그랬다(2026-08-23의 `서버 & API` 분리가 빠져 사이트가 `웹 프로그래밍`을
  보여줬다).
- **파일 sha256으로 "같은 DB냐"를 판정하면 안 된다.** SQLite는 **열기만 해도**
  본체를 바꾸고, WAL에 변경이 있으면 내용이 다른데도 본체 해시는 같게 나온다.
  실제로 이 두 성질이 겹쳐 "해시가 맞으니 같다"고 잘못 판단했다. 행을 뽑아
  해싱하거나(`select id||slug||status from posts order by id`), 올린 뒤 **렌더링된
  화면을 로컬과 diff**한다. 뒤엣것이 가장 확실하다.
- **CI는 `blog.db`를 절대 안 건드린다.** 서버 DB가 정본이고 admin 화면이
  거기에 글을 쓴다. 배포마다 덮으면 그 사이에 쓴 글이 통째로 사라진다.
  로컬 파이프라인 결과를 올릴 때는 `deploy/upload-db.sh`를 쓴다 —
  아래 "DB 백업과 정본 방향" 참고.
- **`-drafts`를 유닛에 넣지 마라.** 작성 중인 글 366편을 그대로 공개한다.
- **`-admin`은 유닛에 직접 적지 않는다.** 유닛은 `$BLOG_ADMIN_FLAG`를 넘기고,
  그 값은 인스턴스의 `/etc/blog/admin.env`에만 있다. 그 파일이 곧 client
  id·secret·허용 계정이 들어 있는 파일이라, **설정이 갖춰진 기계에서만 켜진다.**
  유닛에 `-admin`을 박으면 설정이 없는 기계에서 서버가 아예 안 뜨고
  (`adminAuth`의 "애매하면 안 뜬다"), 그러면 admin이 아니라 사이트가 죽는다.
  - `$BLOG_ADMIN_FLAG`는 **중괄호 없이** 쓴다. systemd는 `${VAR}`를 한 덩어리로,
    `$VAR`를 공백으로 쪼개서 넘긴다 — 비었을 때 빈 인자가 남으면 안 된다.
  - `EnvironmentFile` 앞의 `-`는 파일이 없어도 뜨라는 뜻이다. 없으면 플래그가
    비어서 admin이 그냥 안 열린다.
- **`blog`는 `127.0.0.1:8080`에만 붙는다.** 밖에서 그 포트에 직접 못 닿는 이유는
  방화벽 규칙이 없어서가 아니라 **소켓이 loopback에만 붙기 때문이다.** 방화벽만
  믿으면 규칙 하나 잘못 건드릴 때 그대로 열린다. Caddy의 관리 API(2019)도 같다.
- **인스턴스의 Go로는 이 저장소를 못 빌드한다** (거기는 1.23.4, `go.mod`는
  1.26.6). 빌드는 CI에서만 한다 — `CGO_ENABLED=0 GOOS=linux GOARCH=amd64`로
  13MB짜리 정적 ELF가 나온다.
- **바이너리는 `/opt/blog` 안에서 rename으로 갈아끼운다.** 같은 파일시스템이라
  원자적이고, 실행 중인 바이너리를 rename으로 덮는 것은 리눅스에서 허용된다
  (`ETXTBSY`는 `open(O_WRONLY)`에서 나지 rename에서 나지 않는다). `/tmp`에서
  곧장 mv하면 파일시스템이 달라 복사가 되고, 그 사이에 반쯤 쓰인 파일이
  실행될 수 있다.
- **재시작 확인만으로는 부족하다.** 마이그레이션이나 DB 때문에 뜨자마자 죽는
  경우가 있어서, 워크플로가 실제로 `http://127.0.0.1/`에서 200이 나오는지 본다.
- **되돌리기는 revert 후 재배포뿐이다.** 이전 바이너리를 남기지 않는다.
- **배포 확인은 두 군데를 본다.** 인스턴스 안에서 8080(blog 자신)과 80(Caddy를
  거친 길), 그리고 러너에서 `https://inquieto.dev/`다. 앞엣것만 보면 Caddy가
  죽었을 때 배포가 초록으로 끝나고, 인스턴스 안만 보면 DNS·방화벽·인증서가
  빠진 채로 초록이 된다.
- 인증은 서비스 계정 키(`GCP_SA_KEY` Secret) + OS Login + IAP 터널이다.
  22번을 0.0.0.0/0에 여는 대신 `allow-iap-ssh`(35.235.240.0/20)만 쓴다.

### HTTPS (2026-08-30)

**`https://inquieto.dev`가 정본 주소다.** Caddy가 80/443을 듣고
`127.0.0.1:8080`의 blog로 넘긴다. 인증서는 Let's Encrypt에서 자동으로 받고
자동으로 갱신한다 — 사람이 할 일이 없다.

- **`deploy/Caddyfile`이 정본이다.** 인스턴스에서 손으로 고치면 다음
  `setup-caddy.sh`와 갈라진다.
- **`www.inquieto.dev`는 Caddyfile에 없다.** 지금 그 이름은 Porkbun 파킹
  (`pixie.porkbun.com`)을 가리킨다. **적으면 그 이름의 HTTP-01 검증이 실패하고
  발급 전체가 막힌다** — 인증서 하나에 이름이 여럿이면 하나만 실패해도 다 안
  나온다. 쓰려면 A 레코드를 먼저 이 기계로 옮기고 그 다음 Caddyfile에 더한다.
- **Caddy를 깔 때 `policy-rc.d`로 자동 시작을 막는다.** 그 시점에는 blog가 아직
  80번을 물고 있어서, 뜨자마자 바인드에 실패하고 postinst가 0이 아닌 값을
  돌려주면 설치 스크립트가 통째로 죽는다.
- **`X-Forwarded-Proto`가 세션 쿠키의 `Secure`를 켜는 유일한 근거다.**
  blog 자신은 평문 8080을 듣고 있어서 `r.TLS`가 언제나 nil이다
  (`internal/admin/auth.go`의 `secure()`). 프록시를 바꿀 일이 생기면 그것도
  이 헤더를 붙이는지부터 본다.
- **HSTS는 `max-age=31536000`, `includeSubDomains`도 `preload`도 없다.**
  서브도메인을 나중에 HTTP로 쓸 여지를 남기고, preload는 목록에 올라가면
  되돌리는 데 몇 달이 걸린다. 되돌려야 하면 `max-age=0`으로 배포하고
  브라우저들이 그 값을 받아갈 때까지 기다린다.
- `encode zstd gzip`도 같이 켰다. `/dev`가 70,482 → 12,548바이트다.
- Caddy에 메모리 drop-in을 뒀다(`deploy/caddy.service.d/limits.conf`,
  128M/192M). **1GB짜리 기계라 여기서 새면 blog와 SSH가 같이 죽는다.**
  패키지가 가져온 유닛은 고치지 않는다 — 패키지를 올릴 때 덮인다.

확인한 것: `https://inquieto.dev/` 200(HTTP/2), `http://` → 308 →
`https://`, 인증서 `CN=inquieto.dev` / Let's Encrypt / 2026-11-28까지,
3.4MB 이미지 BLOB이 바이트까지 동일, 밖에서 8080·2019 둘 다 닿지 않음.

### DB 백업과 정본 방향 (2026-08-31)

**정본이 로컬에서 서버로 넘어갔다.** admin 화면이 열려 있어서 글이 서버의
`/var/lib/blog/blog.db`에 쓰인다. 로컬 `blog.db`는 이제 이관 파이프라인을
돌리는 **작업본**이다. 3단계(저장)를 막고 있던 마지막 선행 조건이 이것이었다.

| 방향 | 도구 | 언제 |
|---|---|---|
| 서버 → 로컬 | `deploy/fetch-db.sh` | **평소** |
| 로컬 → 서버 | `deploy/upload-db.sh` | 이관 파이프라인을 다시 돌렸을 때만 |

- **`fetch-db.sh`는 로컬 `blog.db`를 덮지 않는다.** `backups/`(gitignore)에
  날짜 이름으로 떨어뜨린다. 덮어쓰는 것은 사람이 보고 직접 한다 — 그 한 번이
  이관 작업본을 날리는 수다.
- **있는 것 중 최신이 아니라 지금 새로 뜬 것을 가져온다.** 어제 것을 받아놓고
  오늘 것이라 믿는 일이 없어야 한다.
- **`upload-db.sh`는 묻지 않고 덮지 않는다.** 올려놓기만 하고
  `deploy/upload-guard.sql`로 **덮으면 사라지는 것이 있는지 먼저 본다.**
  한 줄이라도 나오면 멈춘다.

  | 표시 | 뜻 |
  |---|---|
  | `LOST` | 웹에서 쓴 글이 사라진다 (`notion_page_id`가 NULL) |
  | `STALE` | 서버 쪽 `updated_at`이 더 최근 — 그 뒤에 웹에서 고쳤다 |
  | `LOSTIMG` | 웹에서 올린 이미지가 사라진다 |
  | `BACK` | **서버에서 지운 글이 되살아난다** (2026-08-31에 더했다) |

  가드를 끄는 스위치는 두지 않았다. **끄고 싶어지는 순간이 바로 잃는 순간이다.**

- **`BACK`은 앞의 셋과 방향이 반대다.** 저 셋은 "덮으면 잃는 것"을 묻는데
  이건 "덮으면 돌아오는 것"이다. **admin에 지우기가 생기면서 열린 자리다** —
  서버에서 지운 글이 낡은 로컬 작업본에 남아 있으면 올릴 때 조용히 되살아난다.
  - **`notion_page_id`가 NULL인 글만 본다.** `import`는 native 글을 절대
    만들지 않으므로(언제나 노션 page id를 넣는다), 올릴 파일에만 있는 native
    글은 **서버에서 지웠다는 뜻밖에 없다.** 그래서 오탐이 없다 — 노션에
    새 페이지가 열려 재이관한 정상적인 경우에는 안 걸린다(확인했다).
  - **노션에서 온 글은 여기서 안 본다.** 그건 재이관이 되살리는 것이 이미
    정해진 계약이고(지우기 화면도 그렇게 경고한다), 진짜로 빼려면
    `curation.DropPosts`에 적어야 한다. 여기서 같이 막으면 멀쩡한 재이관마다
    걸려서 확인이 습관이 되고, **습관은 곧 안 읽는 것이다.**
- **그래서 3단계의 저장 코드는 웹에서 만든 글에 `notion_page_id`를 넣으면 안
  된다** (`source`는 `native`). 가드가 "이관이 아니라 사람이 여기서 쓴 글"을
  그 NULL로 알아본다. 노션 id를 채우면 그 글은 가드에 안 걸리고 조용히 사라진다.

**백업은 인스턴스에서 하루 한 번 돈다** (`blog-backup.timer` → `deploy/backup-db.sh`).

- **`VACUUM INTO`다. 서버를 멈추지 않는다.** 읽기 트랜잭션 하나로 일관된
  스냅샷을 뜨므로 글을 쓰는 도중에 돌아도 반쪽이 안 나온다. `cp`는 그렇지
  않다 — WAL에만 있는 변경을 두고 본체만 베끼면 옛 DB가 나온다.
- **`.backup`이 아닌 이유**는 결과가 새 파일 하나라 `-wal`/`-shm` 짝을 따로
  챙길 필요가 없어서다. 덤으로 압축된다(82.6MB → 78.2MB, gzip 뒤 68.6MB).
- **뜬 백업을 그 자리에서 열어본다.** `integrity_check`가 `ok`가 아니면 지우고
  실패한다. 못 여는 백업은 백업이 아닌데, 그걸 복구하는 날에 알게 되면 늦다.
- **`setup-backup.sh`는 timer만 켜고 끝내지 않고 지금 한 번 돌려본다.** 안
  그러면 첫 실패를 하루 뒤에, 그것도 아무도 안 볼 때 알게 된다.
- 오래된 것은 **새 백업이 성공한 뒤에만** 지운다(7벌).
- **7벌은 인스턴스 안에만 있다.** 기계 밖으로 꺼내는 것은 `fetch-db.sh`다.
  버킷 자동 업로드는 아직 안 했다("남은 일").
- `/var/backups/blog`는 0700 blog:blog다. **glob을 `sudo` 안에서 풀어야 한다** —
  바깥에서 풀면 패턴이 아무것에도 안 맞고 리터럴이 그대로 `sudo ls`에 넘어가
  "No such file"이 난다. 실제로 여기서 한 번 걸렸다.

**지문은 언제나 행을 뽑아 해싱한다.** 파일 해시는 여기서 거짓말을 한다 —
SQLite는 열기만 해도 본체를 바꾸고, WAL에 변경이 남아 있으면 내용이 다른데도
같게 나온다(2026-08-24에 실제로 속았다). 스크립트 전부가 같은 식을 쓴다:
`select id,slug,status,updated_at from posts order by id`
(sqlite3의 기본 구분자가 `|`라 따로 이어붙이지 않는다).

확인한 것: 백업 3벌이 실제로 떠서 `posts=1356 images=445` · 지문
`53ba1ba2b94b1228`, `fetch-db.sh`로 받은 것이 서버·로컬과 지문까지 동일,
가드 SQL이 `LOST`/`STALE`/`LOSTIMG` 셋을 실제로 잡고 같은 DB끼리는 침묵.

### 밖에서 닿는 곳 (2026-08-24 점검, 2026-08-30 갱신)

**열린 포트는 80·443(블로그)과 22(SSH)뿐이다.** 실제로 듣고 있는 것은
Caddy(80, 443)와 sshd(22)이고, **blog(8080)와 Caddy 관리 API(2019)는
`127.0.0.1`에만 붙는다.**

```
LISTEN 127.0.0.1:2019   caddy   ← 관리 API. 밖에서 못 닿는다
LISTEN 127.0.0.1:8080   blog    ← **방화벽이 아니라 소켓이 막는다**
LISTEN         *:80     caddy   → 308로 443에 보낸다
LISTEN         *:443    caddy
LISTEN         *:22     sshd
```

**"방화벽에 규칙이 없으니 안전하다"로 두지 않았다.** 규칙은 사람이 실수로 열 수
있고 실제로 이 프로젝트에도 껍데기 규칙이 다섯 개 있었다. loopback에만 붙이면
규칙과 무관하게 닫힌다.

| 규칙 | 허용 | 출처 | 대상 태그 |
|---|---|---|---|
| `allow-iap-ssh` | tcp:22 | 35.235.240.0/20 | — |
| `default-allow-http` | tcp:80 | 0.0.0.0/0 | `http-server` |
| `default-allow-https` | tcp:443 | 0.0.0.0/0 | `https-server` |
| `default-allow-icmp` | icmp | 0.0.0.0/0 | — |
| `default-allow-internal` | tcp/udp 전체, icmp | 10.128.0.0/9 | — |
| `default-allow-ssh` | tcp:22 | 0.0.0.0/0 | — |

- **`test-jupyter`·`test-rstudio`·`test-vscode-server`·`test-http`와
  `default-allow-rdp`를 없앴다.** 전부 0.0.0.0/0인데 그 포트에 듣는 것이
  없었다. `test-http`가 80/443을 겸해 열고 있었지만 `default-allow-http`가
  `http-server` 태그로 같은 포트를 열고 인스턴스에 그 태그가 있어서 영향이 없다.
  **인스턴스는 `playground` 하나뿐이라** 대상 태그 없는 규칙을 지워도 다른
  기계가 딸려 죽지 않는다.
- **`default-allow-ssh`(22, 0.0.0.0/0)는 일부러 남겼다.** 지우면
  `--tunnel-through-iap` 말고는 들어갈 길이 없어진다. 배포는 이미 IAP를 쓰므로
  익숙해지면 지워도 된다.
- SSH는 **키 전용**이고(`passwordauthentication no`) 어느 계정에도
  `authorized_keys`가 없다 — 접근이 전부 메타데이터/OS Login을 거쳐 IAM으로
  통제된다. `unattended-upgrades`는 active다.

**공개된 글에 자격증명이 없다는 것을 전수로 확인했다.** 990편의 body에서 AWS
키·GitHub 토큰·OpenAI 키·Notion 토큰·Slack 토큰·개인키 블록·Google API 키·JWT·
DB 접속 문자열을 정규식으로 찾아 **0건**이다. `password=` 5건이 걸리지만 전부
튜토리얼 예시값이다(`your_password`, `admin_password`, 도커 컴포즈 예제 등).
**노션에서 옮겨온 글이 늘면 다시 돌려볼 것** — 학습 노트라 언제든 섞일 수 있다.

- **`/img/{sha256}`는 어느 글이 그 그림을 쓰는지 보지 않는다.** 그래서 draft만
  쓰는 이미지도 해시를 알면 받아진다. 현재 445개 중 1개가 그렇고(`OPTIONS` 글의
  PNG) 1개는 아무 글도 안 쓴다. 그 해시는 어느 공개 페이지에도 안 나오므로
  사실상 닿을 수 없어 그냥 뒀다. **draft를 더 가려야 할 일이 생기면 여기가
  마지막 구멍이다.**

**아직 안 한 것:** 백업을 기계 밖으로 자동으로 꺼내기(지금은 인스턴스 안
7벌 + 사람이 `fetch-db.sh`), robots.txt/sitemap.xml, 속도 제한(Caddy에서 하면
된다), `www` 리다이렉트, 배포 키를 Workload Identity Federation으로 옮기기.

**3단계(서버 DB에 쓰기)를 막던 것이 전부 없어졌다.** HTTPS가 2026-08-30에,
DB 정본 방향과 백업이 2026-08-31에 끝났다 — 아래 "Admin 화면" 참고.

## 마이그레이션

- 파일명은 `001_init.sql` 형식. 번호는 사전순이 아니라 **숫자순**으로 적용된다.
- **down 마이그레이션은 만들지 않는다.** 러너가 `*.down.sql`을 발견하면 거부한다.
  되돌려야 하면 되돌리는 내용의 새 마이그레이션을 추가한다.
- **이미 적용된 마이그레이션 파일은 고치지 않는다.** 러너가 sha256을 기록해두고
  내용이 바뀌면 실행을 거부한다 (DB의 실제 스키마와 파일이 갈라지는 걸 막기 위해).
- 각 마이그레이션은 트랜잭션 안에서 돌고, 실패하면 DDL과 적용 기록이 함께 롤백된다.

## SQLite에서 조심할 것

- **`_time_format=sqlite`를 DSN에서 빼지 마라.** 이게 없으면 드라이버가 `time.Time`을
  Go의 `String()` 형식으로 저장하고, SQLite의 `date()`/`datetime()`/`strftime()`이
  그걸 파싱하지 못해 **에러 없이 NULL**을 돌려준다. 발행일 기준 쿼리가 조용히 빈 결과를
  낸다. `internal/db/time_test.go`가 이걸 지키고 있다.
- **`foreign_keys`는 SQLite 기본값이 OFF다.** 커넥션 단위 설정이라 DSN으로 건다.
  `Open`이 실제로 켜졌는지 확인하고 아니면 에러를 낸다.
- 테스트에서 `:memory:`를 쓰지 마라. 커넥션 풀이 커넥션마다 별개의 인메모리 DB를
  만들어서 마이그레이션을 건 DB와 조회하는 DB가 달라진다. `t.TempDir()`에 파일로 만든다.
- **CHECK 제약은 나중에 못 고친다.** SQLite는 CHECK을 ALTER로 수정할 수 없어서
  테이블을 새로 만들어 옮겨야 하는데, 그건 아래 "삭제 금지" 원칙과 부딪힌다.
  값이 늘어날 수 있는 컬럼(`posts.source` 등)에는 CHECK을 걸지 않고 애플리케이션에서
  검증한다. `posts.status`는 세 값으로 확정이라 CHECK을 걸었다.

## 데이터 현황

- 노션 페이지 **1408개** 덤프 완료. 이미지 **446개** 로컬 저장 완료
  (`scripts/dump/images/{sha256}.{ext}`, 내용 해시 기준 dedup).
- **전부 이관 대상이다.** 일부만 골라 옮기는 게 아니다. 공개 여부는 삭제가 아니라
  `status` 컬럼으로 가린다.
- `notion-page-status.csv`가 계산해둔 초기 status: **`draft` 405개**(블록 5개 미만인
  stub이거나 제목 없음), **`unlisted` 1003개**(그 외 전부). `published`는 나중에 수동 지정한다.
  공개 서버의 status 뱃지는 **`draft`만 찍는다.** unlisted가 1003건이라 전부 찍으면
  목록이 그 글자로 덮인다.
- **노션 원본에 이미 중복된 글이 있다.** 수리통계1에서 8쌍(`카이제곱분포`, `t분포`,
  `F분포`, `베타분포`, `중심극한정리 증명`, `이변량 정규분포`, `다변량 정규분포`,
  `다항분포`)이 본문 sha256까지 똑같다. 2022-08-27에 만든 `확률분포 정리` 절을
  2022-12-30에 `6. 분포의 활용 : 예시`로 복사한 것이다. 노션 page id가 다르니
  이관은 정상이고, 어느 쪽을 남길지는 사람이 정할 일이다. 아직 안 정했다.
- **CSV의 제목 필드에 콤마가 들어간 행이 있다.** `cut -d,`나 `split(",")`로 파싱하면
  컬럼이 밀려서 status 자리에 `true`/`false`나 날짜가 들어온다. 실제로 이것 때문에
  한동안 375/936을 365/918로 잘못 알고 있었다. 반드시 CSV 파서를 쓸 것.

## 지켜야 할 것

**스키마**
- 스키마 변경은 **반드시 마이그레이션 파일로** 한다. 돌아가는 DB에 직접 `ALTER TABLE`을
  치지 않는다.
- **컬럼이나 테이블을 삭제하는 마이그레이션은 만들지 않는다.** 안 쓰게 된 컬럼은
  쓰지 않는 상태로 남겨둔다. 이름을 바꿔야 하면 새 컬럼을 추가하고 백필한다.

**`scripts/dump/` 는 수정하거나 삭제하지 않는다**
- 재수집에 **43분** 걸린다. 노션 API가 느리고 이미지를 전부 다시 받아야 한다.
- 읽기만 한다. 이관 코드가 이 디렉토리에 쓰지 않게 한다. 정리/청소 명목으로도
  건드리지 않는다. 파생물이 필요하면 다른 경로에 새로 만든다.

**작업 완료 기준**
- 테스트가 실패하는 상태에서 완료라고 말하지 않는다. 깨진 테스트가 남았으면
  그 사실과 출력을 그대로 보고한다.

**의존성**
- 새 의존성을 추가할 땐 **왜 필요한지 명시한다.** 표준 라이브러리로 되는 일이면
  표준 라이브러리로 한다. 특히 프론트엔드 쪽 의존성은 "빌드 스텝 없음" 원칙과
  충돌하는지 먼저 따진다.
- 현재 직접 의존성은 둘이다:
  - `modernc.org/sqlite` — SQLite 드라이버. cgo 없이 순수 Go로 빌드돼서
    `CGO_ENABLED=0`으로 정적 단일 바이너리와 크로스 컴파일이 된다.
    `mattn/go-sqlite3`는 cgo가 필요해서 쓰지 않는다.
  - `github.com/yuin/goldmark` — 마크다운 렌더러. CommonMark를 그대로 따르고
    순수 Go이며, **파서를 확장할 수 있다**. 확장이 필요한 이유는 수식이다(아래 참고).
    표·취소선·체크박스는 goldmark에 들어 있는 GFM 확장으로 처리한다.
    gomarkdown이나 blackfriday는 CommonMark 준수도가 낮고 확장 지점이 마땅치 않다.
  - 마이그레이션 러너는 외부 라이브러리(golang-migrate 등) 없이 `internal/db`에
    직접 구현했다. 이 프로젝트의 규칙(down 금지, 적용된 파일 수정 금지)을
    러너가 강제해야 하는데 범용 도구는 그 반대를 기본값으로 삼는다.

## 나중에 접근 권한이 열린 페이지 받기

integration에 페이지를 새로 연결하면 그 트리가 통째로 새로 보인다. 2026-08-16에
`웹 프로그래밍`(77) + `리눅스 & 쉘`(20) = **97개**가 이렇게 들어왔다.

```sh
# 1) 받을 page id 목록을 만든다 (search 결과에서 기존 덤프에 없는 것만)
# 2) 목록만 덤프한다. 기존 파일은 existsSync 검사가 건너뛴다.
node --env-file=.env scripts/notion-block-dump.mjs --pages-file <목록파일>
# 3) audit 인벤토리에 새 객체를 덧붙이고 status CSV를 다시 만든다
node --env-file=.env scripts/notion-page-status.mjs
# 4) 신규만 변환·이관한 뒤 나머지는 전부 다시 돌린다 (전부 멱등)
go run ./cmd/import -pages "<쉼표구분 id>" -db blog.db
go run ./cmd/relink -db blog.db -apply
go run ./cmd/categorize -db blog.db -apply
go run ./cmd/sortorder -db blog.db -apply
```

- `--pages-file`은 `notion-block-dump.mjs`에 나중에 붙인 옵션이다. 없으면
  `notion-audit-raw.json`(옛 목록)을 쓰므로 새 페이지를 못 받는다.
- **`notion-page-status.mjs`는 `notion-audit-raw.json`에 의존한다.** 새 페이지가
  거기 없으면 제목이 `(제목 없음)`, 경로가 자기 제목만으로 나온다. 새 객체를
  먼저 덧붙여야 한다.
- 전체 audit 재실행은 모든 페이지의 블록을 다시 읽어서 비싸다. `search`로 새
  객체만 추가하면 몇 초면 된다.

## 공개 서버 (읽기 전용)

`cmd/blog`. 인증과 접근 제어는 아직 없다. 글쓰기/수정은 `internal/admin`에
따로 있고 **`-admin`을 줘야 뜬다**(아래 "Admin 화면" 참고).

```sh
go run ./cmd/blog -db blog.db -addr 127.0.0.1:8080            # draft 가림 (배포와 같은 모습)
go run ./cmd/blog -db blog.db -addr 127.0.0.1:8080 -drafts    # draft까지 보임 (로컬 확인용)
go run ./cmd/blog -db /tmp/admin.db -admin -admin-no-auth -drafts  # admin까지 (로컬 전용)
```

### 없는 길과 터진 요청 (2026-08-24)

**404와 500도 이 사이트의 화면으로 나간다** (`internal/web/errorpage.go`,
`templates/error.html`). 홈 표제지와 같은 활자만 쓰고 사이드바가 함께 나온다 —
막다른 화면이면 뒤로 가기 말고는 길이 없다.

- **상태 코드는 그대로다.** 404 화면은 404를 준다. 화면이 생겼다고 200을 주면
  검색엔진과 링크 검사기에게 거짓말이 된다.
- **숨긴 글과 없는 글의 화면이 글자 하나까지 같아야 한다.** 다르게 말하면
  그 차이 자체가 "여기 draft가 있다"는 신호가 된다. 테스트가 지킨다.
- **`Cache-Control: no-store`.** 글 하나를 공개로 바꿨는데 중간 캐시가 404를
  들고 있으면 그 글은 계속 없는 것이 된다.
- **`/img/`와 `/static/`은 예전대로 글자 한 줄이다.** `<img>`가 오류 화면을
  받아봐야 깨진 그림 아이콘 하나로 끝나고, 67KB짜리 페이지만 헛되이 나간다.
  자산 라우트에서만 `http.NotFound`를 그대로 쓴다.
- **`GET /`를 catch-all로 등록했다.** 네 단계 이상처럼 어느 패턴에도 안 걸리는
  경로가 ServeMux 기본 404(글자 한 줄)로 빠지던 것을 막는다. `GET /{$}`가 홈이라
  더 구체적인 쪽이 이긴다.
- **`renderError`는 `render`를 거치지 않는다.** `render`는 실패하면 `fail`을
  부르고 `fail`은 다시 `renderError`로 오므로, 그 길로 가면 사이드바 조회가 한 번
  삐끗할 때 무한히 돈다. 여기서는 사이드바를 "되면 좋은 것"으로 다룬다 — 못
  가져오면 없는 채로 그린다. **오류 화면만은 DB가 어떻든 나가야 한다.**
- **본문을 다 그린 뒤에 한 번에 내보낸다.** 템플릿이 도중에 실패해도 반쯤 그린
  화면이 나가지 않는다. 그것마저 실패하면 마지막으로 글자만 내보낸다.
- 글자는 공통 UI라 `data-i18n` 키로 `preferences.js`의 고정 사전이 세 언어로
  바꾼다. **키가 코드(`errorInfos`)에 있고 화면에는 속성값으로만 나가서** 이름을
  바꿔도 조용히 한국어로 남는다 — `TestErrorPageTextIsInEveryDictionary`가 지킨다.

**panic이 연결만 끊고 끝나지 않는다** (`internal/web/recover.go`).

- **net/http도 panic을 잡는다.** 다만 잡은 뒤에 하는 일이 연결을 그냥 끊는
  것이라 브라우저는 빈 화면이나 "연결이 재설정됨"을 본다. 가로채면 스택이
  로그에 남고 읽는 사람에게는 500 화면이 간다.
- **panic은 응답을 쓰는 도중에도 난다.** 템플릿이 본문 절반을 내보낸 뒤 터지면
  상태 코드는 이미 200으로 나가 있어 바꿀 수 없다. `trackedWriter`가 무엇이든
  쓰기 시작했는지 기억해두고, **안 썼을 때만** 500 화면을 그린다. 이미 썼으면
  로그만 남기고 끊는다 — 잘린 HTML 뒤에 오류 화면을 덧붙이면 더 읽기 힘들다.
- **`http.ErrAbortHandler`은 되던진다.** 그건 "조용히 끊어라"라는 약속된
  신호라, 우리가 삼키면 그 뜻이 사라지고 로그만 지저분해진다.

**HTTP 타임아웃을 걸었다** (`cmd/blog`의 `httpServer`).

**`http.ListenAndServe`의 기본값은 전부 "무제한"이다.** 연결을 열고 요청을 끝내지
않는 클라이언트가 고루틴과 파일 디스크립터를 계속 물고 있어서, 공개 주소에 그대로
두면 느린 연결 몇 개로 서버가 멎는다. 인증이 없는 읽기 전용 서버라 더 그렇다.

| 값 | 왜 |
|---|---|
| `ReadHeaderTimeout` 5초 | GET뿐이라 받을 것이 사실상 헤더밖에 없다 |
| `ReadTimeout` 15초 | 〃 |
| `WriteTimeout` 60초 | 가장 큰 응답이 3.4MB짜리 이미지 BLOB이다. 60초면 57KB/s에서도 끝난다 — **짧게 잡으면 느린 회선에서 그림이 잘린다** |
| `IdleTimeout` 60초 | keep-alive를 그보다 오래 붙들지 않는다 |
| `MaxHeaderBytes` 64KB | 정상 요청에 쿠키도 인증 헤더도 없다 |

- 헤더를 끝내지 않는 소켓이 **5.0초 뒤 끊기는 것**을 실측했다. 그 전에는 영원히
  기다렸다.
- `cmd/blog/main_test.go`가 네 타임아웃이 0(무제한)이 아닌지, 쓰기가 읽기보다
  긴지 지킨다.

### draft는 남에게 안 보인다 (2026-08-23)

**`draft`는 목록·카운트·본문 링크 어디에도 안 나오고 `/p/{slug}`도 404다.**
지금 draft 366편이 가려지고 990편이 남는다. status의 뜻은 이렇게 굳혔다:

| status | 뜻 | 공개 서버 |
|---|---|---|
| `draft` | 아직 안 쓴 자리 | 숨긴다 |
| `unlisted` | 아카이브 본문 | 보인다 |
| `published` | 골라 앞에 세운 글 | 보인다 + 나중에 홈에 |

- **"published만 보여준다"가 아니다.** 그렇게 두면 published가 0건이라 사이트가
  텅 빈다. 이 사이트의 실체는 `unlisted` 990편이고, `published`는 나중에 홈을
  다시 짤 때 고를 표시로 남겨둔다.
- **판정은 `store` 한 곳에만 있다** (`notHidden`). 핸들러마다 챙기게 하면 새 조회를
  더할 때마다 구멍이 하나씩 생긴다 — render가 사이드바를 한 번에 채우는 것과 같은
  이유다. `-drafts`(`web.WithDrafts()`)면 예전처럼 전부 보인다.
- **기본값은 "가린다"다.** 켜고 끄는 것을 실수해도 새는 방향이 아니라 막는 방향으로
  틀리게 둔다.

**가리면 연쇄로 깨지는 자리가 있어서 함께 처리했다.**

| 무엇 | 규모 | 처리 |
|---|---:|---|
| 글 상세 | 366편 | `PostBySlug`가 걸러 404 |
| 목록·트리 | — | `PostsInCategory`·`ChildPosts`·`PostAncestors`·`InlineDBGroups`·`CategorySubtreePostSlugs`·`PathBranches` |
| 사이드바 글 수 | 1,356 → **990** | `subtreePostCount` |
| 표지가 draft인 분류 | 3개 | `coverSlug`가 빈 값을 줘서 표지 없는 분류로 다룬다 |
| draft를 가리키는 본문 링크 | 129개 / 32편 | **링크만 벗기고 글자는 남긴다** (`unlinkHidden`) |
| 통째로 비는 인라인 DB 목록 | 16개 | 〃 |
| 낱개 링크 상자 | 전부 draft 12 / 일부 47 | 위의 결과로 저절로 정리된다 |
| 글이 0이 된 분류 | 3개 | 아래 참고 |
| where42 카드 글 수 | 3 → 2 | 보이는 링크만 센다 |

- **표지가 draft면 반드시 걸러야 한다.** 표지는 본문을 목록 위에 **통째로 펼치므로**,
  안 거르면 draft 본문이 그대로 공개된다. 현재 pipex·Shell·Javascript 세 곳이고,
  지금은 평소 목록으로 그려진다.
- **링크는 지우지 않고 푼다.** 줄째 지우면 문장이 끊기고, 링크로 두면 눌러야 없는
  줄 아는 길이 된다. `[페이지 링크]` 자리표시자는 그냥 풀면 그 말이 글자로 드러나서
  **진짜 제목으로 바꿔** 남긴다.
- **`PostSummariesBySlug`만은 숨긴 글도 돌려준다** (`PostSummary.Hidden`).
  `resolveBody`가 **"posts에 없는 slug = 노션 데이터베이스"** 로 판정하기 때문이다.
  draft를 조회에서 그냥 빼면 draft 링크가 이 판정에 걸려 **엉뚱한 목록이 펼쳐진다.**
  세 상태를 구별해야 한다: 보인다 / 숨겼다 / 아예 없다.
- **`InlineDBGroups`는 행이 다 숨겨져도 이름을 남긴다**(빈 슬라이스). "행이 하나도
  안 남은 목록"과 "애초에 없는 목록"은 다르게 다뤄야 한다 — 앞은 링크를 풀고,
  뒤는 손대지 않는다(짝을 못 찾은 죽은 링크다).
- **글이 0이 된 분류는 노션 유래만 감춘다** (`visibleCategory`). `알고리즘 : 기초
  (1)`·`(2)`는 덤프가 만든 마디라 `0`으로 남으면 막다른 길이지만, `라이프`는
  사람이 앞으로 채우려고 세운 빈 선반이라 남긴다. 이 구분은 `source_name`이
  이미 하고 있다(NULL이면 사람이 만든 것).
- **본문은 짧아도 남을 묶는 마디는 `curation.StatusEdits`로 `unlisted`로 올린다.**
  덤프 분석이 블록 5개 미만을 draft로 보는데, 빅데이터 분석기사의 `개념정리`(자식 23)와
  `실전문제`(자식 8)가 그 어림짐작에 걸렸다. 가리면 표지의 두 링크가 글자로 풀리고
  자식 27편이 묶음을 잃는다. DB를 손으로 고치면 다음 `import -db`가 CSV 값으로
  되돌리므로 표에 적는다.
- 전수 검증: 숨긴 글 366편 URL이 **전부 404**, 보이는 글 990편 + 카테고리 88개
  **전 페이지 200**, 그 1,078페이지에 그려진 `/p/` 링크 986개 중 **숨긴 글을
  가리키는 것 0개**, 사이드바 합계 990 = DB 990.

| 라우트 | 하는 일 |
|---|---|
| `GET /{$}` | 표제지 하나뿐인 홈 (분야 탐색은 사이드바가 전담) |
| `GET /{l1}` `GET /{l1}/{l2}` `GET /{l1}/{l2}/{l3}` | 하위 분류 + 직속 글 |
| `GET /p/{slug}` | 글 상세 (마크다운 렌더링) |
| `GET /img/{sha256}` | 이미지 BLOB, Content-Type은 `images.mime` |
| `GET /static/{file}` | 바이너리에 박힌 JS |
| `GET /` | 어디에도 안 걸린 경로 → 404 화면 |

- **홈은 표제지 하나다** (2026-08-22). 눈썹줄·제목·리드 세 줄이 전부고 배경도
  테두리도 색면도 애니메이션도 없다. **분야로 가는 길은 사이드바가 전담한다** —
  사이드바가 모든 페이지에서 87개 분류 트리를 이미 들고 있어서, 홈이 같은 트리를
  한 벌 더 그릴 이유가 없었다. 자기소개 본문은 여전히 `/intro`에서만 편다.
  홈 전용 JS도 CSS 애니메이션도 없다.
- **`기록의 지도`(분야 카드 8장)와 `어디서부터 볼까요?`(추천 경로 3개)는
  `home.html`에 Go 템플릿 주석으로 재워뒀다.** 구성 자체가 틀린 게 아니라 순서만
  바뀔 물건이라 지우지 않았다 — 직접 쓴 글이 쌓이면 새 글이 `01`이 되고 이 지도가
  `02`로 내려간다. **대응 CSS 약 95줄은 함께 재우지 않고 지웠다**: `layout.html`은
  모든 페이지의 `<style>`에 들어가는데 CSS 주석은 Go 템플릿 주석과 달리 그대로
  실려 나간다(7,142바이트). 되살릴 때는 그 커밋의 부모에서 가져온다.
- **홈에 최근 글이 붙었다** (2026-09-01, `store.RecentPosts`). 예전에는 정직하게
  만들 수 없다고 적어뒀는데 **그 전제가 바뀌었다** — 아카이브의 최신 글이 실제로
  2025년이고, admin이 붙어 새 글은 쓰는 날짜가 그대로 들어간다. 그래서
  `original_created_at`이 곧 진짜로 쓴 날이다.
  - **날짜를 같이 찍는다.** "최근"이라는 말이 날짜보다 앞서 나가지 않게 —
    목록에 원본 작성일을 찍어 순서의 근거를 보이게 한 것과 같은 이유다.
  - 날짜 없는 글은 뺀다. 없는 순서를 지어내지 않는다. 6편, 중첩 없이.
  - 방명록은 여전히 없다.

- **카테고리 경로는 3단계까지 받는다.** 글 1408건 중 1279건이 3단계에 있어서,
  두 단계만 열면 대부분의 글에 탐색으로 닿지 못한다.
- 경로는 위에서부터 부모가 맞는지 확인한다. 존재하는 카테고리라도 엉뚱한 부모
  밑에 붙여 부르면 404다.
- `/{l1}`이 `/p/...`나 `/img/...`를 삼킬 것 같지만, Go의 ServeMux가 더 구체적인
  패턴을 먼저 고르므로 리터럴이 있는 쪽이 이긴다.
- **글 하나를 가리키는 경로는 `/p/{slug}`뿐이다.** `/{slug}`는 글이 아니라 최상위
  카테고리로 해석된다. 본문 안의 링크를 만드는 쪽(`internal/importer`의
  `PostURLPrefix`)과 이 라우트가 같은 접두사를 써야 한다. 한동안 어긋나 있었다.
- **본문의 죽은 링크 두 종류는 렌더링 직전에 손본다** (`internal/web/inline.go`).
  DB의 `body`는 건드리지 않는다. 변환기를 고쳐 재이관하는 길도 있었지만, 그러면
  1408건 재변환 + relink 재실행이 딸려오고 다음 재이관 때 또 날아간다.
  렌더링에서 처리하면 재이관과 무관하게 항상 맞는다.

  | | 무엇 | 어떻게 | 현재 |
  |---|---|---|---|
  | ① | `[페이지 링크](/p/{slug})` | 대상 글의 `title`로 글자만 바꾼다 | 14개 / 9글 |
  | ② | `[제목](/p/{데이터베이스 id})` | 그 밑 글들을 목록으로 펼친다 | 100개 / 57글, 763건 |

  - ①의 원인은 **`link_to_page` 블록에 제목이 없다는 것**이다(페이로드에 `page_id`만
    있다). 변환기는 페이지를 하나씩 따로 처리해서 그 시점에 제목을 알 수 없다.
    링크 글자가 `페이지 링크`와 **정확히 같을 때만** 바꾼다. 본문에 그냥 글자로
    "페이지 링크"가 들어 있는 글이 1건 있는데(`[Ch1] 빅데이터의 이해`) 안 건드린다.
  - ②는 **노션 데이터베이스가 페이지가 아니라 `posts`에 절대 안 들어가서** 생긴다.
    행 793건은 이미 posts에 다 있고, 없는 건 목록 그 자체다.
  - **데이터베이스 id로는 그 자식을 찾을 수 없다.** 남은 유일한 끈이
    `original_path`다. 한 층 더 들어간 글의 경로가 `{주인 경로} > {DB 이름} > {제목}`
    꼴이라 가운데 칸이 곧 DB 이름이고, 링크 글자와 **정확히 같을 때만** 펼친다.
    바로 아래 한 칸인 이름은 뺀다 — 그건 실제 글(child_page)이지 DB가 아니다.
  - **이름이 어긋나면 그냥 둔다.** 남은 것끼리 순서대로 짝지어 보는 것도 해봤는데,
    링크는 6개 더 품는 대신 **남의 글 46건이 섞여 들어갔다.** 지금 못 품는 19개는
    전부 이름이 `Untitled`/`New database`인 데이터베이스다.
  - 펼친 HTML은 **줄바꿈이 없어야 한다.** 마크다운 본문에 끼워 넣는데 CommonMark의
    HTML 블록은 빈 줄에서 끝나므로, 줄이 나뉘면 뒷부분이 마크다운으로 다시 해석돼
    태그가 글자로 드러난다. `inlineDBHTML`이 이걸 확인하고 어기면 에러를 낸다.
  - 목록 순서는 `sort_order`를 그대로 따른다. 데이터베이스 행의 sort_order는
    `created_time` 출처라 신뢰도가 낮아서 **번호순으로 안 보인다**(`2.`가 `1.`보다
    위에 오는 식). 없는 순서를 지어내지 않는다는 원칙대로 둔 것이다.
- **하나짜리 링크는 그 절의 내용이 그것뿐일 때만 묶는다** (`isWholeSection`,
  2026-09-02). 원래는 둘 이상만 묶었고 근거는 "하나짜리는 목록이 아니라 문장
  사이의 링크다"였다. 63개 중 36개는 실제로 그렇다. 그런데 나머지 27개는 제목
  바로 아래에 링크 하나만 있고 다음 제목까지 아무것도 없는 자리다 — 그건 **항목이
  하나뿐인 목록**이지 문장 사이의 링크가 아니다. `확률분포 정리` 표지에서
  `다변량, 연속형 분포`는 상자인데 바로 아래 `다변량, 이산형 분포`는 맨 링크
  하나로 나왔다. **같은 구조가 같아 보이지 않으면 읽는 사람이 그 차이를 뜻으로
  읽는다.**
- **줄줄이 이어진 낱개 글 링크도 같은 상자로 묶는다** (`groupLinkRuns`). 사람이
  `link_to_page` 블록을 여러 개 쌓아 만든 목록은 인라인 데이터베이스와 겉모습이
  달라질 이유가 없다.
  - **상자 안에는 작성일과 `draft` 뱃지를 함께 찍는다.** 본문에서 뽑아낸 링크에는
    slug와 글자밖에 없어서, 나머지는 `store.PostSummariesBySlug`로 가져와 채운다.
    이걸 안 넘기면 같은 모양의 상자가 한쪽만 날짜를 달고 나온다 — 실제로 그랬다
    (`/dev/tooling`의 `Git / Github` 상자).
  - **링크 글자는 본문 것을 쓴다.** 사람이 그 자리에 쓴 말이 제목과 다를 수 있고,
    자리표시자는 이 시점에 이미 진짜 제목으로 바뀌어 있다.
  - **짝이 없으면 날짜와 뱃지만 빠지고 링크는 남는다.** 전부 훑어보니 그런 자리는
    이름 없는 인라인 데이터베이스(`Untitled`/`New database`)를 가리키는 3곳뿐이고,
    그건 posts에 행 자체가 없는 것이다(아래 "남은 일" 참고).
- **글 상세에는 본문에서 뽑은 목차가 붙는다** (`markdown.Outline`). h1~h3만, 제목이
  3개 이상일 때만. **누르면 접힌다** — `<details open>`이 하므로 스크립트가 없어도
  된다(사이드바가 서버에서 펼쳐 나가는 것과 같은 이유). 삼각형은 사이드바가 쓰는
  것과 같은 SVG다. 목차는 **죽은 링크를 손본 뒤의 문자열**에서 뽑아야 앵커가 본문과
  맞는다. 같은 파서 설정으로 같은 원문을 두 번 읽으면 자동 id가 같은 답을 낸다.
- **본문 제목은 한 단계 내려서 그린다** (`markdown.headingShift`). 페이지의 `<h1>`은
  글 제목(또는 카테고리 이름)이고 템플릿이 그린다. 본문의 `# 제목`까지 `<h1>`이면
  한 페이지에 `<h1>`이 여러 개가 되어, 문서 구조상 형제가 아닌 것이 형제로 보인다.
  파서 단계(AST 변환기)에서 내리므로 **렌더링과 `Outline`이 같은 값을 본다.**
  - **앵커(id)는 안 바뀐다.** goldmark의 자동 id는 제목 글자로 만들고 단계를 보지
    않는다. 이미 나가 있는 `#앵커` 링크가 안 깨진다.
  - `Outline`은 사람이 읽을 단계(본문 기준 1부터)로 **되돌려서** 돌려준다.
    템플릿이 `toc-h{{.Level}}`로 들여쓰기 때문에, 안 되돌리면 목차 들여쓰기가
    통째로 한 칸 밀린다.
  - CSS도 같이 밀었다. 본문에서 가장 큰 제목은 이제 `h2`라 **`article h2`가 옛
    `article h1` 자리**를 쓴다. `article h1` 규칙은 본문에 h1이 없으므로 지웠다.
  - 글 1502페이지 전부 `<h1>`이 정확히 1개임을 확인했다.
- **제목 앵커 생성기를 갈아끼웠다** (`markdown.headingIDs`). goldmark 기본 구현은
  한 바이트가 아닌 글자를 전부 버려서 한글 제목이 죄다 `heading`, `heading-1`이
  된다. 링크는 되지만 주소에 뜻이 없고, **글을 고쳐 제목 하나만 늘려도 뒤쪽 앵커가
  통째로 밀린다.** 규칙은 카테고리 slug와 같다 — 소문자 + 공백/`:`/`/`/`.`→하이픈,
  나머지 기호는 버리고 한글은 그대로. 생성기는 쓴 id를 기억하는 상태라 파싱마다
  새로 만든다(`newParseContext`).
- **본문이 한 번도 안 가리키는 아래 갈래는 상자로 편다** (`store.SubBranches`,
  2026-09-02). 노션에서 온 표지 글은 **나중에 다른 출처로 들어온 글을 알 리가
  없다.** `Java` 표지의 목차 여덟 절은 기초 문법을 전부 안내하는데, 그 아래
  `Java > 모던 자바 >` 16편은 GitHub에서 2026-09-01에 들어온 것이라 본문에
  한 줄도 없었다. `프로그래밍 언어`는 카드만 보여주므로(카드가 있는 분류는
  카드만) **주소를 직접 치는 것 말고 닿는 길이 아예 없었다.**
  - 재료는 `original_path`다. `PathBranches`가 카테고리 화면에서 잃어버린
    첫 칸을 되살리는데, 그보다 깊은 칸은 그 갈래의 표지 글까지 와야 보인다.
  - **한 편이라도 본문에 링크돼 있으면 그 갈래는 안 그린다.** 판정은
    `dropLinkedChildren`과 같은 `linkedSlugs`다 — 두 곳이 같은 질문("본문이
    이미 안내했나")을 하므로 답도 한 곳에서 낸다. `문자열 다루기`와
    `자바 : 자료구조`가 그렇게 빠진다.
  - **두 편부터다.** 대부분은 갈래가 아니라 직속 자식 한 편인데, 그건 층이
    아니라 글 하나다. 이름표를 씌워 상자로 세우면 알려주는 것보다 늘어나는
    것이 많다.
  - **`hiddenBranch`는 웹의 화면 장치다.** `(제목 없음)`은 이름이 아니고,
    `수업 : 통수 & 선계`는 사람이 `curation.BodyEdits`로 이미 없앤 묶음이라
    여기서 되살리면 **코드가 사람이 지운 것을 되돌리는** 셈이다(남은 다섯
    편은 최적화이론의 평범한 글로 이미 목록에 선다). 갈래 카드·외부 링크와
    같은 이유로 코드에 둔다 — 본문(DB)에 적으면 `import -db`가 덮고,
    curation에 적으면 웹이 그 패키지를 읽게 되어 "DB가 정본"이 깨진다.
  - 상자 모양은 **인라인 데이터베이스를 펼친 것과 같다.** 둘 다 "이 글 아래에
    이런 목록이 있다"를 말하는 자리다.
  - 공개 글 962편을 전부 훑어 상자가 붙는 곳이 **둘뿐**임을 확인했다:
    `Java`의 `모던 자바` 16편, `R`의 `난수 만들기` 3편. 돌연변이 넷(guard를
    전부 없앰 / 한 편짜리도 세움 / 이름 검사 없앰 / 본문 링크 무시)을 넣어
    테스트가 실제로 실패하는 것을 확인했다.
- **글 상세의 "하위 글"에서, 본문이 이미 링크로 가리키는 글은 뺀다**
  (`dropLinkedChildren`). `cmd/postparent`가 `parent_id`를 채운 근거가 child_page
  블록인데 **그 블록은 본문에서 링크가 된다.** 그래서 손대지 않으면 본문에 목록이
  있고 그 아래 "하위 글"에 같은 목록이 또 나온다.
  - **본문 HTML에서 찾는다.** 원문 마크다운이 아니라 렌더링 결과를 보는 이유는,
    펼친 인라인 데이터베이스처럼 렌더링 단계에서 생기는 링크까지 세야 하기 때문이다.
  - **글 하나씩 따진다.** 카테고리 쪽 `dropCoveredChildren`과 달리 "일부만 겹치면
    남긴다"를 안 따진다. `Children`은 직속 하위 글의 평평한 목록이고 뺀 글로 가는
    링크가 본문에 그대로 있어서 길이 사라지지 않는다.
  - 지금은 하위 글이 있는 부모가 13편이다(수리통계1 6, 수리통계2 5,
    탐색적 자료분석 1, 확률과정론 1). 링크된 직속 자식만 빼기 때문에 일부만
    본문에 있는 경우에는 안내되지 않은 자식이 그대로 남는다.
- **표지 글 본문이 이미 통째로 펼친 하위 분류는 목록에서 뺀다**
  (`dropCoveredChildren`). 노션에서 한 절의 머리에 두던 인라인 데이터베이스가 두
  벌로 나타나기 때문이다: categorize가 경로에서 그 이름을 카테고리로 뽑아 "하위
  분류"에 넣고, 같은 데이터베이스가 표지 글 본문에도 링크로 남아 이제 목록으로
  펼쳐진다.
  - **이름이 아니라 글로 따진다.** `Language > 프로그래밍 언어`는 이름이 같은데
    분류에 191건이 있고 본문에 펼쳐진 건 7건뿐이다. 이름만 보고 빼면 184건으로
    가는 길이 사라진다.
  - 섹션을 통째로 없애지 않는 이유도 같다. `머신러닝 & 딥러닝`은 본문이 안 다루는
    분류가 하나 더 있다.
  - 표지 글이 있는 분류 중 9개가 이 처리에 걸리고, 그중 6개는 섹션이 통째로 사라진다.
- **좁은 화면(375px)에서 페이지가 가로로 밀리지 않는다.** 넘칠 수 있는 것은
  전부 **제 안에서만** 스크롤한다: `pre`(코드), `table`, `.math-display`,
  `.math-inline`. **1000페이지**를 375px 폭 iframe으로 훑어 문서 폭이 375를 넘는
  곳이 0개임을 확인했다(2026-08-19, 레이아웃 개편 뒤 재검증).
  - 훑을 때 **검사기가 진짜로 잡는지 먼저 확인할 것.** 900px짜리 div를 끼워 넣어
    918이 잡히는지 보는 식이다. "0개"가 검사가 안 돌아서 나온 0일 수 있다.
  - 배경 탭에서는 `requestAnimationFrame`이 안 돈다. 대기에 rAF를 쓰면 스윕이
    조용히 멈춘 채 "진행 중"으로 보인다. iframe의 `load`면 충분하다 —
    math.js와 highlight-init.js는 그 전에(DOMContentLoaded) 이미 돈다.

  손볼 때 깨지기 쉬운 자리 넷:
  - **인라인 `code`에 `white-space: nowrap`을 걸지 마라.**
    `array(['longitude', 'latitude', ...])` 같은 조각이 460px까지 삐져나간다.
  - **인라인 수식은 줄바꿈이 안 된다.** KaTeX가 한 덩어리로 그리므로
    `.math-inline`을 `inline-block` + `overflow-x: auto`로 가둔다.
  - **`overflow-x: auto`만 걸면 스크롤바 두 개가 생긴다.** 한 축이 `visible`이
    아니면 다른 축의 `visible`은 `auto`로 계산되므로 `overflow-y`까지 `auto`가 된다.
    KaTeX가 그린 식이 줄 상자보다 2px 높은 것만으로 세로 스크롤바가 뜨고, 그게
    폭을 15px 먹으면 이번엔 가로까지 넘쳐 가로 스크롤바가 또 뜬다 — 짧은
    `\sqrt x` 옆에 회색 알약 두 개가 붙는다. 수식 1528개 중 **730개(글 113편)**가
    이랬다(2026-08-21에 고쳤다). 세로는 스크롤할 것이 없으니 `overflow-y: hidden`,
    가로는 스크롤은 살리되 `scrollbar-width: none`으로 **막대만 감춘다.**
    여백으로 반올림 차이를 흡수하는 길도 재봤는데(실측 최댓값은 `\bar{d}`의 4px),
    그러면 매직 넘버가 남고 식 1526개에 없던 공백이 붙는다. **본문 폭을 진짜로
    넘치는 인라인 식은 1528개 중 2개뿐이다.** 블록 수식(`.math-display`)은 제 줄을
    차지하는 덩어리라 넘칠 때 막대가 보이는 편이 맞아서 이 규칙에서 뺐다.
  - **스크롤을 두 겹으로 두지 마라.** highlight.js 테마가 `code.hljs`에도
    `overflow-x: auto`를 걸어서, 안 지우면 `pre`와 `code`가 각각 스크롤한다.
  - **가로 스크롤은 `.codeblock pre`가 아니라 `article pre`에 걸어라.**
    들여쓰기 코드 블록에는 껍데기가 없다. 실제로 여기서 한 번 깨졌다(472px).
- **갈래가 몇 개 안 되는 분류는 목록 대신 아이콘 카드로 편다** (`internal/web/deck.go`).
  목록 한 줄짜리 링크로는 그 갈래가 무엇을 담는지 알 수 없다. **아무 데나 쓰지
  않는다** — 지금은 `데이터 & 수리`, 알고리즘, CS 이론, 개발, 프로그래밍 언어,
  마크업 / 스타일링 / 표현식, 프로젝트다.
  - **글 내용이 아니라 화면 장치라 코드에 둔다.** 표지 글 본문(DB)에 넣으면
    `import -db`가 덮어써서 사라진다.
  - **카드를 만드는 방법은 분류마다 하나씩이고 slug로 찾는다** (`deckSources`).
    대부분은 하위 분류만 있으면 되지만(`childDeck`) 언어와 마크업은 원본 경로에서
    다시 묶은 갈래가(`languageDeck`·`markupDeck`), 프로젝트는 표지 글 본문이
    (`projectDeck`) 더 있어야 한다. 그 차이를 핸들러의 if 문으로 늘어놓지 않는다 — 카드 묶음을
    더할 때 손대는 곳은 이 표 한 줄과 새 `deckSource` 하나다.
    **만들 수 없으면 error가 아니라 nil이다.** 그러면 화면이 평소 목록으로
    돌아간다. error는 DB 조회가 실패했을 때만이다.
  - **그림이 없는 하위 분류가 하나라도 있으면 통째로 포기한다.** 반은 카드고
    반은 목록이면 어느 쪽도 아니게 된다. 조용히 일어나는 일이라
    `TestChildDeckSlugsHaveArt`가 slug별로 그림이 있는지 지킨다 — 갈래를 옮기거나
    이름을 바꾸면 slug가 달라진다.
  - **평평한 잉크 상자다** (2026-09-01). 3D 원근·기울기·빛 훑기·떠다니는
    아이콘을 전부 뺐고 `static/deck.js`도 없앴다 — 깊이는 조명이 아니라
    hover의 "판 어긋남"(블러 없는 오프셋 그림자)으로만 표현한다.
  - `prefers-reduced-motion: reduce`면 판이 어긋나지 않는다.
  - **카드가 있는 분류는 카드만 보여준다.** 표지 글 본문과 글 목록까지 함께 두면
    같은 곳으로 가는 길이 세 벌이 된다. 카드가 곧 그 분류의 안내다.
  - **프로젝트는 예외적으로 하위 분류만 카드로 만들지 않는다.** école 42는 기존
    하위 분류로, where42와 심심조각은 `Projects` 표지 글의 해당 제목 앵커로 보낸다.
    두 서비스의 글 수는 그 DB 본문 절에 있는 `/p/` 링크를 세므로 코드에 박지 않는다.
    셋 중 하나라도 없으면 평소 목록으로 돌아간다.
  - **경로에서 다시 묶은 갈래 카드는 제 분류에 선다** (2026-09-01에 내렸다).
    `프로그래밍 언어` 6장(C·C++·Java·Python·R·TypeScript)과
    `마크업 / 스타일링 / 표현식` 7장(HTML·CSS·LaTex·Mermaid·정규 표현식·
    형식 지정자·아스키코드)이다. **예전에는 둘 다 한 층 위 `Language`에
    있었다** — 그러면 그 화면이 두 층을 섞어 보여준다: 언어 카드는
    `프로그래밍 언어` 안의 것이고 마크업 카드는 그 형제 분류다. 카드가 제
    분류에 있어야 사이드바에서 누른 곳과 화면에 나오는 것이 같아진다.
    `Language`는 그 하위 분류 둘을 그대로 편 2장짜리 카드가 된다 — **카드가
    세 층으로 이어지므로** 어느 층에서도 같은 것이 두 벌로 나오지 않는다.
  - **카드를 아래층으로 내리면 위층에서 눌러놨던 것이 드러난다.** 템플릿이
    표지 본문·하위 분류·글 목록을 전부 `not .Deck`으로 가리기 때문이다
    (`category.html`). 실제로 `Language`에서 카드를 걷어내자 표지 글이 펼친
    인라인 목록 13줄과 하위 분류 목록이 한 화면에 함께 나왔다 —
    "카드가 있는 분류는 카드만 보여준다"의 뒷면이다. 카드를 옮길 때는
    **떠난 자리에 무엇이 드러나는지** 함께 본다.
  - **재료는 `store.PathBranches(categoryID, prefix)`다.** 카테고리 트리는
    3단계가 끝이라 이 갈래가 `categories`에는 없지만 `original_path`에는
    그대로 남아 있다. **본문 없는 표지 글만 있는 갈래는 안 돌려준다** —
    눌렀는데 빈 화면으로 가는 `Swift`와 `Markdown`이 그 경우다.
  - **순서와 글자는 사람이 정한 표가 준다**(`languageOrder`/`languageArt`,
    `markupOrder`/`markupArt`). 갈래 이름은 경로에서 온 것이라 가나다순도
    알파벳순도 여기서 뜻이 없다. **표에 없는 갈래는 카드가 되지 않는다** —
    설명 없는 카드는 이름만 큼직한 목록 한 줄이라 세울 값이 없다.
    그래서 순서표에만 있고 그림표에 없는 이름은 **조용히** 사라지는데,
    `TestBranchOrdersHaveArt`가 그 어긋남을 잡는다. 실제로 `Swift`가
    그렇게 남아 있었다.
  - **갈래 카드는 JS 포인터 기울기를 쓰지 않는다.** CSS hover/focus만
    쓴다(`data-native`).
  - **`deckFor`는 "자식이 있어야 한다"를 보지 않는다.** 그건 하위 분류를
    그대로 카드로 바꾸는 `childDeck`에만 해당하는 조건이라 그쪽이 제 눈으로
    본다 — 이 둘은 자식이 없는 말단 분류다.
- **`하위 분류`는 머리띠를 두른 상자로 그린다** (2026-09-02, `.listbox`).
  인라인 데이터베이스를 펼친 목록과 **같은 모양**이다 — 둘 다 "여기 이런 목록이
  있다"를 말하는 자리라, 한 화면에서 생김새가 갈리면 무엇이 다른지 읽는 사람이
  알 수 없다. 실제로 `알고리즘: 이론`은 상자로, `알고리즘: 실전`은 밑줄 목록으로
  나와서 같은 것이 두 모양이었다.
  - **머리띠 정의는 한 곳이다.** `.inline-db`와 `.listbox`가 같은 규칙을 쓴다.
    띠는 코드 라벨·목차·표 머리와 함께 이 디자인의 시그니처라 정의가 갈라지면 안 된다.
  - **제목은 `<h2 class="section-title">`을 그대로 둔다.** `preferences.js`가 그
    **글자로** 번역 키를 찾으므로(`sectionKeys`) 요소나 글을 바꾸면 조용히
    한국어로 남는다. 띠 모양은 `listbox-title`이 덧입힌다.
  - `.section-title`이 들고 오는 h2 여백과 꼬리 괘선은 띠 안에서 끈다 — 괘선은
    상자 테두리가 이미 하고 있다.
- **아카이브 바깥으로 나가는 링크는 `internal/web/links.go`에 둔다.** 지금은
  `소개` 화면의 개인 페이지(`https://inryeolchoi.github.io`) 하나다. 자기소개를
  읽은 사람이 다음으로 갈 곳은 이 아카이브의 다른 분류가 아니다.
  - **갈래 카드와 같은 이유로 코드에 둔다.** 표지 글 본문(DB)에 적으면 다음
    `import -db`가 덮어써서 사라진다.
  - 본문 안의 외부 링크와 **같은 카드 모양**(`a.extcard`)을 쓴다. "여기서 나간다"는
    신호가 화면마다 다르면 안 된다. `rel="noreferrer"`도 같이 붙인다.
  - 글자는 `data-i18n` 키로 `preferences.js`의 고정 사전이 세 언어로 바꾼다.
    사전에 키가 없으면 언어를 바꿔도 조용히 한국어로 남으므로 테스트가 확인한다.
- **표지 글이 있는 카테고리는 그 본문을 목록 위에 펼친다.** 소개처럼 목록이 아니라
  글 자체가 그 분류의 알맹이인 경우가 있다. 한 번 더 눌러 들어가게 하지 않는다.
  펼친 표지 글은 아래 글 목록에서 제외하고, `표지` 뱃지와 `이 글만 따로 보기` 링크도
  쓰지 않는다. 내용 없는 카테고리의 `비어 있다.` 자리표시자도 그리지 않는다.
  - **머신러닝은 예외로 목록형 첫 화면을 쓴다.** 표지 지정은 DB에 보존하지만 웹에서는
    펼치지 않아, `수리/통계: 이론`처럼 두 하위 분류와 글 수만 먼저 보여준다.
- **표지가 안내한 글의 자손은 `original_path`로도 뺀다** (`dropShownPostTrees`,
  2026-09-02). 예전에는 `Children`만 훑었는데 **`parent_id`는 5개 카테고리
  172건에만 채워져 있다.** 나머지 글은 부모가 목차에 있어도 자손이 뿌리로
  남고, 그러면 `pathTree`가 그것들을 경로로 다시 묶으면서 **이미 뺀 부모를
  이름표로 되살린다** — 표지의 목록과 아래 `글`이 같은 사슬을 두 벌로 보여준다.
  `HTTP 완벽 가이드`가 그랬다(표지에 5편, 아래에 그중 하나의 자손 60여 편).
  - **경로를 견줄 때 구분자 ` > `를 붙인다.** 안 붙이면 `3. HTTP 메시지`가
    `3. HTTP 메시지 확장` 같은 형제까지 삼킨다. 테스트가 지킨다.
- **표지 본문의 목차가 가리키는 글 트리는 아래 `글` 목록에서 다시 그리지 않는다.**
  살아 있는 내부 글 링크와 펼친 인라인 DB 행을 모두 보고, 링크된 글과 그 자손을
  목록에서 뺀다. 표지에서 안내하지 않은 글은 그대로 남긴다. `데이터 & 수리`의
  12개 말단 페이지를 전수 점검해 상단 목차와 하단 목록이 겹치던 9개 페이지를
  이 규칙으로 정리했다.
- **표지 글에서 글 목록 아래로 보낼 절은 `notion_page_id`별로 이름을 적는다**
  (`internal/web/server.go`의 `deferredSections`, 2026-09-02에 일반화했다).
  전에는 "참고 동영상"이라는 절 이름 하나만 전역으로 찾았는데, 최적화이론의
  `수치해석과 코딩`도 같은 자리로 보내야 하면서 **이름이 같은 절이 다른 글에도
  생기면 엉뚱한 곳까지 떼어내는** 문제가 생길 수 있었다. 이제는 표지 글의
  `notion_page_id`를 키로 절 이름을 찾는다 — 상세 글은 원문 순서 그대로,
  카테고리에 펼칠 때만 그 절을 떼어 아래에 그린다. 현재 대상은 `선형대수`(참고
  동영상)와 `최적화이론`(수치해석과 코딩) 둘이다. 떼어낸 절도 `Assets` 검사에
  포함해야 유튜브 스크립트가 빠지지 않는다.
  - **사람이 정하는 화면 장치라 웹 코드에 둔다.** 갈래 카드·외부 링크·바깥으로
    나가는 링크와 같은 이유다 — 본문(DB)에 순서를 적으면 다음 `import -db`가
    덮어써서 사라진다.
- **카테고리 글 목록은 `parent_id`대로 중첩해서 그린다** (`nestPosts`). 뿌리는
  `parent_id`가 NULL인 글과 **부모가 그 목록 밖에 있는 글**이다. 후자가 흔하다 —
  카테고리 최상단 글들의 부모는 보통 한 단계 위 카테고리에 붙은 표지 글이다.
  그래서 "`parent_id IS NULL`인 것만 뿌리"로 잡으면 목록이 통째로 빈다.
- **`parent_id`가 없는 분류는 노션 경로에 남은 층으로 다시 묶는다**
  (`internal/web/pathtree.go`). `프로그래밍 언어` 191편처럼 한 층으로 쏟아지던
  목록이 그 대상이다 — categorize가 3단계에서 평평하게 만든 층이
  `original_path`에는 그대로 남아 있어서, 분류 이름을 닻으로 삼아 그 뒤의 칸을
  다시 세운다. `postparent`가 채운 `parent_id`와 달리 **DB는 안 건드리고 읽을
  때만 묶는다** — `sortPosts`가 제목 앞 번호를 읽는 것과 같은 성질이다.
  - **못 묶으면 error가 아니라 nil이다.** 그러면 화면이 예전 `posttree`로
    돌아간다. 반쪽짜리 묶음보다 평소 목록이 낫다.
  - **포기하는 조건이 넷이고 넷 다 테스트가 지킨다**(돌연변이로 확인했다):
    ① 사람이 만든 분류(`source_name`이 NULL)거나 12편 미만,
    ② `parent_id`가 이미 층을 만든 목록 — 계층이 두 벌이면 어느 쪽이 맞는지
    화면만 보고는 알 수 없다, ③ **한 편이라도 닻을 못 찾으면 통째로** —
    `PostMoves`가 다른 분류에서 데려온 글(선형대수 11편)이 그렇고, 그 경로로
    묶으면 남의 층을 빌려온다, ④ 마디 수가 글 수의 절반을 못 넘기면 —
    45편짜리 백준 목록처럼 노션에서도 원래 평평했던 곳이라 이름표만 늘고
    목록은 그대로다.
  - **닻은 경로에 마지막으로 나온 이름이다.** 같은 이름이 두 번 나오는 자리가
    실제로 있다(`네트워크 > HTTP 완벽 가이드 > HTTP 완벽 가이드 > …`) —
    분류가 된 페이지 안에 같은 이름의 인라인 데이터베이스가 또 있는 것이다.
  - **형제가 하나뿐인 층은 벗겨낸다.** 모두가 같은 첫 칸을 가지면 그 칸은
    아무것도 가르지 않고 들여쓰기만 한 칸 늘린다. 그 반복문은
    **`len(items) > 0`을 스스로 확인한다** — ①의 최소 개수 검사가 지금은 빈
    목록을 막고 있지만, 40줄 떨어진 검사에 반복문의 종료를 맡기지 않는다.
    실제로 돌연변이 시험에서 ①을 지웠더니 여기가 영원히 돌았다.
- 재귀 템플릿 `posttree`는 **layout.html에** 정의한다. 페이지마다 템플릿 집합을
  따로 파싱하는데 layout은 모든 집합에 들어가므로, 거기 둬야 category와 post
  양쪽에서 쓸 수 있다.
- **템플릿은 페이지마다 따로 파싱한다.** Go 템플릿은 이름 공간이 하나라 여러 파일을
  한 번에 파싱하면 각 파일의 `{{define "content"}}`가 서로를 덮어써서 마지막 것만
  남는다. 그러면 모든 페이지가 같은 내용을 그린다.

### 레이아웃과 색

**왼쪽 고정 사이드바 + 본문.** 사이드바는 모든 페이지에 나온다(`layout.html`).

- 트리는 **한 번의 조회**로 가져온다(`store.NavTree`). 87개뿐이라 전부 읽어
  Go에서 묶는다. 페이지마다 단계별로 세 번 찌르는 것보다 낫다.
- **최상위 8개는 늘 펼쳐진 목록**이고 그 아래(18 + 61)는 접혀 있다. 한 번에 다
  펼치면 목록이 화면보다 길어져서 지금 어디 있는지가 오히려 안 보인다.
- **지금 보고 있는 곳으로 가는 길만 서버가 펼쳐서 보낸다** (`markNav`, `nav.go`).
  펼침/현재 위치를 노드에 표시해서 넘기는 이유: Go 템플릿은 재귀할 때 dot이
  바뀌어서 바깥의 맵을 같이 넘기기가 번거롭다. 노드가 자기 상태를 들고 있으면
  템플릿은 그리기만 하면 된다.
- 그래서 **`sidebar.js`가 못 떠도 길이 막히지 않는다.** 스크립트가 더해주는 것은
  "다른 가지를 눌러 열기"와 좁은 화면의 서랍 여닫기뿐이다.
- 좁은 화면(60rem 미만)에서는 사이드바가 서랍으로 숨고 상단에 햄버거가 나온다.
  넓어지면 서랍 상태를 강제로 닫는다 — 안 그러면 scrim과 스크롤 잠금만 유령처럼
  남는다.

**본문 폭은 두 겹이다.** 산문은 `--measure`(46rem)에서 멈추고, **코드·표·블록
수식만** `--column`(62rem)까지 쓴다. 그것들은 줄바꿈이 아니라 스크롤로 감당하는
것이라 넓을수록 읽기 쉽고, 산문은 한 줄이 길어지면 눈이 되돌아오는 거리가 멀어진다.

**팔레트는 "편집숍"이다** (2026-09-01에 갈아엎었다). 흰 지면에 잉크빛
괘선(`#121415`), 포인트는 **형광 노랑 블록**(`#FFE600`) 하나다.
그림자·그라데이션·둥근 모서리를 쓰지 않고 **테두리 두께로 위계를 만든다.**

- **노랑은 잉크가 아니라 지면이다.** 글자색으로 쓰면 흰 바탕에서 대비가
  1.2:1이라 읽을 수 없다. 언제나 블록으로 깔고 그 위에 검정 글자를 얹는다 —
  형광펜으로 그은 자리에 가깝다.
- **그래서 링크가 노랗지 않다.** 링크는 잉크에 가깝게(`#1F2937`) 두고
  **밑줄로 알린다.** 색을 하나만 쓰는 데서 편집숍의 절제가 나온다 —
  링크까지 색을 다투면 지면이 시끄러워진다. 대신 hover에서 노랑이 깔린다.

- **강조는 색 글자가 아니라 색 지면이다.** 사이드바의 현재 위치, 홈 표제지의
  둘째 줄, 주 버튼이 전부 노랑 블록이고 그 위 글자는 언제나 검정이다.
- **머리띠(반전 블록)가 이 언어의 시그니처다.** 코드 라벨·목차·표 머리·인라인
  데이터베이스·최근 글이 전부 같은 띠를 쓴다 — 상자 안에 무엇이 들었는지
  말하는 자리는 언제나 같은 모양이다.
- **허용하는 그림자는 블러 없는 오프셋뿐이다**(`--hard-shadow`). 인쇄물의
  판 어긋남이지 빛이 아니다. 갈래 카드와 외부 링크 카드의 hover가 그것이다.
- 부가 정보는 `#5B6467`(흰 바탕 5.9:1)다.

**왜 갈아엎었나.** 옛 "썰물" 팔레트는 모래빛 표면에 파스텔 하늘색이라
부드럽고 무난했는데, 그 무난함이 곧 "AI가 만든 화면" 인상이었다. 갈래 카드의
3D 원근·빛 훑기·떠다니는 아이콘이 특히 그랬다 — 그림도 글자도 평면인데 배경만
입체인 척했다.

- **CSS 토큰 이름은 색이 아니라 역할이다** (`--surface`, `--rail`, `--badge-bg`,
  `--mark-bg` …). `--sand`, `--sky`로 짓지 않은 이유가 이것이다. 같은 역할에
  라이트/다크의 다른 색을 매핑하고 컴포넌트 규칙은 공유한다.
- **다크는 색 반전이 아니라 판의 반전이다.** 지면이 잉크가 되고 잉크가 지면이
  되므로, **머리띠는 밝은 띠에 검정 글자**가 된다. "반전 블록"이라는 정체성이
  테마와 무관하게 유지된다.
- **포인트 노랑은 양쪽에서 같은 색이다**(`#FFE600`). 검정 글자를 얹는 블록
  색이라 흰 지면에서도 검정 지면에서도 그대로 통한다 — 두 테마가 다른
  사이트처럼 갈라지지 않는다. 다크에서는 링크만 밝은 잉크로 바뀐다.
- highlight.js의 밝은 테마는 다크에서 읽히도록 토큰 색을 별도로 덮는다.
- **우측 하단 원형 버튼이 화면 설정을 연다** (`static/preferences.js`). 원과 패널을
  별도 레이어로 두지 않는다. `prefs-shell` 하나가 닫히면 46px 원이고, 열리면 우하단을
  기준으로 폭 304px·높이 280px인 둥근 사각형까지 직접 커진다. 설정 내용과 X 버튼은
  그 안에 있으므로 둘 사이에 틈이나 별도 외곽선이 없다. 바깥을 누르면 같은 껍데기가
  다시 원으로 줄어든다. 열린 모서리는 22px + `corner-shape: squircle`(지원 브라우저)다.
  - 한국어/English/Español 공통 UI는 고정 사전으로 바꾼다. 본문·제목은 브라우저의
    온디바이스 `Translator` API가 있을 때 코드·수식을 빼고 번역한다. 지원하지 않는
    브라우저에서는 조용히 성공한 척하지 않고 설정창에 지원 불가를 명시한다.
  - 테마는 시스템/화이트/다크 세 가지다. 시스템은 OS의 색상 변경 이벤트를 실시간으로
    따라가고, 명시적 테마는 고정한다. 선택은 `localStorage`에 저장한다. 저장된 테마는
    `<head>`에서 CSS보다 먼저 적용해 첫 화면이 밝게 번쩍이지 않는다.

### 코드 블록의 언어 라벨

`internal/markdown/codeblock.go`가 펜스 코드 블록을 껍데기로 감싼다:

```html
<div class="codeblock"><span class="lang">python</span><pre><code class="language-python">
```

- **브라우저가 아니라 서버가 붙인다.** 언어는 이미 본문에 적혀 있으니 JS를 기다릴
  이유가 없고, CDN이 막혀도 라벨은 남는다. highlight.js는 여전히 안쪽
  `code.language-*`만 보므로 하이라이팅은 그대로다.
- **`text`(265건)에는 라벨을 붙이지 않는다.** 그건 언어 이름이 아니라 변환기가
  "모른다"고 적어둔 값이다. 전부 `text`라고 찍으면 알려주는 것 없이 시선만 끈다.
  `plaintext`/`plain`/`txt`/`none`도 같이 뺀다. **클래스는 남긴다** —
  highlight-init.js가 그 값을 보고 "칠하지 말 것"으로 알아본다.
- 정보 문자열은 사람이 손으로 적은 것이라 무엇이든 들어온다. 라벨은 화면에 그대로
  나가므로 형태를 좁혀뒀다(소문자+숫자로 시작, 20자 이내). 안 걸리면 라벨만
  생략하고 클래스는 그대로 낸다.
- 현재 언어가 적힌 펜스 2578개 중 **2313개에 라벨이 찍히고 265개(전부 `text`)가
  빠진다.** 상위: python 548, c 440, r 423, bash 249, java 169, javascript 142,
  sql 134, cpp 71, mermaid 30.
- **들여쓰기(4칸) 코드 블록은 이제 0개다** (2026-08-21). 변환기가 코드를 언제나
  백틱 울타리로 내보내므로 결과에 4칸 들여쓰기가 나오면 그건 사고였다 —
  아래 "문단의 자식은 들여쓰지 않는다" 참고. 그래도 **상자(배경·테두리·가로
  스크롤)는 `.codeblock pre`가 아니라 `article pre`에 건다.** `.codeblock`은 라벨
  자리만 만들고, 껍데기 없는 `<pre>`가 다시 생기면 그때 상자가 없으면 긴 줄이
  375px 화면을 통째로 민다(실제로 472px까지 밀렸다).

### 코드 블록의 복사 버튼 (2026-08-31)

코드 블록 오른쪽 위 모서리에 아이콘만 있는 복사 버튼이 붙는다
(`internal/web/static/copy.js`). 언어 라벨과 같은 띠에 나란히 선다.

- **여기서는 서버가 안 찍고 스크립트가 만든다.** 언어 라벨과 정반대인데
  이유가 있다: 라벨은 스크립트가 없어도 읽을 수 있는 글자지만, **클립보드는
  JS 없이 쓸 수 없다.** 미리 찍어두면 스크립트가 꺼진 브라우저에 눌러도 아무
  일이 없는 죽은 버튼이 남는다. youtube.js가 누르기 전까지 그냥 링크로 있는
  것과 같은 점진적 향상이다.
- **`navigator.clipboard`는 보안 컨텍스트(HTTPS/localhost)에만 있다.** 없으면
  버튼을 아예 만들지 않는다. 못 하는 일에 버튼을 두지 않는다.
- **기준은 `<pre>`가 아니라 껍데기 `.codeblock`이다.** `article pre`에는
  `overflow-x: auto`가 걸려 있어서, 그 안에 absolute로 두면 **긴 줄을 밀 때
  버튼이 코드와 함께 흘러간다.** 껍데기는 스크롤하지 않는다. 실측으로
  확인했다 — 592px짜리 코드를 336px 창에서 끝까지 밀어도 버튼의 x가 357에서
  안 움직인다.
- **껍데기가 없는 `<pre>`는 copy.js가 씌우고 나서 단다.** `codeblock.go`는
  펜스 코드 블록만 감싸므로, 들여쓰기(4칸) 코드 블록이 다시 생기면 `<pre>`만
  홀로 나온다(현재 0개). 그때 붙일 기준이 없으면 버튼이 스크롤하는 pre 안으로
  들어간다.
- **`Assets.Copy`는 `Assets.Code`와 다른 판정이다.** Code는 "**색칠할** 것이
  있는가"라서 `text` 265건처럼 일부러 색칠을 건너뛰는 블록만 있는 글은 거짓인데
  **복사할 코드는 그대로 있다.** 그래서 언어 클래스가 아니라 `<pre>` 자체를
  센다. 실제로 갈리는 글이 있다 — `0701bd18…`은 highlight.js를 안 받고
  copy.js만 받는다.
- **`copy.js`는 `preferences.js`보다 앞에 둔다.** `defer`끼리는 순서가
  지켜지므로, 여기서 만든 버튼의 `aria-label`을 뒤이어 도는 사전이 세 언어로
  바꿔준다. `TestCopyScriptRunsBeforePreferences`가 지킨다.
- **`aria-label`은 `textContent`가 아니라 속성이다.** 상태가 바뀔 때마다
  문서 전체를 다시 훑으면 본문 번역까지 다시 돌게 되므로, `preferences.js`가
  요소 하나에 사전을 입히는 `window.blogLabel`을 열어뒀다.
- 아이콘 셋(복사/완료/실패)을 한꺼번에 넣어두고 **어느 것을 보일지는 CSS가
  정한다**(`.copy[data-state]`). JS는 `data-state` 한 글자만 바꾼다.
- **실패해도 조용히 넘어가지 않는다.** 눌렀는데 아무 표시가 없으면 복사된 줄
  안다. 실패는 `--warn`(라이트 `#B3261E`, 다크 `#F0938B`) 색의 × 표시다.
- **모바일에는 hover가 없다.** 평소에도 무엇인지 알아볼 만큼(`opacity: .68`)
  보이게 두고 hover/focus에서 또렷해진다. 전환은 위쪽의 `*` 규칙이
  `prefers-reduced-motion`에서 끈다.
- admin 미리보기도 같은 스크립트를 쓴다. `innerHTML`을 갈아치우면 버튼이 통째로
  사라지므로 `window.blogCopyButtons`를 다시 부른다 — `blogRenderMath` /
  `blogHighlight`와 같은 자리다.
- 헤드리스 브라우저로 확인했다: 29페이지 · 버튼 553개, **375px에서 문서폭이
  375를 넘는 곳 0개**, 껍데기 밖으로 나간 버튼 0개, 눌렀을 때 `<code>`
  textContent와 정확히 같은 글자가 클립보드에 들어감, aria-label이 ko/en/es로
  바뀜, 라이트·다크 모두 읽힘. **검사기가 진짜로 잡는지 먼저 봤다** — 900px
  조각을 끼우면 901px이 나온다.

### mermaid는 다이어그램으로 그린다 (2026-09-02)

```` ```mermaid ```` 코드 블록 **32개(글 22편)**가 그림이 된다
(`internal/web/static/mermaid.js`). 그전까지는 색도 안 칠해진 소스가 그대로
보였다 — hljs에 없는 언어라 "모르는 언어라 그대로 둔다"에 걸려 있었다.

- **원문 `<pre>`를 지우지 않고 감춘다.** 그린 SVG를 그 옆에 끼우고 CSS가
  (`.codeblock.has-diagram pre`) 원문을 감춘다. 그래야 **복사 버튼이 그대로
  소스를 집어가고**(copy.js는 `<code>`의 textContent를 읽는다) 못 그렸을 때
  되돌릴 것이 남는다. KaTeX가 **성공했을 때만** 원문을 대신하는 것과 같다.
- **못 그리면 소스가 그대로 남는다.** 반쯤 그린 것을 남기지 않는다. 실제로
  망가진 도형을 끼워 넣어 `<pre>`가 `display:block`으로 남는 것을 확인했다.
- **`startOnLoad`를 끈다.** 그건 `.mermaid` 요소를 제 마음대로 찾아 그리는데,
  우리 마크업은 코드 블록이고 무엇을 그릴지는 우리가 고른다 — `highlightAll()`을
  안 쓰는 것과 같은 이유다.
- **색은 지면에서 가져온다** (`theme: "base"` + `themeVariables`). mermaid의
  기본 팔레트는 연보라 도형에 노란 묶음 상자라, 잉크 괘선과 포인트 하나로
  버티는 이 사이트에서 **그 상자만 남의 것으로 보인다.** `--ink`·`--bg`·
  `--surface`·`--line`·`--mono`를 읽어 넘기므로 라이트·다크가 저절로 따라오고
  팔레트를 또 갈아엎어도 여기를 고칠 일이 없다. **`default`/`dark` 테마로는
  안 된다** — 그 둘은 `themeVariables`를 무시하고 제 팔레트를 고집한다.
- **테마를 바꾸면 다시 그린다.** `MutationObserver`가 `<html data-theme>`만
  본다 — preferences.js와 mermaid.js가 서로를 알 필요가 없다.
- **3.5MB짜리다.** 그래서 `Assets.Mermaid`가 참인 22편에서만 받는다. 같은
  이유로 **mermaid는 `skipLangs`에 있다** — 예전에는 그 30건 때문에
  highlight.js를 받아놓고 정작 칠할 것이 없었다. `static/highlight-init.js`의
  같은 목록과 맞춰야 하고, `TestNeedsForCodeMatchesHighlightInit`이 지킨다.
- **`Mermaid`는 `Code`와 다른 판정이다.** `Copy`가 `Code`와 다른 것과 같은
  자리다 — mermaid만 있는 글은 색칠할 것은 없지만 그릴 것과 복사할 것은 있다.
- admin 미리보기도 같은 스크립트를 쓴다(`window.blogRenderMermaid`). admin은
  무엇을 쓸지 미리 알 수 없어서 늘 받는다 — `PreviewAssetTags`가 `mermaid-cdn`을
  함께 뽑아간다.

**`Mermaid` 설명 글은 본문이 깨져 있었다.** 글쓴이가 노션에서 **인라인 코드
하나에 여러 줄을 넣고** 그 안에 `<div class="mermaid">`를 적어뒀다 — 그 시절
블로그의 렌더러가 그 div를 찾아 그렸던 것이라 원본에서는 뜻이 있었다. 여기서는
CommonMark의 코드 스팬이 그 모양으로 짝이 안 맞아 **백틱 하나가 글자로 남고
도형 소스가 문단으로 흘러나왔다.**

- **`curation.BodyEdits`로 여는 줄과 닫는 줄만 갈아끼운다.** 사이의 소스는
  손대지 않아도 되고, 줄 단위로만 고친다는 원칙에 그대로 맞는다. DB의 body를
  손으로 고치면 다음 `import -db`가 되살린다.
- **같은 줄이 두 번 나와서 항목도 두 벌이다.** `replaceLine`은 첫 번째 것만
  바꾸므로, 두 번 적으면 앞엣것이 바뀐 뒤 뒤엣것이 걸린다.
- 이 한 편만 다시 이관하고 `relink -apply`를 돌렸다.

확인한 것: 헤드리스로 22페이지를 훑어 **코드 블록 32개 전부 SVG가 됐고**,
375px에서 문서폭이 375를 넘는 곳 0개, 본문에 새어 나온 `<div class="mermaid"`
0개, 다크로 바꾸면 같은 그림이 다시 그려지고, 망가진 도형에서는 소스가 남는다.
돌연변이 셋(mermaid를 skipLangs에서 뺌 / `Mermaid`를 안 채움 / 늘 참으로)을
넣어 테스트가 실제로 실패하는 것을 확인했다.

### 본문 안의 바깥 링크는 카드가 된다

`internal/markdown/extlink.go`. **문단 하나가 통째로 링크일 때만** 카드다.

- 노션에서 `bookmark`와 `link_preview`는 블록이었고(변환기가 `[캡션](url)` 한 줄로
  옮겼다), 문장 속 인라인 링크는 원래 글자였다. 그 구분을 그대로 살린다. 문장
  중간의 링크까지 카드로 만들면 "자세한 건 [여기]를 봐라"가 상자로 끊긴다.
- 현재 외부 링크 117개 중 **71개(글 48편)가 제 문단을 통째로 차지**해서 카드가
  되고, 나머지 46개는 글자 링크로 남는다.
- **내부 글 링크(`/p/...`)는 절대 카드가 되지 않는다.** 스킴이 있는 http(s) 절대
  주소만 본다. 이게 깨지면 본문 링크 670개가 통째로 카드가 된다.
- 두 줄을 이렇게 정한다: 캡션이 있으면 `제목=캡션 / 아래=호스트`, 없으면
  `제목=호스트 / 아래=경로`. 캡션이 없는 bookmark는 링크 글자가 URL 그대로라,
  그냥 쓰면 두 줄이 같은 말을 두 번 한다.
- **쿼리는 화면에서 버린다.** 노션이 호스팅하던 첨부파일 URL은 서명이 붙어 쿼리만
  1,500자가 넘는다. `href`에는 그대로 둔다.
- `rel="noreferrer"`를 붙인다. 우리 주소가 상대 쪽 로그에 남지 않게 하려는 것이다.
- 아이콘은 **호스트에 상관없이 같은 기본 그림**이다. 진짜 파비콘은 이관 때 받아
  DB에 넣기로 했다 — "남은 일" 참고.

### 유튜브는 누른 뒤에야 부른다

본문에서 제 문단을 통째로 차지한 유튜브 링크는 카드 대신 **재생 자리**가 된다
(`internal/markdown/youtube.go`). 현재 링크 11개 / 글 7편, 그중 10개가 대상이다.

- **누르기 전에는 유튜브에 아무 요청도 가지 않는다. 섬네일조차 받지 않는다** —
  그것도 유튜브 서버에서 오는 것이라 글을 열기만 해도 독자 IP가 제3자에게 간다.
  파비콘을 외부에서 불러오지 않기로 한 것과 같은 이유다. 재생 삼각형은 우리가 그린다.
- 눌렀을 때 `youtube-nocookie.com` 플레이어를 끼운다(`static/youtube.js`).
  **겉은 여전히 `<a href="원래 주소">`라** 스크립트가 못 뜨면 그냥 유튜브로 간다.
- **referrer를 아주 끊으면 안 된다.** 유튜브 임베드는 출처를 확인해서 `no-referrer`면
  **"오류 153 · 동영상 플레이어 구성 오류"**로 재생을 거부한다. 실제로 겪었다.
  이미 사람이 눌러 연결에 동의한 뒤라 `strict-origin-when-cross-origin`으로 도메인까지만 보낸다.
- 재생목록(`list`)이 붙어 있으면 같이 넘긴다. 원래 링크가 목록의 한 편을 가리키던
  맥락을 잃지 않는다.
- `youtu.be`, `/embed/`, `/shorts/`도 같이 잡는다. id는 `[\w-]{6,64}`로 좁혀 둔다 —
  그대로 HTML 속성으로 나가기 때문이다.

### 브라우저에서 도는 것

**빌드 스텝은 여전히 없다.** 손으로 쓴 JS를 `internal/web/static/`에 두고
`embed.FS`로 바이너리에 박아 `GET /static/{file}`로 서빙한다. 라이브러리는 CDN에서
받는다(npm도, 번들러도 쓰지 않는다).

| 무엇 | 어디에 | 하는 일 |
|---|---|---|
| KaTeX 0.18.4 | CDN | `.math` 안의 원문 LaTeX를 그린다 |
| highlight.js 11.12.0 (common) | CDN | `<pre><code class="language-*">`를 색칠한다 |
| `static/math.js` | 바이너리 | 위 둘을 우리 마크업에 맞게 부른다 |
| `static/highlight-init.js` | 바이너리 | 〃 |
| `static/copy.js` | 〃 | 코드 블록 오른쪽 위의 복사 버튼 |
| mermaid 11.17.2 | CDN | ```mermaid 코드 블록을 다이어그램으로 그린다 |
| `static/mermaid.js` | 바이너리 | 위를 우리 마크업·팔레트에 맞게 부른다 |
| `static/anim.js` | 〃 | 이름 붙인 애니메이션(`:::anim`)을 그린다 |
| `static/palette.js` | 〃 | `/` 조각 팔레트. admin과 공개 편집기가 같이 쓴다 |
| `static/inline-edit.js` | 〃 | 글 화면에서 바로 고치기. **로그인 확인된 요청에만** |
| `static/sidebar.js` | 바이너리 | 사이드바 아코디언 + 좁은 화면의 서랍 |
| `admin/static/admin.js` | 〃 | admin 화면 전체 (CSR). `-admin`일 때만 |

- **CDN에서 받는 것은 그 페이지에 필요할 때만 받는다** (`internal/web/assets.go`).
  본문 HTML을 보고 정한다 — 서버가 이미 `.math`와 `language-*` 클래스를 붙여뒀으니
  그 표시를 세면 되고, 원문을 다시 파싱할 필요가 없다. `render`가 한 곳에서 채우므로
  핸들러마다 챙기지 않아도 된다(사이드바를 거기서 채우는 것과 같은 이유).

  | | 받는 페이지 | 전체 |
  |---|---:|---:|
  | KaTeX | 215 | 1502 |
  | highlight.js | 598 | 1502 |
  | mermaid | 22 | 1502 |
  | latex / dockerfile / powershell 묶음 | 5 / 4 / 1 | 1502 |

  - **`skipLangs`는 `static/highlight-init.js`의 같은 목록과 맞춰야 한다.**
    한쪽만 고치면 스크립트를 안 받았는데 칠할 것이 있거나, 받았는데 칠할 것이 없다.
    `text`(265건)처럼 "칠하지 말라"는 뜻인 언어만 있는 글은 highlight.js를 안 받는다.
  - 공통 묶음에 없는 언어(latex·dockerfile·powershell)는 그 언어가 실제로 나올 때만
    받는다. 예전에는 코드가 있는 모든 페이지가 세 개를 다 받았다.
- **CDN에는 무결성 해시(SRI)를 건다.** 버전을 올릴 때 해시도 같이 바꿔야 한다.
  안 바꾸면 브라우저가 **조용히** 실행을 거부한다. 해시는 이렇게 만든다:
  `curl -sL <url> | openssl dgst -sha384 -binary | openssl base64 -A`
- **CDN이 막혀도 페이지는 읽힌다.** 수식은 원문 LaTeX가, 코드는 색 없는 코드가
  그대로 보인다. 두 스크립트 모두 라이브러리가 없으면 아무것도 하지 않는다.
  다만 **오프라인이나 폐쇄망에서는 꾸밈이 사라진다** — 단일 바이너리 배포라는
  전제와 어긋나는 지점이라, 나중에 자산을 같이 박을지 정해야 한다.
- **`highlightAll()`을 쓰지 않는다.** 그건 언어가 없거나 모르는 블록에 자동
  추측을 돌린다. 여기서는 `text` 265건이 "색칠하지 말라"는 뜻이고 `mermaid`
  30건은 hljs에 없는 언어라, 추측을 돌리면 엉뚱한 색이 칠해진다. 등록된 언어가
  붙은 블록만 고른다. 현재 색칠 2282건 · 의도적으로 건너뜀 265건 ·
  모르는 언어라 그대로 둠 1건(nix). **mermaid 32건은 이제 다이어그램이다** —
  바로 아래 절 참고.
- common 묶음이 40여 개 언어를 담아 python·c·r·bash·java·sql 등 큰 것을 덮는다.
  거기 없는 것만 따로 붙였다(latex 16, dockerfile 4, powershell 2).

### 수식은 마크다운 파서로부터 지켜야 한다

`internal/markdown/math.go`가 `$...$`와 `$$...$$`를 goldmark보다 먼저 가로챈다.
**두 파서로 나눠 맡는다.**

| 파서 | 맡는 것 | 우선순위 |
|---|---|---|
| `mathBlockParser` (블록) | 제 줄에서 시작해 다음 줄 이후에 닫히는 `$$` | 99 |
| `mathParser` (인라인) | `$...$`와 한 줄로 닫히는 `$$...$$` | 강조보다 앞 |

**블록 파서가 반드시 필요하다.** 인라인 파싱은 블록이 다 정해진 뒤에 도는데,
수식 안의 한 줄이 블록 문법으로 먼저 읽히면 문단이 쪼개져서 인라인 파서에게는
이미 조각난 것만 온다. 실제로 이렇게 깨졌다:

```
$$
a\lambda^n + \cdots + c
=
(\lambda - \lambda_1) \cdots = 0
$$
```

`=` 한 글자만 있는 줄이 **CommonMark의 setext 제목 밑줄**이라 윗줄이 h1이 된다.
`#`(제목), `-`(목록), 백틱 세 개(코드 펜스)도 같은 식으로 문단을 끊는다. 그래서
우선순위를 setext 제목(100)보다 앞인 **99**로 둔다.

- **닫는 `$$`는 빈 줄 전까지만 찾는다.** 변환기가 블록 수식 안에 빈 줄을 남기지
  않으므로(`normalizeBlockMath`) 빈 줄은 곧 "닫는 짝이 없다"는 뜻이다. 예전에는
  문서 전체에서 다음 `$$`를 찾아서, 짝이 안 맞는 `$$` 하나가 뒤의 제목과 코드
  블록을 통째로 삼켰다. 그렇게 망가진 자리가 **64개** 있었고 KaTeX를 붙이니
  드러났다. 지금은 2898개 전부 정상 렌더링되고 글자로 남는 `$$`도 0개다.
- **한 줄로 닫히는 `$$...$$`는 블록 파서가 가져가지 않는다.** 가져가면 그 줄의
  앞뒤 글자가 사라진다. 인라인 파서에게 넘긴다.
그냥 넘기면 CommonMark 규칙이 LaTeX를 먹는다:

- `\{` → `{` (백슬래시 이스케이프)
- `X_t ... _n`의 밑줄이 짝지어져 `<em>`
- `*`가 강조로

가로챈 수식은 원문 그대로 들고 있다가 `<span class="math math-inline">` /
`<div class="math math-display">`로 내보낸다. **서버에서 수식을 그리지 않는다** —
브라우저에서 KaTeX가 그린다(`internal/web/static/math.js`).

- **KaTeX의 auto-render 확장을 쓰지 않는다.** 그건 문서 전체를 훑으며 `$...$`를
  찾는데, 본문에 진짜 `$`를 쓴 글이 90개다. 서버가 이미 수식을 골라 `.math` 안에
  `$` 없이 원문만 넣어놨으므로 **그 요소만 집어서** `katex.renderToString`을 부른다.
- **성공했을 때만 넣는다.** 실패하면 원문을 그대로 두고 `.katex-failed`만 붙인다.
  `throwOnError:false`로 KaTeX에게 맡기면 못 읽은 원문을 자기가 빨갛게 그리는데,
  여기서 실패하는 건 대개 수식이 아니라 본문이 통째로 딸려 들어온 자리라
  빨간 덩어리가 되면 더 읽기 힘들다.
- CDN에 **무결성 해시(SRI)**를 건다. 버전을 올릴 때 해시도 같이 바꿔야 한다 —
  안 그러면 브라우저가 조용히 실행을 거부한다.
- 스크립트가 못 떠도 원문 LaTeX가 그대로 보인다. 빈 자리가 되지 않는다.

- 코드 블록과 코드 스팬 안에서는 goldmark가 인라인 파서를 부르지 않는다. 그래서
  R의 `data1$col`이나 Makefile 변수는 저절로 걸러진다. 실제로 `$`가 270개인
  페이지를 확인해보니 math 노드가 0개였다.
- 이 확장은 변환기의 두 가지 보장에 기댄다: **인라인 수식은 한 줄**,
  **블록 수식 안에 빈 줄 없음**. 그래서 블록 파서 없이 인라인 파서 하나로 충분하다.
  변환기 쪽 정규화를 지우면 여기가 깨진다.

## Admin 화면 (진행 중)

`internal/admin`. 글을 웹에서 쓰고 고치는 화면이다. **네 단계로 나눠서 하고,
각 단계는 이전 단계가 끝난 뒤에 순서대로 한다.**

1. **✅ CSR 골격** (화면만, 저장·인증 없음) — 2026-08-25
2. **✅ 인증** — GitHub OAuth, 허용 목록의 계정만 — 2026-08-26
3. **✅ 저장** — 실제 DB 쓰기, 트랜잭션, 이미지 업로드, 메타 패널 — 2026-08-31
4. **AI 삽입** — MCP로 admin API를 노출해서 수식·이미지·코드 삽입을 AI에게 요청

> **✅ DB 정본은 이미 서버로 뒤집었다** (2026-08-31, 3단계 전에). 평소 방향은
> `deploy/fetch-db.sh`(서버 → 로컬)이고, 올리는 쪽은 `deploy/upload-db.sh`가
> 웹에서 쓴 글을 덮게 되면 멈춘다. 백업은 인스턴스에서 하루 한 번 돈다.
> 자세한 것은 위 "배포 > DB 백업과 정본 방향".
>
> **그래서 저장 코드가 지켜야 할 것이 하나 생겼다: 웹에서 만든 글에
> `notion_page_id`를 넣지 마라**(`source`는 `native`). 가드가 "이관이 아니라
> 사람이 여기서 쓴 글"을 그 NULL로 알아본다. 노션 id를 채우면 그 글은 가드에
> 안 걸리고 다음 `upload-db.sh`에 조용히 사라진다.

### 정해둔 것 (2026-08-26)

로드맵 뒤쪽을 어떻게 만들지 네 가지를 미리 정했다. **이 결정들이 2·3단계의
설계를 좌우하므로 먼저 읽는다.**

**① 본문 정본은 계속 마크다운이다.** 노션처럼 블록 JSON을 정본으로 두는 길은
안 간다. 그러면 렌더링 파이프라인(goldmark + 수식 파서 + 코드 라벨 + 외부 링크
카드 + 제목 내리기)을 통째로 다시 써야 하고, **1357편을 마크다운 → 블록으로
되돌려야 한다** — 노션에서 마크다운으로 옮겨온 것을 다시 블록으로 옮기는 셈이다.
쓸 만한 블록 에디터는 대부분 npm/번들러를 전제해서 "빌드 스텝 없음"과도 부딪힌다.

**② ✅ 노션식 `/` 팔레트는 마크다운 위에 얹는다** (2026-08-31에 만들었다). `/`를 누르면 팔레트가 뜨고,
고르면 **마크다운 조각이 커서 자리에 꽂힌다**(코드 펜스, `$$수식$$`, 이미지, 표…).
WYSIWYG가 아니라 지금처럼 왼쪽 편집·오른쪽 미리보기다. 노션 느낌의 대부분은
팔레트 하나에서 오고, 그 값에 ①을 버릴 이유는 없다. **3단계(저장) 뒤에 만든다** —
저장이 되기 전에 만들면 예쁘게 꽂히는데 안 남는다.

**③ ✅ 편집은 결국 공개 글 화면 안으로 들어간다** (네이버 블로그처럼, 2026-08-31에 했다). 인증되면
글 화면에 편집 버튼이 나타나고 거기서 바로 고치는 형태다. **이건 어려운 쪽이
아니다** — 이 사이트는 이미 SSR이라 `layout.html`에 세션 조건 한 줄이면 된다.
지금 `/admin`을 별도 앱으로 만든 건 인증이 없어서 격리해 둔 것이고, 2단계가
끝나면 옮길 수 있다. **1단계 결과물은 버려지지 않는다** — `/api/admin/*`와
미리보기 렌더러가 그대로 그 편집창의 뒷단이 되고, `/admin` 목록은 "글 관리
화면"으로 남는다.

**④ ✅ 애니메이션을 본문에 JS로 박지 않는다** (2026-08-31에 `:::anim`으로 만들었다). 렌더러가 `html.WithUnsafe()`라
(`internal/markdown/render.go`) 본문에 `<script>`를 쓰면 **이미 실행된다.** 그래서
"되냐"가 아니라 "그래도 되냐"가 문제다. 본문에 임의 JS를 담기 시작하면 그 글
자체가 XSS 벡터가 되고 — **4단계에서 AI가 본문을 만들기 시작하면 특히 그렇다** —
나중에 CSP를 걸 길도 막힌다. "HTML → CSS → 마지막에 JS" 원칙과도 어긋난다.
대신 **이름 붙인 컴포넌트 중에서 고르게 한다**: 본문에는 `:::anim sort-bubble`
같은 표시만 들어가고 실제 JS는 `internal/web/static/`에 사람이 쓴 파일로 있다.
본문은 안전한 텍스트로 남고, 같은 애니메이션을 여러 글에서 다시 쓸 수 있다.

**✅ 글별 메타 패널은 3단계에서 같이 만들었다.** `분류 · 계층 · 날짜`를 접어둔
`<details>` 안에 분류(select)·부모 글(slug)·형제 순서·작성일을 둔다. 아래
"3단계로 만든 것" 참고.

- **✅ 옛 글 편집 화면에 경고를 띄운다.** 노션에서 온 글은 제목·날짜·순서를
  `internal/curation`이 이기고 본문은 `import -db`가 통째로 덮는다. 웹에서 고쳐도
  **다음 재이관에 그냥 사라진다.** `PostDetail.Source`와 `Managed`가 그걸
  알려주고 편집 화면이 폼 위에 띠로 적는다.

### 1단계로 만든 것

- **`-admin` 플래그가 없으면 화면 자체가 없다** (`cmd/blog`). 인증이 2단계라
  지금 이 화면은 아무나 글을 고칠 수 있다. 배포 유닛(`deploy/blog.service`)은
  이 플래그를 주지 않고, `TestAdminIsOffUnlessAskedFor`가 이걸 지킨다.
  **`-drafts`와 같은 원칙이다** — 실수해도 새는 방향이 아니라 막는 방향으로 틀린다.
- **`internal/web`과 다른 패키지다.** web은 "읽기 전용 공개 페이지"고 draft를
  어디에도 안 보여주는 것이 그쪽 규칙인데(`notHidden`), admin은 반대로 **draft가
  주인공**이다. 한 패키지에서 플래그로 가르면 언젠가 공개 쪽에 샌다.
- **붙이는 방법은 바깥 mux 하나다** (`cmd/blog`의 `withAdmin`). 공개 mux에는
  `GET /` catch-all이 있어서 같은 mux에 넣으면 두 패키지가 라우팅을 나눠 갖게 된다.
  ServeMux가 더 구체적인 패턴을 먼저 고르므로 `/admin/`이 `/`를 이긴다.

| 라우트 | 하는 일 |
|---|---|
| `GET /admin`, `GET /admin/{rest...}` | 껍데기 하나. 안은 `admin.js`가 그린다 |
| `GET /admin/static/{file}` | admin 전용 JS·CSS |
| `GET /api/admin/posts` | 목록 (**draft 포함**, 본문은 안 싣는다) |
| `GET /api/admin/posts/{slug}` | 글 하나 + 본문 |
| `POST /api/admin/preview` | 마크다운 → HTML (실시간 미리보기) |
| `GET /api/admin/categories` | 메타 패널의 분류 선택지 (경로까지 만들어서) |
| `POST /api/admin/posts` | 새 글. 201 + 저장된 글 |
| `PUT /api/admin/posts/{slug}` | 고치기. `{slug}`는 **지금의** slug다 |
| `POST /api/admin/images` | 이미지 저장. sha256으로 dedup |
| `GET /api/admin/posts/{slug}/refs` | 지우면 무엇을 잃는지 |
| `DELETE /api/admin/posts/{slug}` | 글 지우기. rev와 확인이 필요하다 |

### 미리보기가 실제 화면과 같아야 한다

이게 이 단계의 핵심이다. 미리보기가 발행 뒤 화면과 다르면 미리보기가 아니다.
그래서 **세 가지를 복사하지 않고 공개 쪽에서 가져다 쓴다.**

| 무엇 | 어디서 | 왜 |
|---|---|---|
| 마크다운 렌더러 | `markdown.New()` | 수식·코드 라벨·외부 링크 카드·제목 내리기가 전부 그 안에 있다 |
| 스타일시트 | `web.SiteCSS()` | `layout.html`의 `{{define "sitecss"}}`를 그대로 뽑아온다 |
| CDN 태그 | `web.PreviewAssetTags()` | `{{define "katex-cdn"}}` / `{{define "hljs-cdn"}}` |

- **`layout.html`에서 CSS와 CDN 태그를 이름 붙은 템플릿으로 떼어냈다.**
  이름만 붙였을 뿐이라 **공개 페이지의 출력은 한 바이트도 안 바뀐다**(표본
  43페이지로 확인했다). 정본이 한 곳이라 admin과 공개 화면이 갈라질 수 없다.
- **CDN 태그를 특히 복사하면 안 되는 이유는 SRI 해시다.** 주소와 버전만 베끼고
  해시를 안 맞추면 브라우저가 **아무 말 없이** 실행을 거부한다. 실제로 이 작업 중에
  해시를 잘못 적었다가 layout에서 뽑아 쓰는 쪽으로 고쳤다.
- **`static/math.js`와 `static/highlight-init.js`도 공개 것을 그대로 쓴다.**
  미리보기는 본문을 계속 다시 그리므로 그때마다 다시 불러야 하는데, 두 스크립트가
  자기 함수를 `window.blogRenderMath` / `window.blogHighlight`로 열어둔다.
  자동 실행은 그대로다 — 공개 페이지 동작은 안 바뀐다.
- **아직 다른 점 하나:** 실제 글 화면은 렌더링 직전에 죽은 링크를 손본다
  (`web`의 `resolveBody` — `[페이지 링크]` 자리표시자와 인라인 DB 펼치기).
  그건 노션에서 온 본문에만 해당하고 DB 조회가 필요해서 미리보기에서는 안 한다.
  **새로 쓰는 글에는 그런 링크가 없으므로 지금은 차이가 없다.** 옛 글을 admin에서
  열었을 때만 그만큼 다르다.
- 공개 페이지는 본문을 보고 필요할 때만 CDN을 받지만(`assets.go`) admin은 늘 받는다.
  작업 도구라 무엇을 칠지 미리 알 수 없다. 다만 common 묶음 밖의 언어
  (latex·dockerfile·powershell)는 아직 안 받아서 미리보기에서만 색이 없다.

### 성공한 척하지 않는다

1단계에서 저장과 업로드는 501을 줬다. **그 띠가 사라지는 시점이 곧 3단계가
끝난 시점**이라고 적어뒀고, 2026-08-31에 사라졌다. 남은 띠는 `-admin-no-auth`로
떴을 때의 경고 하나뿐이다.

**원칙은 그대로 뒤집혀 남는다: 실패를 숨기지 않듯 성공도 삼키지 않는다.**
새 글을 저장하면 주소가 바뀌면서 폼을 다시 그리는데, 그때 "저장했다"가 같이
지워지면 사람은 저장이 됐는지 알 수 없다. 다시 그리는 폼에 그 말을 넘긴다
(`renderEditor`의 `note`). **실제로 그렇게 지워지던 것을 브라우저에서 보고
고쳤다.**

### 2단계로 만든 것 — GitHub 로그인 (`internal/admin/auth.go`)

**허용 목록에 적은 GitHub 계정 말고는 아무도 못 들어온다.**

- **OAuth는 "누구인지"만 알려준다. "들어와도 되는지"는 우리가 정한다.**
  GitHub 계정은 누구나 몇 초면 만드니까, 로그인에 성공했다는 것만으로 통과시키면
  인증이 없는 것과 같다. `allows()`가 진짜 관문이고,
  **`AllowedLogins`가 비면 "전부 허용"이 아니라 "전부 차단"이다** — 설정을
  빠뜨렸을 때 열리는 쪽으로 틀리는 것이 여기서 제일 위험한 실수다.
- **비밀번호를 우리가 들고 있지 않는다.** 사용자 테이블도 비밀번호 해시도 없고
  앞으로도 만들 이유가 없다.

**세션은 서명한 쿠키 하나다.** 세션 테이블을 안 만든다 — 마이그레이션이 늘고,
지우는 마이그레이션을 안 만든다는 원칙 때문에 나중에 물리기도 어렵다. 대신
`login|만료`를 HMAC-SHA256으로 서명해 쿠키에 담는다.

- `HttpOnly` — 본문 렌더러가 `html.WithUnsafe()`라 본문에 들어온 스크립트가
  실행될 수 있다. 그때 세션까지 가져가면 안 된다.
- `SameSite=Lax`, 만료 12시간. 훔쳐간 쿠키를 서버에서 끊을 방법이 없어서 짧다.
- **`Secure`는 HTTPS로 온 요청에만 붙인다.** 지금 배포가 평문 HTTP라 무조건
  붙이면 브라우저가 쿠키를 아예 안 보낸다 — "로그인이 되는데 로그인이 안 되는"
  상태가 된다. `r.TLS`나 `X-Forwarded-Proto`를 본다.
- **서명이 맞아도 허용 목록을 다시 본다.** 설정에서 계정을 빼면 살아 있는 쿠키도
  그 순간부터 안 통해야 하는데, 세션 테이블이 없어서 취소할 길이 이것뿐이다.
- `BLOG_SESSION_KEY`를 안 주면 시작할 때 무작위로 만든다. 재시작하면 다시
  로그인해야 하지만, 키를 저장소나 유닛 파일에 적어두는 것보다 낫다.

| 라우트 | 하는 일 |
|---|---|
| `GET /admin/login` | 로그인 화면. **관문 밖이다** |
| `GET /admin/auth/start` | state를 심고 GitHub으로 보낸다 |
| `GET /admin/auth/callback` | code → 토큰 → login → 허용 목록 대조 |
| `POST /admin/logout` | 세션 쿠키를 지운다 |

- **관문(`guard`)은 mux보다 바깥이다.** 핸들러마다 챙기게 하면 새 API를 더할
  때마다 구멍이 하나씩 생긴다 — web이 draft 판정을 `store` 한 곳에 모은 것과
  같은 이유다. `openPaths`에 없는 `/admin`·`/api/admin`은 **전부 막힌다**.
- **API에는 로그인 화면을 주지 않는다.** `fetch()`가 받는 자리라 HTML이 오면
  JSON 파싱에서 터지고 "로그인이 풀렸다"는 진짜 이유가 가려진다. 401 JSON이다.
- **`state`는 CSRF 방지다.** 없으면 남이 자기 code로 우리 callback을 부르게
  만들어서 브라우저 주인이 모르는 사이에 **남의 계정으로 로그인시킬** 수 있다.
  쿠키 path가 `/admin`이라 **지울 때도 같은 path를 줘야 한다** — 다르면 안
  지워지고 같은 이름이 두 개가 된다.
- **로그아웃은 POST다.** GET이면 남의 페이지에 박아둔 `<img>` 하나로도 사람을
  로그아웃시킬 수 있다.
- **`redirect_uri`를 우리가 만들어 보내지 않는다.** GitHub이 OAuth 앱에 등록된
  주소를 쓴다. 조립하는 자리를 만드는 것 자체가 위험하고 등록된 것과 어긋나기도 쉽다.
- `scope`는 비운다. 알아야 할 것이 `login` 하나뿐이라 저장소나 이메일 권한을
  달라고 할 이유가 없다.
- 거절당한 계정 이름은 **로그에만** 남기고 화면에 도로 찍지 않는다. 남이 지은
  이름이 우리 화면에 나오는 자리를 만들지 않는다.

**설정은 환경변수로 받는다. 플래그로 받지 않는다** — client secret이 플래그면
`ps`에 그대로 보이고 셸 히스토리에도 남는다.

| 환경변수 | 뜻 |
|---|---|
| `BLOG_GITHUB_CLIENT_ID` | OAuth 앱의 client id |
| `BLOG_GITHUB_CLIENT_SECRET` | 〃 secret |
| `BLOG_ADMIN_LOGINS` | 허용할 GitHub login (쉼표로 여럿) |
| `BLOG_SESSION_KEY` | 세션 서명 키. 없으면 무작위. **32바이트 이상** |

**애매하면 안 뜬다** (`cmd/blog`의 `adminAuth`). 인증이 반쯤 설정된 채로 서버가
뜨는 것이 여기서 제일 나쁜 결과라, 모자라면 `log.Fatal`이다.

- `-admin`인데 설정이 모자라면 → **안 뜬다.** 무엇이 없는지 이름으로 알려준다.
- 인증 없이 여는 길은 **`-admin-no-auth` 하나뿐이고 loopback에서만 된다.**
  `-addr`이 밖에서 닿는 주소면 안 뜬다. **`:8080`은 loopback이 아니다** —
  모든 인터페이스에 붙는다는 뜻이라 여기서 틀리면 그대로 공개된다.
- 설정과 `-admin-no-auth`를 **같이 주면 거절한다.** 사람이 무엇을 원하는지 알 수
  없는 상태고, 조용히 인증을 끄는 쪽으로 고르면 그게 사고다.
- 이름을 길게 지은 것도 일부러다. **위험한 것은 이름이 위험해야 한다.**

**테스트는 가짜 GitHub을 `httptest`로 세워 네트워크 없이 전체 흐름을 돈다.**
진짜 GitHub을 부르면 남의 서비스에 매달리고, 무엇보다 "거절당하는 계정" 같은
경우를 만들 수가 없다. 돌연변이 셋을 넣어 테스트가 **실제로 실패하는 것**을
확인했다: `allows()`가 늘 통과, `guard`를 뺌, `":8080"`을 loopback으로 침.

### 3단계로 만든 것 — 저장 (2026-08-31)

**글이 실제로 DB에 들어간다.** `internal/admin/save.go`·`upload.go`·`csrf.go`.

#### 새 글에는 `notion_page_id`를 넣지 않는다

이것이 3단계에서 **가장 중요한 한 줄**이다. `source`는 `native`이고
`notion_page_id`는 NULL이다. `deploy/upload-guard.sql`이 그 NULL 하나로
"이관이 아니라 사람이 여기서 쓴 글"을 알아본다 — 노션 id를 채우면 그 글은
가드에 안 걸리고 다음 `upload-db.sh`에 조용히 사라진다.
`TestCreateMarksThePostAsNative`가 지키고, 실제로 웹에서 쓴 글 3편과
올린 이미지 2개가 가드에 `LOST`/`LOSTIMG`로 잡히는 것을 확인했다.

#### 덮어쓰기를 막는 표 (`rev`)

**두 탭에서 같은 글을 열어 각각 저장하면 나중 것이 앞의 것을 통째로 지운다.**
그게 조용히 일어나는 것이 문제다. 그래서 불러올 때 준 `updated_at` 글자를
저장할 때 그대로 돌려받아 DB의 지금 값과 견준다. 다르면 409다.

- **`rev`가 비어 있어도 거절한다.** 검사를 건너뛰는 쪽으로 틀리지 않는다.
- **`time.Time`이 아니라 글자 그대로 주고받는다.** JSON 왕복에서 정밀도가
  깎이면 멀쩡한 저장이 거절당한다.
- **저장 응답에 새 `rev`를 실어 보낸다.** 안 주면 두 번째 저장이 늘
  "그새 바뀌었다"로 거절당한다.

#### PUT은 부분 갱신이 아니라 통째로 바꾸기다

안 보낸 칸은 "그대로 두라"가 아니라 "비우라"로 읽는다. 부분 갱신으로 두면
"이 칸을 지우고 싶다"와 "이 칸은 안 건드린다"를 JSON으로 구별할 수 없고,
그 모호함은 결국 사람이 지운 줄 안 값이 남는 쪽으로 끝난다.

**단 `published_at`은 요청에 아예 없다.** 사람이 적는 값이 아니라 status가
**처음** published가 되는 순간 서버가 찍는 값이다. 받아주면 두 곳에서 정해지고,
저장할 때마다 갱신하면 "언제 공개했나"가 "마지막으로 저장한 때"가 된다.

#### slug를 바꾸면 그 글을 가리키는 링크도 같이 바꾼다

CLAUDE.md의 "내부 링크" 절이 요구하던 것이다. relink가 모든 내부 링크를
`](/p/{slug})` 한 형태로 맞춰뒀으므로 닫는 괄호까지 붙여 정확히 짝지어
같은 트랜잭션에서 바꾼다. 안 하면 다른 글 몇 편이 **조용히 404를 가리킨다.**

- slug에는 `%`도 `_`도 들어갈 수 없어서(Slugify가 걸러낸다) LIKE가 안전하다.
- `PUT /api/admin/posts/{slug}`의 `{slug}`는 **지금의** slug이고 본문의
  `slug`는 **바꾸고 싶은** slug다. 둘이 다른 경우가 곧 이름 바꾸기다.

#### slug 규칙은 카테고리와 같은 함수가 정한다

`importer.Slugify(s) == s`면 쓸 수 있는 형태다. 규칙을 admin에 다시 적으면
카테고리 slug와 언젠가 갈라진다.

**그래서 한글이 그대로 남는다.** 스키마 주석은 "영문 소문자 + 하이픈"이라고
적혀 있지만 **카테고리 slug는 이미 한글을 쓰고 그 주소가 배포되어 있다**
(`/data-math/수리-통계-이론/선형대수`). 글만 로마자로 강제하면 같은 사이트
안에서 주소 규칙이 두 벌이 되고, 한국어 제목에서 slug를 만들 길이 없어진다.
`제목을 비우면 제목에서 만든다` — `"웹에서 쓴 글"` → `웹에서-쓴-글`.

#### 부모 사슬이 도는 것은 여기서 막는다

**`posts.parent_id`에는 `categories`와 달리 깊이·순환을 막는 트리거가 없다**
(002는 categories만 지킨다). 도는 사슬을 한 번 만들면 `nestPosts`와
`PostAncestors`가 각자 방어해야 하고 `cmd/postparent`도 같은 검사를 자기
쪽에서 한다. 저장할 때 막는 것이 가장 싸다.

부모는 id가 아니라 **slug로 주고받는다.** 사람이 화면에서 알아볼 수 있는
값이어야 잘못 붙였을 때 눈에 띈다.

#### 이미지: 멱등 키는 파일 이름이 아니라 내용의 sha256

`cmd/import`가 445개를 넣을 때 쓴 규칙과 같다. 같은 그림을 두 번 올려도 행이
늘지 않고, 이름을 바꿔 올려도 새 그림이 되지 않는다. 있는 행은 덮지 않는다 —
`caption`·`original_url`처럼 이관이 채워둔 칸을 지울 이유가 없다.

- **SVG는 안 받는다.** `/img/{sha256}`이 `images.mime`을 그대로 Content-Type에
  실어 보내는데, `image/svg+xml`은 브라우저가 문서로 열고 그 안의 `<script>`가
  **우리 도메인에서** 실행된다. 그림 하나 올리는 일이 사이트 전체에 대한 XSS가
  된다. png·jpeg·gif·webp만이다.
- **형식은 내용을 보고 정한다**(`http.DetectContentType`). 브라우저가 보낸
  Content-Type도 확장자도 올리는 쪽이 마음대로 적는 값이라, 믿고 `images.mime`에
  넣으면 그 값이 그대로 응답 헤더가 된다. HTML에 `.png` 이름표만 붙인 것이
  실제로 걸러지는 것을 테스트가 확인한다.
- **크기는 아는 만큼만 적는다.** webp는 표준 라이브러리가 못 읽어서 NULL이다.
  없는 값을 0으로 적으면 "가로 0픽셀"이 되어 뒤에 읽는 쪽을 속인다.
- **본문에 꽂을 마크다운은 서버가 만들어 보낸다.** 화면과 서버 두 곳에 규칙을
  두면 언젠가 갈라진다. alt는 비운다 — 파일 이름을 넣으면 `IMG_4821.png`가
  화면 낭독기에 읽힌다.
- **커서 자리에 끼운다.** 쓰던 자리에서 올렸는데 글 맨 끝에 생기면 다시 옮겨야 한다.
- **여기서만 읽기 마감을 미룬다**(`http.NewResponseController`). 서버 전체의
  `ReadTimeout`은 15초인데 8MB 업로드에는 초당 546KB가 필요하다. 서버 값을
  늘리면 **공개 GET의 방어까지** 헐거워지므로 이 요청 하나만 2분으로 늘린다.
  헤더 단계(`ReadHeaderTimeout` 5초)는 그대로라 느린 연결 공격은 거기서 막힌다.

#### 쓰기에는 관문이 하나 더 있다 (`csrf.go`)

세션 쿠키가 `SameSite=Lax`라 남의 페이지에서 보낸 POST에는 안 붙는다. 그래서
지금도 실제로는 막힌다 — **다만 그건 브라우저가 막아주는 것이다.** 서버가
자기 눈으로도 봐야 한다.

`Origin` 헤더 하나를 본다. 브라우저는 POST·PUT·DELETE에 반드시 붙이고 그 값은
스크립트가 바꿀 수 없다.

| | |
|---|---|
| Origin이 있고 우리 것 | 통과 |
| Origin이 있고 남의 것 | 403 |
| **Origin이 없음** | **403** |

- **없으면 막는다.** 통과시키면 검사 전체가 "헤더를 빼면 그만"이 된다.
  스크립트로 부를 일이 생기면 그때는 세션이 아니라 토큰을 쓸 자리다.
- **GET/HEAD는 안 본다.** 상태를 안 바꾸고, 남의 페이지가 응답을 읽는 것은
  CORS가 막는다. 여기서 GET까지 막으면 로그인 화면조차 못 연다.
- **관문(`guard`)보다 안쪽이다.** 로그인하지 않은 요청의 진짜 이유는 "출처가
  다르다"가 아니라 "로그인이 필요하다"이고, CSRF는 살아 있는 세션을 지키는
  장치라 세션이 있을 때 의미가 있다. 순서를 바꾸면 401이 403이 된다.
- 거절 사유에 **남이 보낸 값을 되찍지 않는다.** 거절당한 GitHub 계정 이름을
  화면에 안 찍는 것과 같은 이유다.

#### 확인한 것

**돌연변이를 넣어 테스트가 실제로 실패하는지 봤다.** 통과만 하는 테스트는
테스트가 아니다.

| 돌연변이 | |
|---|---|
| `notion_page_id`를 채움 | 잡힘 |
| `rev` 검사를 건너뜀 | 잡힘 |
| slug를 바꿔도 링크를 안 고침 | 잡힘 |
| 순환 검사를 통과시킴 | 잡힘 |
| SVG를 받아들임 | 잡힘 |
| CSRF 검사를 무력화 / 다른 출처도 통과 | 잡힘 |
| 롤백 대신 커밋 | 잡힘 |

- **롤백을 가르는 테스트는 따로 만들어야 했다.** 첫 UPDATE에서 걸리는 경우는
  롤백이 있으나 없으나 결과가 같아서 트랜잭션을 확인하지 못한다.
  `TestSaveRollsBackWhatItAlreadyWrote`는 **UPDATE가 성공한 뒤** 링크 재작성이
  트리거로 터지게 만들어 둘을 가른다.
- "롤백을 아예 안 함"은 안 잡히는데 **구멍이 아니다.** 커밋을 안 하면 SQLite가
  데이터를 안 남긴다 — 롤백은 연결을 제때 돌려주려고 있는 것이다.
- 브라우저(헤드리스)로 화면을 직접 몰아 확인했다: 새 글 작성 → 저장 → 주소 이동,
  분류 선택지 88개, 이미지를 커서 자리에 꽂기, 미리보기의 코드 라벨과 복사 버튼,
  노션 글의 경고 띠, **두 탭 충돌 시 화면이 실패를 말하는 것**.
- 실물 왕복도 봤다: 올린 PNG가 `/img/{sha}`에서 **바이트까지 동일**,
  저장한 글이 공개 화면에서 200이고 한글 앵커(`<h2 id="머리">`)와 코드 라벨이
  그대로 나온다.

### 글 지우기 (2026-08-31)

**이 프로젝트는 원래 글을 지우지 않는다.** 공개 여부는 삭제가 아니라 status로
가리고(`draft`), 노션에서 온 글을 빼는 것은 `curation.DropPosts`에 적는 일이다.
그 원칙은 그대로다.

여기가 필요한 이유는 **웹에서 잘못 만든 글**이다. 저장이 붙은 뒤로 오타 하나로
생긴 빈 글이 남을 수 있는데, 치우는 길이 DB를 직접 여는 것뿐이면
"DB를 손으로 고치지 마라"는 원칙과 정면으로 부딪힌다.

**그래서 지우는 것보다 "말해주는 것"이 이 기능의 일이다** (`delete.go`).
누르기 전에 `GET /api/admin/posts/{slug}/refs`가 무엇이 걸리는지 먼저 알려준다.

| 무엇 | 어떻게 |
|---|---|
| 자식이 있다 | **아예 막는다.** force로도 안 된다 |
| 분류의 표지다 | 알려주고, 확인하면 표지를 잃는다(`ON DELETE SET NULL`) |
| 노션에서 왔다 | 알려준다 — **다음 `import -db`가 되살린다** |
| 가리키는 링크가 있다 | 같은 트랜잭션에서 **글자로 푼다** |

- **링크를 반드시 풀어야 한다.** 안 풀면 그 링크의 대상이 "posts에 없는 slug"가
  되는데, `resolveBody`는 그걸 **노션 인라인 데이터베이스로 판정한다**
  (위 "공개 서버"의 `②`). 죽은 링크가 남는 데서 그치지 않고 **엉뚱한 목록이
  펼쳐질 수 있다.** 링크를 통째로 지우지 않고 글자만 남기는 것은 공개 쪽
  `unlinkHidden`과 같다 — 줄째 지우면 문장이 끊긴다.
- **SQLite에는 정규식이 없어서** 푸는 일은 Go에서 한다. 한 글을 가리키는 링크는
  많아야 몇 편이라 통째로 읽어도 된다. **정규식이 못 잡은 것이 남으면 멈춘다** —
  중첩 대괄호 같은 모양을 조용히 넘기면 죽은 링크가 남는다.
- 저장과 같은 `rev` 표를 쓴다. 목록에서 본 그 글이 맞는지 확인하지 않으면
  다른 탭이 고친 글을 모르고 지운다.
- **잃을 것이 없으면 확인을 두 번 묻지 않는다.** 잘못 만든 빈 글을 치우는 것이
  본래 쓰임이라, 거기에 확인 창을 겹쳐두면 매번 무심코 누르게 된다.
- 돌연변이 5종(자식 있어도 지움 / 확인 없이 지움 / 링크 안 풂 / 링크를 글자
  대신 통째로 지움 / rev 건너뜀)을 넣어 테스트가 실제로 실패하는 것을 확인했다.

### `/` 조각 팔레트 (2026-08-31)

본문 칸에서 `/`를 치면 조각 목록이 뜬다(`internal/web/static/palette.js`).
고르면 **마크다운 조각이 커서 자리에 꽂힌다** — WYSIWYG가 아니라 지금처럼
왼쪽 편집·오른쪽 미리보기다. 정본이 계속 마크다운인 이유는 "정해둔 것 ①"에 있다.

- **서버에 묻지 않는다.** 목록이 그 파일 안에 다 있고 거르는 것은 부분 일치
  하나다. `/`를 친 순간과 목록이 뜨는 순간 사이에 네트워크가 끼면 그 지연은
  절대 감출 수 없다 — 미리보기(250ms 디바운스)와 정반대의 요구다.
  실측 **8.2ms**, 네트워크 요청 0건이다.
- **줄 처음이나 공백 뒤의 `/`만 본다.** 안 그러면 `https://`나 경로를 칠 때마다
  팔레트가 튀어나온다.
- 캐럿 좌표는 **거울 div**로 잰다. 텍스트에어리어에는 캐럿 위치를 알려주는
  API가 없어서 같은 글꼴·같은 폭의 숨은 div에 커서 앞까지를 복사하고 그 끝을
  잰다. 아래로 넘치면 커서 위로 올린다.
- 항목은 20개다: 제목·목록·인용·구분선, 수식(인라인/블록), 코드 6가지,
  표, 애니메이션 2가지, 접기, 링크, **이미지 올리기**.
  - **"이미지 올리기"만 조각을 꽂지 않는다.** 파일 고르는 창을 열고, 올린 뒤에
    서버가 준 마크다운이 커서 자리에 들어간다.
  - 검색어는 한국어와 영어를 함께 단다(`/수식`도 `/math`도 `/katex`도 걸린다).
    **초성 검색 같은 것은 안 한다** — 목록이 스무 줄이라 그럴 값이 없고,
    규칙이 늘면 예측이 어려워진다.
- **`internal/web/static`에 둔다.** admin 편집기와 공개 화면의 바로 고치기가
  같은 팔레트를 쓴다 — 두 벌로 두면 반드시 갈라진다.

### 이름 붙인 애니메이션 (2026-08-31)

본문에 `:::anim sort-bubble` 한 줄을 쓰면 그 자리가 애니메이션이 된다
(`internal/markdown/anim.go` + `internal/web/static/anim.js`).

**"정해둔 것 ④"를 그대로 구현한 것이다.** 렌더러가 `html.WithUnsafe()`라
본문에 `<script>`를 쓰면 **이미 실행된다.** 그래서 "되냐"가 아니라 "그래도
되냐"가 문제다. 본문에 임의 JS를 담기 시작하면 그 글 자체가 XSS 벡터가 되고 —
4단계에서 AI가 본문을 만들기 시작하면 특히 그렇다 — 나중에 CSP를 걸 길도 막힌다.

- **본문에는 이름만 들어간다.** 실제 JS는 `anim.js`에 사람이 쓴 파일로 있고,
  새 애니메이션을 더할 때 손대는 곳은 `COMPONENTS` 한 줄과 그 함수 하나다.
  지금 있는 것은 `sort-bubble`(버블 정렬)과 `binary-search`(이진 탐색)다.
- **이름 형태를 좁혀뒀다**(`^[a-z][a-z0-9-]{0,40}$`). HTML 속성값으로 그대로
  나가는 값이다. **안 맞으면 지시로 안 받아준다** — 그 줄이 평범한 문단으로
  남아 글자 그대로 보인다. 조용히 사라지는 것보다 낫다.
- **한 줄짜리다.** 닫는 `:::`을 요구하지 않는다. 지금 넣을 것이 이름뿐이라
  여는 줄과 닫는 줄 사이가 늘 비고, 빈 껍데기를 요구하면 자꾸 빠뜨린다.
- **`PeekLine`은 개행을 붙여 준다.** Go의 `$`는 (멀티라인이 아니면) 마지막
  개행 **앞**에서 맞지 않는다 — Perl과 다른 점이고 여기서 실제로 한 번 걸렸다.
  줄 끝을 떼고 견준다.
- 스크립트가 없으면 **빈 자리를 남기지 않는다.** 서버가 "애니메이션 {이름} —
  스크립트가 있어야 움직인다"를 적어두고 anim.js가 그 자리를 대신 채운다.
  **모르는 이름도 조용히 넘기지 않는다** — "그런 애니메이션이 없다: nope".
- **계산과 그리기를 나눈다.** 걸음을 미리 다 만들어 두고 하나씩 보여줘서
  "한 걸음씩"과 "자동 재생"이 같은 목록을 쓰고 되감기도 공짜다.
- **`joint-probability`는 죽은 apption.co 임베드를 대신한다** (2026-09-02).
  `1. 확률`의 조건부확률 절은 "노란색 부분을 결합확률, 초록색 부분을 주변확률"
  이라고 설명하는데 정작 볼 그림이 없었다 — 그 자리의 임베드가 이미 죽어 있었다.
  - **눈높이 둘을 버튼으로 오간다.** 기본은 위에서 내려다보기(표처럼 읽힌다)고,
    누르면 오른쪽 위에서 비스듬히 본 3D 막대가 된다(높이가 곧 확률이다).
  - **등각투상을 직접 계산한다.** 막대 하나가 윗면·왼면·오른면 세 다각형이고,
    `i+j`가 작은 것부터 그려 앞 막대가 뒤를 가린다(화가 알고리즘).
  - **노란색은 결합확률, 초록색은 주변확률이다.** 본문이 바로 아래에서 그
    두 색을 **글자로 부르기 때문**에, 이 그림만은 색을 둘 쓴다. 처음에는
    포인트 한 색으로 칠했는데 그러면 무엇이 결합이고 무엇이 주변인지 구별이
    안 되고 그 두 문장이 그림과 어긋난다.
  - **칸마다 흰 바탕을 먼저 깐다.** 진하기로 크기를 나타내는데 그냥 투명하게
    두면 다크에서 옅은 칸이 검게 가라앉아 그 위의 검정 숫자가 안 보인다.
    노랑·초록은 검정 글자를 얹는 지면이라 어느 테마에서든 밝은 바탕 위에
    있어야 한다.
  - **3D 면은 불투명한 세 단계다.** `fill-opacity`로 명암을 만들면 바닥
    격자와 뒤 막대가 비쳐 입체가 아니라 유리 덩어리로 보인다.
  - **결합 막대와 주변 막대를 한 목록에 담아 함께 정렬한다.** 노란 막대를
    다 그린 뒤 초록 막대를 그리면, 뒤에 있어야 할 초록이 앞의 노랑을 덮는다 —
    깊이는 종류가 아니라 자리가 정한다.
  - **같은 대각선(`i-j`가 같은) 칸은 화면에서 x가 똑같다.** 그래서 그 칸들의
    숫자 라벨은 세로로만 갈리는데, 높이 차이가 깊이 차이를 지우면 겹친다 —
    실제로 두 라벨이 14px까지 붙었다. 깊이 간격을 키우고 높이 배율을 줄여
    최소 간격을 32px로 벌렸고, **라벨은 막대를 다 그린 뒤 한 번 더 돌아서**
    앞 막대가 뒤 라벨을 덮지 않게 했다.
  - 칸을 누르면 그 칸의 행과 열만 남고 나머지가 물러난다 — 조건부확률이
    "그 줄만 떼어 다시 1로 만든 것"이라는 게 눈에 보여야 한다.
  - SVG `viewBox`라 폭에 맞춰 저절로 줄어든다. 모바일·태블릿·PC를 따로 손보지
    않았고, 넘치면 페이지가 아니라 그 상자가 가로로 스크롤한다.
- `prefers-reduced-motion: reduce`면 **저절로 움직이지 않는다.** 버튼은
  그대로라 보고 싶은 사람은 한 걸음씩 볼 수 있다.
- `Assets.Anim`이 있는 페이지에만 `anim.js`를 받는다.

### 글 화면에서 바로 고치기 (2026-08-31)

로그인이 확인되면 글 제목 옆에 "고치기"가 나오고, 누르면 **본문이 있던 자리가
편집기가 된다.** `/admin`으로 옮겨 가지 않으므로 읽던 맥락을 잃지 않는다
(`internal/web/static/inline-edit.js`).

**web과 admin의 경계를 지키는 방법이 이 기능의 핵심이다.**

- web은 "읽기 전용 공개 페이지"고 admin은 따로 있다. 세션이 무엇인지 web이
  알게 되면 그 경계가 무너지므로, **"지금 누가 들어와 있나"라는 질문 하나만**
  함수로 받는다(`web.WithEditor` ← `admin.Server.LoginFor`). 쿠키도 서명 키도
  web은 모른다.
- **기본값은 "닫힘"이다.** 옵션을 안 주면 글 화면은 예전 그대로다 — 버튼도
  스크립트도 나가지 않는다. `cmd/blog`가 `-admin`을 켤 때만 건넨다.
  그래서 **admin을 web보다 먼저 만든다.**
- **요청마다 다시 묻는다.** 한 번 로그인한 것을 서버가 기억해두면 세션이 풀린
  뒤에도 화면이 열린 채로 남는다. `render` 한 곳에서 채운다 — 사이드바와 자산
  판정을 거기서 채우는 것과 같은 이유다.
- **화면이 권한을 판단하지 않는다.** 버튼을 지운다고 아무것도 못 하게 되지
  않고, 억지로 눌러봐야 API가 401을 준다. 진짜 관문은 언제나 서버다.
- **draft를 가리는 규칙은 그대로다.** 로그인은 "고칠 수 있다"는 뜻이지
  "공개 화면의 규칙이 달라진다"는 뜻이 아니다. 그 둘이 섞이면 어느 화면이
  무엇을 보여주는지 아무도 모르게 된다.
- 뒷단은 admin과 **같은 API**다(`/api/admin/posts/{slug}`, `/api/admin/preview`).
  편집기를 두 벌 만들면 "여기서 본 것과 발행 뒤 화면이 같다"는 보장이 두 배로
  깨지기 쉬워진다. 팔레트도 같은 파일을 쓴다.
- **메타는 여기서 안 건드린다.** PUT은 통째로 바꾸기라 안 보낸 칸이 비워지므로
  지금 값을 그대로 되돌려 보낸다. 분류나 계층을 옮기는 것은 "자세히 ↗"로 가서
  할 일이다.
- **저장 뒤에는 다시 그리지 않고 새로 받아온다.** 실제 글 화면은 렌더링 직전에
  죽은 링크를 손보는데(`resolveBody`) 미리보기는 그걸 안 한다 — 저장 뒤에
  그 차이가 남으면 안 된다.
- 취소하면 **원래 화면을 통째로 되돌린다.** 다시 그리면 수식·코드 색칠·복사
  버튼·애니메이션을 전부 다시 붙여야 하는데, 그 목록은 언젠가 하나 빠진다.
- 좁은 화면(60rem 미만)에서는 편집과 미리보기가 위아래로 쌓인다. 375px에서
  두 열은 어느 쪽도 못 읽는다. 실측으로 문서폭이 375를 안 넘는 것을 확인했다.
- 돌연변이 3종(옵션 없어도 실어보냄 / 로그인 안 해도 버튼을 그림 / 요청마다
  안 묻고 한 번만 물음)을 넣어 테스트가 실제로 실패하는 것을 확인했다.

### 배포에서 admin 켜기 (2026-08-30) — **켜져 있다**

`https://inquieto.dev/admin`이 **지금 살아 있다.** 허용 목록의 GitHub 계정으로
로그인해야 들어간다. 저장은 아직 501이다(3단계).

**HTTPS가 붙었다**(위 "배포 > HTTPS"). `secure()`가 Caddy의
`X-Forwarded-Proto`를 보고 세션 쿠키에 `Secure`를 자동으로 켠다 — 조건부로
둔 것이 여기서 값을 한다. 실물에서 갈리는 것을 확인했다:

| 경로 | `Set-Cookie` |
|---|---|
| Caddy 거쳐 HTTPS | `HttpOnly; **Secure**; SameSite=Lax` |
| loopback 평문 8080 | `HttpOnly; SameSite=Lax` |

밖에서 확인한 관문: `/admin` 303 → `/admin/login`, `/admin/login` 200,
`/api/admin/posts` GET·POST 모두 **401 JSON**(HTML 로그인 화면이 아니다),
`/api/admin/images` POST 401, `/admin/auth/start`가 `scope=`를 비운 채
GitHub으로 보내고 state 쿠키를 심는다.

**켜는 스위치는 인스턴스의 `/etc/blog/admin.env` 하나뿐이다.**

```
BLOG_GITHUB_CLIENT_ID=...
BLOG_GITHUB_CLIENT_SECRET=...
BLOG_ADMIN_LOGINS=InryeolChoi
BLOG_SESSION_KEY=...        # setup-caddy.sh가 만든다
BLOG_ADMIN_FLAG=-admin      # 이 줄이 곧 스위치
```

- **`deploy/blog.service`에는 `-admin`이 없다.** 유닛은 `$BLOG_ADMIN_FLAG`를
  넘길 뿐이라, 설정이 갖춰진 기계에서만 켜진다. 유닛에 박으면 설정이 없는
  기계에서 **admin이 아니라 사이트가 죽는다**(`adminAuth`는 애매하면 안 뜬다).
- **client secret은 저장소에도 GitHub Secret에도 없다.** `deploy/enable-admin.sh`가
  인스턴스에서 물어보고, 입력은 화면에 안 찍히며, 곧장 0600 root:root 파일로
  들어간다. **인자로도 환경변수로도 받지 않는다** — 인자는 `ps`에 보이고 셸
  히스토리에 남는다.
- **세션 키는 `setup-caddy.sh`가 인스턴스에서 만든다.** 사람 손도 저장소도
  거치지 않는다. 바꾸면 살아 있는 로그인이 전부 풀리므로 있으면 덮지 않는다.
- OAuth 앱의 callback은 **정확히** `https://inquieto.dev/admin/auth/callback`이어야
  한다. 서버가 `redirect_uri`를 만들어 보내지 않고 등록된 것을 쓴다.
- 끄는 것은 그 한 줄을 주석 처리하고 `systemctl restart blog`다. 사이트는 그대로
  돈다.

**3단계(저장) 전에 남은 것은 DB 정본 방향이다.** 이 절 맨 위의 블록인용 참고.

### 로컬에서 보기

```sh
# **진짜 blog.db로 열지 마라.** 이제 정말로 쓴다 — 사본으로 연습한다.
cp blog.db /tmp/admin.db

# 화면만 볼 때 (로그인 없이, loopback 전용)
go run ./cmd/blog -db /tmp/admin.db -addr 127.0.0.1:8080 -admin -admin-no-auth -drafts

# 로그인까지 확인할 때 — GitHub에 OAuth 앱을 하나 만들고
# callback을 http://127.0.0.1:8080/admin/auth/callback 으로 등록한다.
BLOG_GITHUB_CLIENT_ID=... BLOG_GITHUB_CLIENT_SECRET=... BLOG_ADMIN_LOGINS=InryeolChoi \
  go run ./cmd/blog -db /tmp/admin.db -addr 127.0.0.1:8080 -admin -drafts
# http://127.0.0.1:8080/admin
```

- `-drafts`를 같이 주면 admin에서 draft를 누른 뒤 `공개 화면에서 보기`가 200이 된다.
  안 주면 그 링크가 404다(공개 규칙대로다).
- 화면 오른쪽 미리보기는 입력이 멈추고 250ms 뒤에 한 번 갱신된다.
- **secret을 셸에 그대로 치면 히스토리에 남는다.** 자주 할 것이면 `.env`처럼
  gitignore된 파일에 두고 `set -a; . ./.env.admin; set +a`로 읽는다.

### 4단계로 넘어갈 때 볼 것

3단계에서 미뤄뒀던 것은 전부 처리했다: HTTPS(2026-08-30), DB 정본 방향과
백업(2026-08-31), 업로드 타임아웃(요청 하나만 미룬다), CSRF(`Origin` 확인).

- **본문 렌더러가 `html.WithUnsafe()`다.** 4단계에서 **AI가 본문을 만들기
  시작하면** 그 글 자체가 XSS 벡터가 된다. 위 "정해둔 것 ④" 참고 — 본문에는
  `:::anim sort-bubble` 같은 이름만 들어가고 실제 JS는
  `internal/web/static/`에 사람이 쓴 파일로 둔다.
- **MCP로 admin API를 노출할 때 세션 쿠키를 쓰지 않는다.** `sameOrigin`이
  Origin 없는 요청을 막으므로 스크립트 클라이언트는 애초에 못 들어온다.
  거기가 곧 **토큰을 쓸 자리**다 — 쿠키 검사를 느슨하게 푸는 쪽으로 가면
  브라우저에서 오는 진짜 요청의 방어까지 같이 헐거워진다.
- **`/` 팔레트는 3단계 뒤에 만든다고 정해뒀다**(위 "정해둔 것 ②"). 저장이
  되므로 이제 만들 수 있다.
- **✅ 글 지우기는 2026-08-31에 붙였다.** 아래 "글 지우기" 참고.
- **아직 없는 것: 이미지 지우기.** 글을 지워도 그 글만 쓰던 그림은 BLOG에
  남는다. 어느 그림을 아무도 안 쓰는지 세는 일은 본문 전체를 훑어야 해서,
  청소 도구를 따로 만들 자리다.

## 진행 상황 (2026-09-01 기준)

### 끝난 것

- **카테고리 3단계 재구성** — 현재 DB는 8개 > 18개 > 62개다.
  `categorize`(경로에서 뽑기) + `regroup`(사람이 얹는 층)이 층을 나눠 맡는다.
- **깊이 트리거 재작성** (002). 삽입·이동 양방향을 보고 서브트리 높이까지 계산한다.
- **`cover_post_id`** (004). 현재 표지가 있는 카테고리는 총 45개다.
  공개 서버는 표지가 있으면 그 본문을 카테고리 페이지에 펼친다.
- **`parent_id`** — 5개 카테고리 172건. 수리통계1 46, 수리통계2 43,
  탐색적 자료분석 22, 확률과정론 19, 빅데이터 분석기사 42다. `cmd/postparent`가 채웠다.
- **`internal/curation`** — 사람이 정한 분류 예외를 categorize와 regroup이 공유한다.
- **소개/프로젝트 정리** — `/intro`가 자기소개를 바로 펼치고, 노션의 "프로젝트"
  하위 분류는 없앴다(글 6건은 `/project` 직속으로).
- **status 뱃지는 draft만** 찍는다.

- **인라인 데이터베이스 목차 펼치기** — 죽은 링크 119개 중 100개를 렌더링
  단계에서 목록으로 펼친다(글 763건). `link_to_page` 자리표시자 14개도 진짜 제목이
  된다. DB는 안 건드렸다. 위 "공개 서버" 절 참고.

- **글 목록 정렬** — 제목 앞 번호를 먼저 따르고, 목록에 원본 작성일을 찍는다.
- **글 목차** — 본문의 h1~h3로 자동 생성. 앵커는 한글을 살린다.
- **수식 렌더링** — KaTeX를 브라우저에서 돌린다. 2898개 **전부** 그려진다.
- **블록 수식 파서** — `$$`가 뒤의 제목·코드를 삼키던 것을 고쳤다(64자리).
- **코드 하이라이팅** — highlight.js. 코드 블록 2578개 중 2282개를 색칠한다.
- **읽기용 기본 스타일 + 화면 설정** — 여백·타이포·대비, 시스템/라이트/다크 팔레트,
  한국어/영어/스페인어 UI 전환을 붙였다.
- **모바일 폭(375px) 정리** — 가로 스크롤 0개. 넘치는 것은 제 안에서만 스크롤한다.
- **하위 분류 중복 제거** — 표지 글 본문이 이미 통째로 펼친 분류는 목록에서 뺀다.

**2026-08-19에 한 것** (위 "레이아웃과 색" 절 참고)

- **왼쪽 고정 사이드바** — 카테고리를 한 번의 조회로 그린다. 최상위 8개는
  펼쳐두고 아래는 아코디언, 현재 위치까지의 길만 서버가 펼쳐 보낸다.
  좁은 화면(60rem 미만)에서는 햄버거로 여는 서랍이 된다.
- **당시에는 홈에 소개 글을 펼쳤다.** 2026-08-22에 홈과 소개가 중복된다는 판단으로
  아카이브 탐색 허브로 다시 분리했다.
- **색을 입혔다** — "썰물" 팔레트. 흰 바탕 + 모래빛 표면 + 하늘색 레일.
  라이트 하나만 했다.
- **코드 블록 언어 라벨** — 서버가 붙인다. `text` 265건은 뺀다.
- **본문 바깥 링크 카드** — 제 문단을 통째로 차지한 외부 링크 71개(글 48편)가
  카드가 된다. 문장 속 링크 46개는 글자 그대로다.
- **사이드바 하단에 GitHub / GitHub Pages 링크.**
- **소개 글 끝의 `[프로젝트]` 링크를 지웠다.** `curation.BodyEdits`에 적어서
  `cmd/import`가 변환 때 덜어낸다. 재이관해도 되살아나지 않는다.

**2026-08-21에 한 것**

- **본문과 "하위 글"이 겹치던 것을 없앴다** — 6편 46개 링크. 본문이 이미
  가리키는 글은 목록에서 뺀다.
- **문단 자식 들여쓰기 때문에 코드 블록이 되던 것을 고쳤다** — 5개 블록(전부
  코드가 아니었다) → 0개. `1. 확률`의 수식이 살아났다.
- **인라인 수식 옆 회색 알약을 없앴다** — 스크롤바 두 개가 서로를 불러내던 것.
  수식 1528개 중 730개(글 113편) → 0개. 375px에서도 문서폭 360px로 안 밀린다.
  `$` 짝 버그(299e48a)와는 무관하고 좁은 화면 대응(99cbc1e) 쪽에서 온 것이다.
- **빈 토글 35개를 없애고 `<summary>` 안 마크다운 노출 75개를 고쳤다** —
  변환기를 고쳐 1408건 재변환 + relink 재실행. `<details>` 428 → 393,
  글 80편 → 62편. 그중 2개는 뒤이어 **문단으로 되살렸다**(summary가 문장이었다).
- **본문 제목을 한 단계 내렸다** — 페이지에 `<h1>`이 하나만 남는다. 1502페이지 확인.
- **CDN 자산을 페이지별로 받는다** — KaTeX 1502 → 215, highlight.js 1502 → 598.
- **변환 유실 전수 감사** — 글자 조각 40,459개 중 못 찾은 것 32개가 전부 의도한
  것이었다. 조용히 새는 곳은 없다.
- **선형대수 참고 동영상을 글 목록 아래로 옮겼다.** 상세 글은 원래 순서를 유지하고,
  카테고리에서 펼친 표지 글만 소개 → 글 목록 → 참고 동영상 순서로 보인다.
- **프로젝트와 Language를 갈래 카드로 정리했다.** 프로젝트는 école 42 / where42 /
  심심조각 세 장이고, Language는 JS 없이 실제 언어 6장 + 마크업 1장을 보여준다.
- **표지/목차와 글 목록의 중복을 없앴다.** 펼친 표지 글은 목록에 다시 넣지 않고
  `표지` 뱃지를 없앴다. 표지 목차가 안내하는 글 트리도 아래 목록에서 제외한다.
- **`수리/통계: 응용` 순서를 정리했다.** 탐색적 자료분석 → 회귀분석 → 다변량분석.
  `빅데이터 분석기사`는 `/career`로 옮겨 `취업 준비` 다음 맨 아래에 뒀다.
  categorize → regroup을 두 바퀴 돌린 뒤 regroup 재실행이 0건인 것도 확인했다.
- **`개발`의 하위 순서를 정했다.** Language → 리눅스 & 쉘 → tooling →
  웹 프로그래밍 → 모바일 프로그래밍 순이다. 배정표에는 이름 변경 전의 안정적인
  `source_name`인 `소프트스킬`로 적고 화면에서는 rename 결과인 `tooling`으로 보인다.
- **우측 하단 화면 설정을 붙였다.** 닫힌 원과 열린 설정창은 `prefs-shell` 하나다.
  그 원 자체가 우하단을 기준으로 가로·세로로 늘어나 둥근 사각형이 되고, 바깥을 누르면
  같은 표면이 원으로 돌아온다. 한국어/English/Español UI와 시스템/화이트/다크를
  기기별로 기억한다. 다크는 청회색 바탕과 따뜻한 회백색 본문을 쓴 별도 팔레트다.

**2026-08-22에 한 것**

- **`컴퓨터 시스템`을 통째로 없앴다.** 표지 1편 + 시스템 프로그래밍 13편 +
  컴퓨터 구조의 빈 글 3편을 `DropPosts`에, 분류 세 개를 `DropCategories`에 기록했다.
  import → relink → categorize → regroup을 돌리고 두 번째 실행이 0건인 것을 확인했다.
- **핸즈온 머신러닝 2 끝을 정리했다.** `연습문제 2` 제목과 유일한 링크를 본문에서
  덜어내고 `교차검증과 과대적합` 글도 이관에서 뺐다. 카테고리 화면의
  `이 글만 따로 보기`와 `비어 있다.` 자리표시자도 없앴다.
- **자기소개 증명사진을 완전히 뺐다.** `BodyEdits`가 본문 참조를 지우고,
  `DropImages`가 sha256 이미지 BLOB을 기존 DB와 다음 이관 양쪽에서 제외한다.
  해당 `/img/{sha256}`가 404인 것도 확인했다.
- **Language를 언어별로 펼쳤다.** C/C++/Java/Python/R/TypeScript 표지 글로 바로
  들어가며, 내용 없는 Swift는 노출하지 않는다.
- **Chrome 138+ 데스크톱에서는 언어 선택이 렌더링된 본문까지 바꾼다.** 공통 UI
  사전뿐 아니라 제목·본문의 한국어 텍스트 노드를 온디바이스 번역한다. 코드·수식은
  건드리지 않는다. Python 표지 글을 English로 바꿔 article 안 한글이 0개가 되는 것을
  Chromium에서 확인했다. **Safari에는 웹에서 호출할 수 있는 Translator API가 없어서
  공통 UI만 바뀐다.** 전체 브라우저 지원은 아래 남은 일이다.
- **`수업 : 통수 & 선계` 16편을 주제에 맞게 갈랐다.** 선형대수 11편만
  `/선형대수`로 옮기고 `1. 벡터`부터 `11. 특이값 분해`까지 번호를 붙였다.
  원작성일은 2022-03-12~2022-07-30 사이에 순서대로 배정했다. 제목·날짜·순서는
  `curation.PostMetadataEdits`가 관리하므로 재이관과 sortorder 뒤에도 유지된다.
  선형계획법·심플렉스 알고리즘·쌍대정리: 선형·심플렉스와 n분위수·라그랑주 승수법은
  내용 있는 최적화 글이라 `/최적화이론`에 둔다.
- **최적화이론 표지에서 `[수업 : 통수 & 선계]` 묶음을 뺐다.** 그 링크는 원래 경로를
  기준으로 16편을 통째로 펼쳐서, 카테고리를 선형대수로 옮긴 뒤에도 최적화 화면에
  중복 노출시켰다. 글을 삭제한 것이 아니라 묶음 링크만 `BodyEdits`로 없앴다.
  별도 `수학적 최적화` draft 6편도 DB에 그대로 둔다. 동명 글은 두 벌이다. 2023년에
  만든 draft 쪽은 대부분 본문이 비었고, 2022년 `수업` 쪽 글에는 실제 본문이 있다.
  옛 수업 상자만 없애되 본문 있는 최적화 5편은 최적화이론의 일반 글로 보인다.
  import → relink →
  categorize → regroup과 sortorder를 적용하고 두 번째 실행 0건, 브라우저 렌더링까지
  확인했다. 확인용 서버는 종료했다.
- **확률과정론 `중간 & 기말`의 전 문항 해답을 작성했다.** 중간 4문항과 기말
  2문항(A/B/C 소문항 포함)을 `curation.BodyAppends`로 관리한다. 문제 이미지는 먼저
  보이고, `중간고사 해답 보기`와 `기말고사 해답 보기`를 눌렀을 때만 풀이와 답이
  펼쳐진다. DB 본문을 손으로 고친 것이 아니므로 전체 재이관 뒤에도 유지된다.
- **머신러닝: 기초이론 표지의 깨진 안내 링크를 정리했다.** `모르면 여기, 저기 클릭!`
  중 404가 나던 `여기, 저기`를 없애고 정상 링크를 건 `모르면 클릭!`만 남겼다.
- **école 42의 목차와 하위 분류 중복을 구조적으로 없앴다.** 부모 표지에 있던
  C·libft·ft_printf 등 입구 글 17편을 같은 이름의 하위 분류로 옮겨 그 분류의
  표지로 삼았다. 부모 표지의 글 링크는 렌더링할 때 해당 카테고리 URL로 바뀌고,
  이미 본문에서 안내한 하위 분류는 아래 목록에 다시 나오지 않는다. 하위 분류로
  들어가면 입구 본문과 세부 글을 한 화면에서 볼 수 있다. `categorize` 검증도
  regroup 담당 표지가 아직 대기 중인 상태를 허용하되 잘못 붙은 표지는 잡도록 고쳐,
  새 `Covers`를 추가한 직후에도 categorize → regroup 순서가 막히지 않는다.
- **빅데이터 분석기사의 표지 목차와 아래 `Posts` 중복을 없앴다.** 42편의
  `parent_id`를 복원해 `개념정리` 24편과 `실전문제` 8편은 각 입구 글 아래로,
  `jusohn’s page` 8편은 표지의 인라인 목록 아래로 묶었다. 표지 본문이 세 갈래를
  모두 안내하므로 카테고리 첫 화면의 별도 `Posts` 목록은 사라지고, 각 입구를
  누르면 그 안의 세부 글을 볼 수 있다. 두 번째 postparent 실행은 변경 0건이었다.
- **Home과 소개를 분리했다.** `/intro`만 자기소개 본문을 편다. (같은 날 홈은
  아카이브 허브에서 표제지 하나로 다시 줄었다 — 아래 항목과 "공개 서버" 절 참고.)
- **홈을 표제지 하나로 줄였다.** 어두운 그라데이션 히어로가 "썰물" 팔레트에서
  겉돌았다: `#07161D` 배경을 하드코딩해 토큰을 하나도 안 쓰고, 그라데이션 글자
  (`background-clip: text`), 떠다니는 노드 6개, 움직이는 3D 원근 격자, 오로라 블롭,
  `PERSONAL KNOWLEDGE ARCHIVE` 대문자 kicker, `source: DB` 카운터를 한꺼번에 썼다.
  다섯 가지 안(표제지·모래 판·색인·모눈·프롬프트)을 실제 팔레트로 만들어 보고
  표제지를 골랐다. 히어로 CSS가 6,185자 → 1,005자가 되고 `@keyframes` 3개가
  사라져서, 이제 홈에는 `prefers-reduced-motion`에서 멈출 것이 없다. 쓰는 색은
  `--ink`/`--ink-dim`/`--line`뿐이라 라이트·다크가 저절로 따라간다.
  홈 구조를 검증하던 테스트 3개도 새 의도로 고쳤다: `TestHomeIsTitlePage`가
  **주석이 화면으로 새지 않는지**와 **사이드바에 `/dev`가 남아 있는지**(지도를 지운
  근거 자체)를 보고, 하위 글 수 검증은 `TestSidebarListsTopCategoriesWithCounts`로
  옮겼다(`subtreePostCount`는 계속 테스트돼야 하는데 홈 카드가 사라졌다).
- **소개 화면의 되돌아오는 갈래를 없애고 개인 페이지로 나가는 길을 냈다.**
  `소개` 밑에는 노션 최상위 `최인렬 (Inryeol Choi)`가 카테고리로 남아 있었고,
  그 갈래의 글은 화면 위쪽에 이미 펼친 자기소개 한 편뿐이라 눌러도 제자리로
  돌아왔다. 글을 `PostMoves`로 `소개`에 직접 붙이고(표지 글이라 목록에서도
  빠진다) 빈 껍데기가 된 카테고리를 `DropCategories`에 적었다. regroup의 `intro`
  묶음도 members가 없어져, 사이드바에서도 그 층이 사라진다. 그 자리에는
  `internal/web/links.go`가 `https://inryeolchoi.github.io`로 가는 카드를 둔다.
  categorize → regroup을 두 바퀴 돌려 두 번째 실행 변경 0건을 확인했다.
- **갈래 카드 만드는 방법을 slug별 `deckSource`로 모았다.** Language와 프로젝트를
  위해 `handleCategory` 안에 있던 특례 두 덩어리가 `deckSources` 표 두 줄이 됐다.
  카드 수(개발 5, Language 7, 프로젝트 3, 데이터 & 수리 3, 알고리즘 2, CS 이론 4)가
  그대로인 것을 확인했다.

**2026-08-23에 한 것**

- **빈 시각화 글 여덟 편을 뺐다.** `R언어 : 시각화`의 다섯 편(ggplot, 고수준 함수,
  저수준 함수, 그래프 그리기, 그래프 꾸미기)은 전부 본문이 0바이트인 draft인데,
  같은 내용을 실제로 쓴 글이 한 층 위의 `기본 시각화1`·`기본 시각화2`·`그래프 꾸미기`에
  따로 있어서 목록에 두 벌로 보였다. `파이썬 : 데이터 시각화`의 `(제목 없음)` 세 건도
  같이 뺐다(같은 데이터베이스의 엑셀 글 네 건은 내용이 있어 남긴다). R 표지의
  `[R언어 : 시각화]` 묶음 링크는 행이 0이 되므로 `BodyEdits`로 함께 없앴다.
  글 1365 → **1357편**. import → relink → categorize → regroup을 돌리고 두 번째
  실행이 0건인 것을 확인했다.
- **`개발`을 `서버 & API` / `클라이언트 & UI`로 갈랐다.** `웹 프로그래밍` 77편이
  Django → Spring이라는 배운 순서와 곁다리(HTML/CSS·React·로그인·웹소켓)로 뒤엉켜
  있었다. 3단계 제한 때문에 그 밑에 한 층을 더 두는 길이 막혀 있어, 웹 프로그래밍
  자리를 두 갈래가 대신한다(그 분류는 `DropCategories`). 모바일 프로그래밍 5편은
  클라이언트 밑으로 내렸다 — 갈래 이름을 "프론트엔드"로 안 지은 이유다.
  입구 글 다섯은 같은 이름 하위 분류의 표지가 되고, 표지 글 한 편은 뺐다.
  카드는 개발 5장 그대로(웹·모바일 → 서버·클라이언트). 위 "카테고리" 절 참고.
- **새 층을 처음 얹을 때 생기던 categorize ↔ regroup 교착을 풀었다.**
  categorize는 아직 없는 대상 slug를 기다리고, regroup은 이번 실행이 만들 slug를
  `Moves` 대상으로 받아들이며, 남은 것이 전부 떠나기로 예정된 분류의 삭제를 미룬다.
  두 바퀴에 수렴하고 세 번째 바퀴는 0건이다.
- **목록의 번호 없는 뒤쪽을 제목 자연 정렬로 세운다.** `탐색적 자료분석 : 문제`처럼
  번호가 제목 끝에 붙은 시리즈가 노션 작성 시각 순으로 흩어져 있었다. 앞 번호 규칙은
  그대로 두고 그 뒤쪽만 `naturalCompare`로 견준다. 51페이지(카테고리 17)의 순서가
  바뀐다 — 위 "형제 순서" 절 참고.
- **draft를 남에게서 완전히 가렸다.** 목록·카운트·본문 링크 어디에도 안 나오고
  `/p/{slug}`도 404다. 판정은 `store` 한 곳(`notHidden`)에 두고 로컬 확인용
  `-drafts` 플래그를 뒀다. 연쇄로 깨지는 자리 아홉 곳을 함께 처리했다 —
  표지가 draft인 분류 3개, 본문 링크 129개, 통째로 비는 인라인 DB 16개,
  글이 0이 된 분류 3개 등. 위 "공개 서버" 절 참고. 전수 검증으로 숨긴 글
  366편 전부 404, 1,078페이지 전부 200, 숨긴 글로 가는 링크 0개를 확인했다.
- **사이트 이름을 `열렬히.뛰기` / `Real.Leaner`로 바꿨다.** 이름에 `data-i18n`이
  없어서 Chrome의 온디바이스 번역이 **사이트 이름까지 기계번역하던 것**을 함께
  고쳤다. 키는 `<a class="brand">`가 아니라 양옆 글자에 따로 단다 — 사전 적용이
  `textContent`를 갈아치우므로 바깥에 달면 가운데 `<span class="dot">`이 사라진다.
- **낱개 링크를 묶은 상자에도 작성일과 `draft` 뱃지를 찍는다.** 인라인 데이터베이스를
  펼친 상자에는 날짜가 있는데 `groupLinkRuns`가 만든 상자에는 없었다 — 본문에서
  뽑아낸 링크에 slug와 글자밖에 없었기 때문이다. `PostTitlesBySlug`를
  `PostSummariesBySlug`로 바꿔 status와 `original_created_at`을 함께 가져온다.
  글 1357편과 카테고리 87개를 전부 훑어, 날짜 없는 줄이 이름 없는 인라인
  데이터베이스를 가리키는 3곳(posts에 행이 없다)만 남은 것을 확인했다.

**2026-09-01에 한 것**

- **GitHub에서 글을 옮겨오기 시작했다.** `Java_Modern`의 모던 자바 정리
  16편(11.5만 자)이 기존 Java 갈래 안에 `모던 자바`로 들어갔다. 글 1,356 →
  **1,372편.** 위 "GitHub 이관" 참고.
  - 멱등 키가 `source_ref`다(`migrations/006`). slug를 키로 쓰면 사람이 admin
    에서 바꾸는 순간 같은 글이 하나 더 생긴다.
  - **작성일은 첫 커밋 시각이다.** git이 이미 아는 값이라 지어낼 것이 없다 —
    2025-04부터 2026-02까지 챕터 순서와 시간 순서가 실제로 일치한다.
  - `<javascript>`가 화면에서 조용히 사라지던 것을 잡았다. 한글 표시
    (`<특징>`)는 우연히 안전해서 건드리지 않는다.
  - **`cmd/categorize`가 깨져 있었다.** `notion_page_id`를 문자열로 스캔하는데
    GitHub 글은 NULL이다. 출처가 하나 늘면서 드러난 자리다.

- **커리어를 통째로 없앴다.** 취업 준비 1 + 빅데이터 분석기사 42 +
  기술면접 : 예상 6 + 취업박람회 1 = **50편**과 분류 다섯을 뺐다.
  글 1,372 → **1,322편**, 분류 88 → **83개.**
  - `DropPosts`·`DropCategories`에 적었으므로 **재이관해도 안 돌아온다.**
    되돌리려면 그 묶음을 지우면 된다 — 덤프는 그대로 있다.
  - 얽혀 있던 것을 함께 걷었다: `Moves`의 `빅데이터 분석기사 → career`,
    같은 글의 `Covers`와 `PostMoves`, `StatusEdits` 두 건, 그리고 `수학 & 통계`
    본문에서 들어오던 링크 한 줄(`BodyEdits`). **그 링크를 두면 대상이
    posts에 없는 slug가 되어, 렌더러가 노션 인라인 데이터베이스로 알아보고
    엉뚱한 목록을 편다.**
  - 이 과정에서 도구 두 곳이 고쳐졌다 — 위 `DropPosts`의 "잎부터 지운다"와
    카테고리 절의 `groupDrops`·`allScheduledToLeave` 항목 참고.
  - categorize → regroup 두 바퀴에 수렴하고, 세 번째 바퀴는 지문이 같다.
    분류 83개 전부 200, `/career`와 지운 글 전부 404.

- **`알고리즘: 이론`의 빈 글 15편을 지웠다.** 전부 본문이 0바이트인데
  **`시간복잡도` 한 편만 draft가 아니라 unlisted라 공개 화면에 빈 페이지로
  나가고 있었다.** `기초 (1)` 7편과 `(2)` 2편은 그 분류의 글 전부라, 분류
  둘도 함께 뺐다(`DropCategories`) — 덤프가 만든 마디라 0으로 남으면 막다른
  길이다. 표지 본문의 두 링크도 `BodyEdits`로 걷었다. 글 1,287 → **1,272편**,
  분류 89 → **87개.**
  - 이 과정에서 **categorize와 regroup이 `sort_order`를 두고 싸우는 것**을
    찾아 고쳤다 — 위 "카테고리" 절 참고. **제 변경 이전부터 있던 것**이고,
    옛 코드에서 순서를 바꿔 돌려 재현했다.

- **백준을 알고리즘별로 다시 짰다.** 노션에서 온 `백준 단계별로 풀기 1~9 /
  10~25 / 알고리즘 강의 / 기타 백준 문제`는 **푼 순서지 무엇을 배웠나가 아니라서**,
  "이분 탐색 문제를 어디서 봤더라"에 아무 답도 못 했다. 문제 104편을 열 갈래로
  나눴다: 수학 & 정수론 21 · 문자열 14 · 그래프 탐색 13 · 자료구조 12 ·
  분할정복 & 재귀 12 · 동적 계획법 10 · 브루트포스 & 백트래킹 10 · 정렬 6 ·
  누적 합 3 · 기하 3. 글 1,322 → **1,287편**, 분류 83 → **89개.**
  - **35편을 지웠다.** 본문이 0바이트인 draft 23편, 사실상 빈 글 3편(16·26·97자),
    본문이 링크 목록뿐인 묶음 글 4편, 같은 문제가 두 벌인 것 5편(짧은 쪽을 지웠다).
    `curation.DropPosts`에 적었으므로 **재이관해도 안 돌아온다.**
  - **갈래를 만드는 자리가 새로 필요했다** — 위 "카테고리" 절의 `topics` 참고.
  - `알고리즘: 실전` 표지 본문은 없어지는 묶음으로 가는 링크 목록뿐이었다.
    `BodyEdits`로 링크를 전부 걷고 제목 줄을 한 문장으로 바꿨다. **남겨두면
    대상이 posts에 없는 slug가 되어 렌더러가 노션 인라인 데이터베이스로
    알아보고 엉뚱한 목록을 편다.**
  - categorize → regroup을 두 바퀴 돌려 수렴했고(첫 바퀴는 "미룬 글 104건"과
    "삭제 대기 4개"로 서로를 기다린다 — 문서에 적힌 그 교착이다), **순서를
    바꿔 돌려도 같은 지문**임을 확인했다. 분류 89개 전부 200, 지운 35편 전부 404.

- **갈래 카드를 제 분류로 내렸다.** 언어 카드 6장이 `Language`가 아니라
  `프로그래밍 언어`에 서고, `마크업 / 스타일링 / 표현식`도 제 갈래 7장을
  얻었다. 예전에는 한 화면이 두 층을 섞어 보여줬다 — 언어 카드는 아래 층의
  것이고 마크업 카드는 그 형제였다. 위 "레이아웃과 색" 앞 절의 갈래 카드 항목 참고.
  - `LanguageBranches`가 `PathBranches(categoryID, prefix)`가 됐다. 경로의
    세 번째 칸으로 고정돼 있던 것을 prefix로 받아, 같은 방법을 다른 평평한
    분류에도 쓴다.
  - **순서표와 그림표가 갈라지는 것을 테스트가 잡는다**(`TestBranchOrdersHaveArt`).
    실제로 `Swift`가 순서표에만 남아 조용히 카드가 되지 않고 있었다.
  - 13장의 링크가 전부 200인 것과 `Language`가 평소 목록으로 돌아간 것을
    확인했다.

- **평평하게 쏟아지던 카테고리 목록을 경로로 다시 묶었다.**
  `프로그래밍 언어`를 누르면 191편이 한 층에 늘어서 있었다. `pathtree.go`가
  `original_path`에 남은 잃어버린 층으로 C / C++ / Java / … 로 다시 세운다
  (`모던 자바`·`Cpp : STL` 등 마디 10개). 근거가 없는 분류는 예전 목록
  그대로다. 위 "공개 서버" 절 참고.
  - 포기 조건 넷에 돌연변이를 넣어 **넷 다 실제로 잡히는 것**을 확인했다.
    그 과정에서 빈 목록에서 영원히 도는 반복문 하나를 찾아 함께 고쳤다.

- **다크의 포인트를 초록으로 갈랐다.** 어두운 지면에서 형광 노랑은 대비가
  17:1이라 눈을 찌른다. 초록 인광은 터미널의 그것이라 어두운 판에 앉는다.
  **두 테마가 다른 색을 쓰는 유일한 자리**이고, "지면 하나에 검정 글자"라는
  규칙이 같아서 같은 사이트로 읽힌다.
- **UI 크롬에 한글 고정폭을 세웠다**(`--chrome`). 사이드바·경로·메타가
  고정폭이라 목록이 파일 트리처럼 읽힌다. **본문은 안 쓴다** — 990편의 긴
  한국어 산문을 고정폭으로 읽는 것은 고문이다. D2Coding·나눔고딕코딩이
  없으면 예전처럼 라틴만 고정폭이라 **웹폰트를 받지 않는다.**
- **로그인 화면을 터미널 창으로 다시 만들었다.** 이 화면이 하는 말은
  "너는 아직 아무도 아니고, 주인임을 증명하면 쓸 수 있다"인데 그건 셸에서
  sudo를 치는 장면이다 — 카드 한 장으로 두면 그 말이 안 들리고 남의 집
  로그인 폼처럼 보인다.
  - **위아래 띠가 창을 닫는다.** 위는 어느 창인지, 아래는 나가는 길이다.
    `esc 블로그로 돌아가기`가 nano의 `^X Exit`이 있는 그 자리에 앉아서,
    버튼 밑에 매달려 있던 것보다 제 집이 분명하다.
  - 창 밖은 지면이 아니라 **책상**이다(어둡게 깐다). 터미널은 언제나 어두운
    것에 얹혀 있다.
- **admin에 데이터 보기를 붙였다**(`/admin/data`, `internal/admin/stats.go`).
  글이 1,356편이라 목록을 넘겨서는 전체 모양이 안 보인다 — 어느 분류에
  쏠렸나, 안 쓰는 이미지가 있나 같은 것을 sqlite3을 직접 열지 않고 본다.
  - **방문자 수는 없다.** 이 서버는 그런 것을 남기지 않는다. 세는 것은
    전부 내가 쓴 것이다.
  - **0인 항목은 줄을 안 그린다.** 할 일 없는 목록에 0을 늘어놓으면 진짜
    할 일이 묻힌다.
  - 막대는 값이 아니라 **가장 큰 것에 대한 비율**이다. 눈금 없는 막대에
    절대값을 기대면 거짓말이 된다.
  - 분류별은 **직속 글만** 센다. 하위까지 더하면 상위가 전부를 삼켜서
    쏠림이 안 보인다.
- **목록 한 줄에서 고치기·보기·지우기를 끝낸다.** 예전에는 "보기"뿐이라
  치우려면 편집 화면까지 들어가야 했다. 지우기는 편집 화면과 **같은 흐름**을
  쓴다(refs로 먼저 묻고, 자식이 있으면 막고, rev를 확인한다).
- **admin 테스트 픽스처에 `notion_page_id`를 넣었다.** 이관은 언제나 그것을
  넣는데 픽스처가 비워두어서, "웹에서 쓴 글"과 구별되지 않았다 — 그 구별에
  기대는 것들(`upload-guard.sql`, 데이터 보기)을 확인할 수 없었다.

- **디자인 언어를 "제도판"으로 갈아엎었다.** 옛 팔레트의 무난함이 곧 "AI가
  만든 화면" 인상이었다. 흰 지면 + 잉크 괘선 + 옅은 하늘색 블록 하나로 바꾸고,
  **그림자·그라데이션·둥근 모서리를 전부 없앴다** — 위계는 테두리 두께로 만든다.
  위 "레이아웃과 색" 참고.
  - **머리띠(반전 블록)가 시그니처다.** 코드 라벨·목차·표 머리·인라인 DB·최근
    글이 같은 띠를 쓴다. 코드 라벨은 모서리 조각에서 폭 전체를 차지하는 띠가 됐다.
  - **포인트는 형광 노랑 하나이고 언제나 블록이다.** 하늘색으로 먼저 해봤는데
    무난해서 그 긴장이 사라졌다 — 편집숍의 인상은 색 하나를 세게 쓰는 데서 온다.
  - **다크는 판의 반전이다.** 띠가 밝은 판에 검정 글자가 되고, 포인트 하늘색은
    양쪽에서 같은 색이다 — 두 테마가 다른 사이트처럼 갈라지지 않는다.
  - 갈래 카드의 3D를 전부 걷어내고 `static/deck.js`를 없앴다. 설정 버튼도
    원에서 사각형이 됐다.
  - 375px 25페이지 스윕에서 넘친 곳 0개(검사기가 실제로 잡는 것도 확인).

- **최적화이론 실전 글 다섯 편 중 `선형계획법`을 맨 앞으로 올렸다** (2026-09-02,
  `curation.optimizationPracticeOrderEdits`). 다섯 편 모두 `created_time`이 분
  단위로 겹쳐 이름 자연 정렬로 서 있었는데(라그랑주 → 선형계획법 → 심플렉스
  알고리즘 → 심플렉스와 n분위수 → 쌍대정리: 선형) 사람이 `선형계획법`을
  앞으로 두기로 정했다. 나머지 넷의 상대 순서는 그대로 둔다.
  - **이 과정에서 categorize가 `categories.sort_order`를 덮던 것을 고쳤다** —
    아래 "카테고리" 절 참고. sortorder 파이프라인을 다시 돌리다가
    `categorize → regroup`과 `regroup → categorize`가 분류 순서를 다르게
    남기는 것을 발견했다.
- **`다변량분석 : 코드` 12편의 순서를 노션 원본과 맞췄다.** 그 과정에서
  **사람이 정한 순서가 화면에서 무시되던 것**을 고쳤다 — `migrations/005`의
  `sort_order_manual`. 위 "형제 순서" 참고.
  - `cmd/sortorder`의 수동 묶음 이름이 `"manual:linear-algebra"`로 박혀 있어
    묶음이 둘이 되면 두 묶음의 0번이 겹치는 것으로 보이던 버그도 같이 고쳤다.
    옛 버그를 되살려 "묶음 1개 → 2개"로 차이가 드러나는 것을 확인했다.

- **로그인 문을 사이드바에 냈다.** 예전에는 `/admin/login`이라는 주소를
  알아야만 들어갈 수 있었다. 감추는 것은 방어가 아니다 — 진짜 관문은 허용
  목록이지 주소를 모르는 것이 아니고, `/admin`이 밖에서 이미 303을 주고
  있어서 감춰서 얻는 것도 없었다. 로그인하면 그 자리가 계정 이름(`/admin`
  링크)과 로그아웃 버튼이 된다.
  - `pageData.AdminOn`과 `Editor`는 **다른 질문이다.** 앞은 "글쓰기 화면이
    있나", 뒤는 "지금 들어와 있나". 하나로 묶으면 로그인하기 전에는 로그인
    링크가 안 나오는 우스운 상태가 된다.
  - 로그아웃 핸들러를 `authenticator`에서 `Server`로 올렸다. 쿠키 하나를
    지우는 일이라 인증 설정이 필요 없고, 인증이 꺼진 서버에서도 등록해 둔다.
- **홈에 최근 글 6편을 붙이고 표제지를 다듬었다.** 눈썹줄에서 이름을 빼고
  ("기술 블로그 겸 학습 아카이브"), "읽기 전용 아카이브 · 작성 중인 글은
  공개하지 않습니다" 푸터를 사전까지 통째로 뺐다 — **더 이상 읽기 전용이
  아니다.** 위 "공개 서버" 절의 최근 글 항목 참고.
- **갈래 카드의 가짜 광원을 없앴다.** 155도 그라데이션이 카드 윗변에 밝은
  띠를 만들어 조명이 위에서 오는 것처럼 보였다 — 그림도 글자도 평면인데
  배경만 입체인 척하는 것이라 평평한 면(`--surface`) 하나로 바꿨다. 깊이는
  hover에서 실제로 기울 때만 준다.
- **한 주소의 과속을 막는다** (`internal/throttle`, 20/s + burst 60, 429 +
  `Retry-After`). **진짜 DDoS는 여기서 못 막는다** — 회선을 채우는 공격은
  Go 코드가 돌기 전에 회선이 먼저 찬다. 여기가 막는 것은 긁는 봇 하나,
  로그인 두드리기, 폭주하는 스크립트 하나다.
  - **주소 수에 상한이 있다**(4096). 없으면 출발지를 바꿔가며 두드리는 것으로
    맵을 무한히 키울 수 있어서, 제한 장치 자체가 메모리 공격 수단이 된다.
    가득 차면 **요청이 아니라 기억을** 버린다 — 통이 다시 가득 찬(한동안 안
    온) 주소부터.
  - **XFF는 맨 뒤 값을 쓴다.** 맨 앞은 보낸 쪽이 지어낼 수 있는 값이라 그걸
    키로 쓰면 제한을 통째로 피해간다. 맨 뒤는 Caddy가 자기 눈으로 보고 붙인
    값이다. **이 전제는 blog가 loopback에만 붙어 있다는 것에 기댄다** —
    밖에 직접 노출하는 순간 `ClientIP`도 같이 고쳐야 한다.
  - 실측: 200개 동시 요청 중 128개가 429, 순차적인 사람 속도는 전부 200.
- **루트에 README.md를 뒀다.** 처음 온 사람용 소개다. 진짜 문서는 여전히
  CLAUDE.md이고 README가 그렇게 안내한다.

**2026-08-31에 한 것**

- **글 지우기·`/` 팔레트·글 화면에서 바로 고치기를 붙였다.** 로드맵의
  "정해둔 것" ②③④가 전부 실물이 됐다. 위 네 절 참고.
  - **지우기는 지우는 것보다 말해주는 것이 일이다.** 자식이 있으면 아예
    막고, 표지·노션 출처·들어오는 링크는 알려준 뒤 확인을 받는다.
    **가리키던 링크는 글자로 푼다** — 안 풀면 그 slug가 "posts에 없는 것"이
    되어 렌더러가 노션 인라인 DB로 착각하고 엉뚱한 목록을 편다.
  - **팔레트는 서버에 묻지 않는다.** 실측 8.2ms, 요청 0건. 20개 항목에
    수식·표·코드 6종·애니메이션 2종·이미지 올리기가 들어 있다.
  - **애니메이션은 본문에 이름만 둔다**(`:::anim sort-bubble`). 렌더러가
    `html.WithUnsafe()`라 본문 `<script>`는 이미 실행되므로, 임의 JS를 담는
    길을 아예 열지 않는다. 실제 컴포넌트 둘(버블 정렬·이진 탐색)을 만들었다.
  - **바로 고치기는 web과 admin의 경계를 지키며 붙였다.** web은 세션을 모르고
    "지금 누가 들어와 있나"라는 질문 하나만 함수로 받는다(`WithEditor`).
    기본값은 닫힘이고, 요청마다 다시 묻고, draft를 가리는 규칙은 그대로다.
  - 돌연변이 13종을 넣어 테스트가 실제로 실패하는 것을 확인했다.

- **admin 3단계(저장)를 끝냈다.** 글이 실제로 DB에 들어가고 이미지도 BLOB으로
  저장된다. 위 "3단계로 만든 것 — 저장" 참고. 화면 위의 "아직 저장이 안 된다"
  띠가 사라졌다 — 그 띠가 사라지는 시점이 곧 3단계가 끝난 시점이라고 적어뒀던
  자리다.
  - **새 글에는 `notion_page_id`를 안 넣는다**(`source`는 `native`).
    `upload-guard.sql`이 그 NULL로 알아본다. 실제로 웹에서 쓴 글 3편과 올린
    이미지 2개가 가드에 `LOST`/`LOSTIMG`로 잡히는 것을 확인했다.
  - `rev`(불러올 때의 `updated_at`)로 **두 탭 덮어쓰기**를 막고, slug를 바꾸면
    그 글을 가리키던 본문 링크를 같은 트랜잭션에서 고친다.
  - 이미지는 내용 sha256이 멱등 키고 **SVG는 안 받는다**(`/img/`가 mime을 그대로
    실어 보내서 스크립트가 우리 도메인에서 돈다).
  - 쓰기에 `Origin` 검사를 얹었다. 세션 쿠키의 `SameSite=Lax`는 브라우저에
    기댄 한 겹이라 서버도 자기 눈으로 본다.
  - 메타 패널(분류·부모 글·형제 순서·작성일)과 **노션 글 경고 띠**를 붙였다.
  - 돌연변이 7종을 넣어 테스트가 실제로 실패하는 것을 확인했다. 롤백을 가르는
    테스트는 따로 만들어야 했다 — 첫 UPDATE에서 걸리는 경우는 롤백 유무가
    결과를 안 가른다.

- **코드 블록에 복사 버튼을 달았다.** 오른쪽 위 모서리에 아이콘만. 위
  "코드 블록의 복사 버튼" 참고. **서버가 아니라 스크립트가 만든다** — 클립보드는
  JS 없이 못 쓰므로 미리 찍으면 죽은 버튼이 남는다. 기준을 `<pre>`가 아니라
  껍데기로 잡은 것이 핵심이다(pre는 가로로 스크롤한다). `Assets.Copy`는
  `Assets.Code`와 **다른 판정**이라 `text`만 있는 글에도 버튼이 붙는다.

- **DB 정본을 로컬에서 서버로 뒤집고 백업을 놓았다.** 3단계(저장)를 막던
  마지막 선행 조건이다. 위 "배포 > DB 백업과 정본 방향" 참고.
  - 인스턴스에서 하루 한 번 `VACUUM INTO` + gzip으로 7벌을 남긴다
    (`blog-backup.timer`). **서버를 멈추지 않고**, 뜬 백업을 그 자리에서
    열어보고, 새것이 성공한 뒤에만 옛것을 지운다.
  - `fetch-db.sh`(평소, 서버 → 로컬)와 `upload-db.sh`(예외, 로컬 → 서버)를
    만들었다. 올리는 쪽은 `upload-guard.sql`이 **덮으면 사라지는 것**을 먼저
    본다 — 웹에서 쓴 글(`LOST`), 웹에서 고친 글(`STALE`), 웹에서 올린
    이미지(`LOSTIMG`). 한 줄이라도 나오면 멈추고, 끄는 스위치는 없다.
  - 실제로 돌려서 확인했다: 백업 3벌 `posts=1356 images=445` 지문
    `53ba1ba2b94b1228`, 받은 파일이 서버·로컬과 지문까지 동일, **가드가 세
    경우를 실제로 잡고** 같은 DB끼리는 침묵.
  - 한 번 걸렸다: `/var/backups/blog`가 0700이라 **glob이 `sudo` 바깥에서
    안 풀렸다.** 리터럴이 그대로 `sudo ls`로 넘어가 "No such file"이 났다.
  - **저장 코드에 제약이 하나 생겼다**: 웹에서 만든 글에 `notion_page_id`를
    넣지 않는다(`source`는 `native`). 가드가 그 NULL로 알아본다.

**2026-08-30에 한 것**

- **HTTPS를 붙였다.** `https://inquieto.dev`. Caddy가 80/443을 듣고
  `127.0.0.1:8080`의 blog로 넘긴다. 인증서는 Let's Encrypt에서 자동으로 받고
  자동으로 갱신한다. 위 "배포 > HTTPS" 참고.
  - **blog를 loopback으로 내렸다.** 밖에서 8080에 못 닿는 이유는 방화벽 규칙이
    없어서가 아니라 소켓이 127.0.0.1에만 붙기 때문이다. 특권 포트에 붙을 일이
    없어져 `AmbientCapabilities`도 비웠다.
  - **`www`는 일부러 안 넣었다.** Porkbun 파킹을 가리키고 있어서, 적으면 그
    이름의 검증이 실패하며 **인증서 발급 전체가 막힌다.**
  - 밖에서 확인: `/` 200(HTTP/2), `http://` → 308, 인증서 2026-11-28까지,
    3.4MB 이미지 BLOB이 바이트까지 동일, 8080·2019 둘 다 안 닿음, admin 404.
  - 배포 워크플로가 이제 **세 군데**를 본다: 인스턴스의 8080(blog 자신),
    인스턴스의 80(Caddy를 거친 길), 러너에서 `https://inquieto.dev/`.
    앞엣것만 보면 Caddy가 죽어도 초록이고, 인스턴스 안만 보면 DNS·방화벽·
    인증서가 빠진 채로 초록이다.
- **admin을 공개 주소에 열었다.** `https://inquieto.dev/admin`. 허용 목록의
  계정만 들어오고, 저장은 아직 501이다. 관문을 밖에서 전수로 확인했다 —
  위 "배포에서 admin 켜기" 참고.
- **배포에서 admin을 켜는 길을 만들었다.** 유닛에 `-admin`을 박지 않고
  `/etc/blog/admin.env`의 `$BLOG_ADMIN_FLAG`로 켠다 — 그 파일이 곧 client
  id·secret·허용 계정이 든 파일이라 **설정이 갖춰진 기계에서만 켜진다.**
  `deploy/enable-admin.sh`가 인스턴스에서 secret을 물어보고(화면에 안 찍힌다)
  0600 root:root로 넣는다. **저장소에도 GitHub Secret에도 secret이 없다.**

**2026-08-26에 한 것**

- **admin에 GitHub 로그인을 붙였다** (로드맵 2단계). 허용 목록에 적은 계정만
  들어온다 — **OAuth 성공만으로는 통과 못 한다.** 세션은 HMAC으로 서명한 쿠키
  하나고 세션 테이블은 안 만들었다. 관문은 mux 바깥에 둘러서 새 라우트가
  기본으로 막히게 했다. 설정이 모자라면 서버가 아예 안 뜨고, 인증 없이 여는
  길(`-admin-no-auth`)은 loopback에서만 열린다. 위 "2단계로 만든 것" 참고.
  - 돌연변이 셋을 넣어 테스트가 **실제로 실패하는 것**을 확인했다:
    `allows()`가 늘 통과, `guard`를 뺌, `":8080"`을 loopback으로 침.
  - **`deploy/blog.service`는 여전히 `-admin`을 주지 않는다.** 배포가 평문
    HTTP라 세션 쿠키가 그대로 오간다. HTTPS가 먼저다.
- **로드맵 뒤쪽 결정 넷을 적어뒀다**: 본문 정본은 계속 마크다운, `/` 팔레트는
  그 위에 얹기, 편집은 결국 공개 글 화면 안으로, 애니메이션은 본문 JS가 아니라
  이름 붙인 컴포넌트. 위 "정해둔 것" 참고.

**2026-08-25에 한 것**

- **admin 화면의 CSR 골격을 세웠다** (로드맵 1단계). 목록·편집 폼·실시간 미리보기·
  이미지 업로드 UI까지. **저장과 인증은 없고 501을 준다.** 미리보기가 실제 화면과
  같도록 렌더러·CSS·CDN 태그를 공개 쪽에서 그대로 가져다 쓴다. 위 "Admin 화면" 절 참고.
- **`layout.html`의 CSS와 CDN 태그를 이름 붙은 템플릿으로 떼어냈다**
  (`sitecss`, `katex-cdn`, `hljs-cdn`). admin이 같은 것을 쓰기 위해서고,
  **공개 페이지 출력은 한 바이트도 안 바뀐다**(표본 43페이지 확인).

**2026-08-24에 한 것**

- **밖에서 닿는 곳을 80과 22로 줄였다.** 껍데기만 남은 방화벽 규칙 5개를
  없애고, 공개된 글 990편에 자격증명이 없다는 것을 전수로 확인했다.
  위 "밖에서 닿는 곳" 절 참고.
- **첫 배포를 했다.** `http://35.230.119.252`. 공개 1,078페이지 전부 200,
  draft 366편 전부 404, 홈·`/dev`·`/intro`·`/data-math`가 로컬 렌더링과
  바이트까지 같은 것을 확인했다. **한 번 틀렸다** — WAL을 checkpoint하지 않고
  올려서 옛 DB가 배포됐고, 파일 해시가 그걸 못 잡았다. 위 "배포" 절 참고.
- **GCP 배포 파이프라인을 놓았다.** `main` push → 테스트 → 크로스 빌드 →
  IAP 터널로 scp → rename 교체 → 재시작 → 200 확인. 위 "배포" 절과
  `deploy/README.md` 참고. **DB는 CI가 안 건드린다.**
- **배포 전 서버 굳히기 세 가지.** HTTP 타임아웃, panic 복구, 404·500 화면이다.
  위 "없는 길과 터진 요청" 절 참고. 셋 다 **결정이 필요 없는 것**이라 먼저 했다 —
  호스팅·DB 배포 방식·HTTPS는 아직 정하지 않았다.
  - 1,078개 공개 페이지가 전부 200인 것과 draft 366편이 전부 404인 것을
    다시 확인했다(새 404 화면으로).
  - 헤더를 끝내지 않는 소켓이 5.0초에 끊기는 것을 실측했다.
  - panic 테스트가 미들웨어를 무력화하면 실제로 실패하는지 확인했다
    (통과만 하는 테스트가 아니다).

### 남은 일

1. **`parent_id` 나머지 카테고리 확장 여부 미정.** 지금은 5개 카테고리 172건이다.
   넓히기 전에 카테고리마다 `original_path` 일치율과 실제 화면 개선 여부를 먼저 본다.
2. (해결됐다. 아래 "변환에서 글자가 새는지 전수로 본다" 참고)
3. **`published` 선별이 0건이다.** 다만 이제 **배포를 막지 않는다** — 공개 규칙이
   "published만 보인다"가 아니라 "draft만 숨긴다"라서, `unlisted` 990편이 곧
   사이트다(2026-08-23). `published`는 홈을 다시 짤 때 고를 표시로 남겨둔다.
   현재 draft 366 / unlisted 990이다.
   - **draft를 통째로 지우는 건 보류했다.** 2026-08-21에 선형대수의 7건만 뺐다.
     남은 draft 중 88건은 본문이 있고(`get_next_line.c`, exam02 풀이 등),
     132건은 다른 글이 링크하고 있다. 분류를 지정해 하나씩 정하는 편이 안전하다.
4. **이름 없는 인라인 데이터베이스 19개** — `Untitled`/`New database`라 경로와
   짝지을 수 없다. 그중 11개는 audit `objects`에도 없어서 **행이 있는지조차 모른다**
   (노션 search가 이름 없는 인라인 DB를 잘 안 돌려준다). 노션 재조회가 필요하다.
   `Cpp part.2`는 이것뿐이라 아직 빈 페이지로 보인다.
5. **첨부파일**: `file` 12개 + `video` 8개 = 20블록. 노션 호스팅 URL은 서명이
   붙은 임시 주소라 **이미 만료됐을 가능성이 높다.** 덤프 스크립트는 이미지만 받아뒀다.
6. (해결됐다. 2026-08-23에 `서버 & API` / `클라이언트 & UI`로 갈랐다 —
   위 "카테고리" 절 참고)
7. 최상위 19건과 동시 생성 85건의 **수동 sort_order 지정**.
8. **노션 원본의 중복 글 8쌍 처리** — 아래 참고. 어느 쪽을 남길지 정하는 일이다.
   `parent_id`를 전체로 넓히기 전에 정하는 게 낫다. 넓히면 두 벌이 각각 다른
   부모 밑에 매달려서 더 눈에 띈다.
9. **외부 링크 카드의 파비콘 이관** — 지금은 카드에 기본 아이콘만 그린다.
   실제 파비콘을 쓰기로 정했고(2026-08-19), 방식은 **이관 때 받아서 DB에 넣는 것**이다.
   외부에서 그때그때 불러오는 방식(구글 s2 등)은 **쓰지 않는다**: 글을 열 때마다
   독자 IP가 제3자에게 가고, 링크 수만큼 외부 요청이 늘고, 오프라인·폐쇄망에서
   빈칸이 된다 — "단일 바이너리 + SQLite" 배포 전제와도 어긋난다.
   - `images` 테이블 패턴을 그대로 쓴다: 내용 sha256으로 dedup, BLOB + mime 저장,
     `GET /img/{sha256}`로 서빙. 호스트당 하나면 되므로 호스트를 멱등 키로 둔다.
   - 대상은 `bookmark` 24 + `link_preview` 12 + 본문 인라인 외부 링크다.
   - 그때까지는 카드에 기본 아이콘을 그린다. 파비콘이 들어와도 카드 마크업은
     그대로고 아이콘 자리만 바뀐다.
10. (해결됐다. 위 "레이아웃과 색" 절의 다크 팔레트와 화면 설정 참고)

11. (해결됐다. 2026-09-02에 확인했다.) **`탐색적 자료분석`과 `확률과정론`은
    `parent_id`로 층을 못 만든다** — 묶는 마디가 노션에서 페이지가 아니라 인라인
    데이터베이스(`탐색적 자료분석 : 목차`, `: 문제`)라 가리킬 대상이 없다.
    **그런데 화면은 이미 묶여 있다.** 표지 글 본문이 그 인라인 데이터베이스들을
    통째로 펼치고(`resolveBody`의 ②), `dropShownPostTrees`가 그렇게 안내된 글을
    아래 `글` 목록에서 뺀다. 그래서 두 화면 모두 **평평한 목록이 아예 없다** —
    탐색적 자료분석은 `기초지식`/`탐색적 자료분석 : 목차`/`: 문제` 세 상자,
    확률과정론은 `확률과정론`/`예시와 과제` 두 상자로 나뉜다.
    - 실제로 훑어 확인했다: 두 페이지 200, `글` 절 없음, `/p/` 링크가
      EDA 25개·확률과정론 19개이고 **중복 0건**이다.
    - `original_path`로 다시 묶는 길(`internal/web/pathtree.go`)은 여기서
      쓸 자리가 아니다. 그건 표지가 안내하지 않아 평평하게 남은 목록을 위한
      것이고, 이 둘은 표지가 이미 전부 안내한다.
12. **갈래 카드를 더 넓힐지 안 정했다.** 지금은 최상위 4개(`data-math`, `algorithm`,
    `cs-theory`, `dev`)와 `프로그래밍 언어`·`마크업 / 스타일링 / 표현식`·`프로젝트`에
    있다. `커리어`는 아직 목록이고,
    새로 생긴 `서버 & API`·`클라이언트 & UI` 밑도 목록이다.
    넓히려면 `internal/web/deck.go`의 `cardArtBySlug`에 그림을 채워야 한다 —
    **하나라도 비면 그 분류는 카드를 통째로 포기하고 목록으로 돌아간다.**
13. **본문 번역을 Safari까지 지원할 방법을 정해야 한다.** 현재 `window.Translator`는
    Chrome 138+ 데스크톱 전용이고 Safari의 주소창 페이지 번역은 사이트 JS가 실행할
    수 없다. 따라서 언어 버튼으로 모든 마크다운을 확실히 바꾸려면 둘 중 하나가 필요하다.
    - **DB 번역본(권장):** `post_translations`에 언어별 제목·본문을 저장하고 원문
      `updated_at`이 바뀌면 번역을 낡은 것으로 표시한다. 읽을 때 외부 요청이 없고
      “DB가 정본” 원칙과 맞지만, 번역본을 만드는 작업/관리 화면이 필요하다.
    - **클라우드 번역 + DB 캐시:** Google Cloud Translation·DeepL 같은 서비스를
      서버가 호출하고 결과를 DB에 보관한다. 바로 자동화할 수 있지만 API 키·비용과
      본문을 외부에 보내는 결정을 먼저 해야 한다. **키나 동의 없이 임의의 무료 번역
      엔드포인트로 본문을 보내지 않는다.**
    - **번역 품질은 일반 직역이 아니다.** 작성자는 데이터 분석을 거쳐 데이터 관리·처리,
      백엔드 프로그래밍·데이터베이스·클라우드에 관심을 넓힌 성인 개발자다. 영어는
      자연스럽고 절제된 비즈니스 영어, 스페인어는 특정 국가의 과한 구어체를 피한
      자연스러운 국제 비즈니스 스페인어로 쓴다. 학생·취업 준비생처럼 낮춰 말하거나
      원문의 한국어 어순을 옮기지 않는다. 코드·수식·URL·마크다운 구조는 그대로 둔다.
      이 목소리는 번역 프롬프트 버전과 함께 저장해야 재생성해도 일관된다. 일반 기계번역
      API보다 문체 지시를 받는 LLM + DB 캐시가 이 요구에는 더 맞다.
    - **Mac의 Ollama가 번역 작업을 가져가는 구조는 채택하지 않는다.** 개인 컴퓨터가
      켜져 있어야 클라우드의 새 글 번역이 끝나는 운영 방식은 원하지 않는다. GCP 배포
      뒤에는 글 저장이 `translation_jobs`를 pending으로 만들고, 별도 일회성 작업이
      Vertex AI 같은 사용량 기반 LLM을 호출해 `post_translations`에 넣는 쪽이 현재
      선호다. 웹 서버에 모델을 상주시켜 요청마다 번역하지 않는다. 원문 hash가 바뀐
      글만 다시 번역하고 읽을 때는 DB 번역본만 SSR한다. 초기 1365편 일괄 번역과 이후
      새 글 번역은 같은 작업을 쓰되 비용을 따로 판단한다.

### 조사해서 버그가 아니라고 판명된 것

- **"같은 글이 목록에 두 번 나온다"** — 렌더링은 정상이다. 수리통계1에서 링크
  46개 / 서로 다른 slug 46개, 중복 0건을 확인했다. **노션 원본에 같은 글이 두 벌
  있는 것이다**: 8쌍(`카이제곱분포`, `t분포`, `F분포`, `베타분포`, `중심극한정리 증명`,
  `이변량 정규분포`, `다변량 정규분포`, `다항분포`)이 **본문 sha256까지 똑같다.**
  2022-08-27에 만든 `확률분포 정리` 절을 2022-12-30에 `6. 분포의 활용 : 예시`로
  복사한 것이다. notion page id가 서로 달라 이관은 정상이다.
  **어느 쪽을 남길지만 정하면 된다.** 이건 코드가 아니라 데이터 판단이다.
