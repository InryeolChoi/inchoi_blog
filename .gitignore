# environment
.env
.env.*

# notion migration scratch data (large, disposable, regenerable via scripts/)
scripts/dump/
scripts/notion-audit-raw.json
scripts/notion-page-status.csv

# go build artifacts (cmd/ 이름과 같은 바이너리가 루트에 떨어진다)
/blog
/import

# 변환 결과 검토용 (덤프에서 언제든 다시 만들 수 있다)
/out/

# node
node_modules/

# sqlite
*.db
*.db-wal
*.db-shm

# 서버에서 내려받은 DB 백업 (deploy/fetch-db.sh)
/backups/

# macOS
.DS_Store
